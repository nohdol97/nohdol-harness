# 하네스 변경 이력 (harness changelog)

> 루트 하네스(AGENTS.md·CLAUDE.md·에이전트·스킬)의 경량 변경 로그. **항상-온 임포트 대상에서 제외**한다 — CLAUDE.md의 `@AGENTS.md` 주입에 딸려 매 세션 로드되지 않도록 AGENTS.md 본문에서 분리했다(ADR 021). 규칙의 단일 원본은 AGENTS.md, 이 파일은 그 변경 이력이다. 새 변경은 대상 파일별 표에 한 행 추가한다.

## AGENTS.md

| 날짜 | 변경 내용 | 대상 | 사유 |
|---|---|---|---|
| 2026-07-12 | 루트 하네스 초기 구성 (AGENTS.md, CLAUDE.md, orchestrate, metaskill, ADR 001) | 루트 전체 | harness-bootstrap-prompt.md 기반 초기 구축. 상세 결정은 docs/adr/001-initial-harness.md |
| 2026-07-12 | `project/`·`dev/` 분리 구조 반영 — 경로 규약·저장소 분리 규칙 추가, README 신설 | 1절, 5절, .gitignore, README.md | 하위 프로젝트를 독립 저장소로 운영하기로 사용자 확정. docs/adr/002-project-dir-separation.md |
| 2026-07-12 | `.claude/` 직접 생성 금지 규칙 명시. explorer·reviewer 에이전트, harness-review 스킬 신설 | 11절(구 12절), .agents/agents/, .agents/skills/harness-review/ | 심링크 원본 규칙이 암시에 그침(사용자 지적). orchestrate 반복 역할 2종과 주 1회 진화 관찰 절차의 실행 수단 부재 |
| 2026-07-12 | "완벽주의 금지" 절 삭제, 절 번호 재조정(10→9, 11→10, 12→11), 전 문서 교차 참조 갱신 | 구 9절, 전 문서 | 사용자 방침 — 완성도에 상한 없음. docs/adr/003-perfectionism-clause-removal.md |
| 2026-07-12 | reviewer 1차 독립 검증 반영 — gitignore 문구 정정, 추적 대상 열거 보완, ADR 트리거 ④ 추가, 라우팅 문구 완화, 도구 제약 서술 정직화, 레지스트리 임시 등록 5행 | 1·5·6·7·8절, 에이전트 2종, README, ADR 001 | 검증 발견 F1~F11 — `_workspace/harness-perfection-review/phase1_reviewer_verdict.md` |
| 2026-07-12 | 레지스트리를 REGISTRY.md로 분리, implementer·integrator 신설(표준 로스터 4종 완성), orchestrate 개정(로스터 재사용·게이트 단일 원본 이관, team-log 이벤트 계약) | 1·5·7절, REGISTRY.md, .agents/ | 사용자 확정 — AGENTS.md는 공용, 레지스트리는 설치 환경별. reviewer 3차 검증 통과. docs/adr/004-registry-separation-standard-roster.md |
| 2026-07-12 | 커밋 규칙 변경 — 작업 완료 시 커밋·푸시 기본 진행 | 5절 | 사용자 상시 승인 ("커밋 푸시는 작업 완료하면 무조건 진행") |
| 2026-07-12 | 이식성 개편 — REGISTRY.md 미추적 전환+설치 시 생성 의무, 경로 규약 REGISTRY.md 이관, 티어 표 탈모델명, harness-install·project-status 스킬 신설 | 1·5·9절, .gitignore, .agents/skills/ | 사용자 확정 — 여러 컴퓨터에서 사용. docs/adr/005-registry-untracked-portable-harness.md |
| 2026-07-12 | branch-workflow 스킬 신설 + 5절 하위 프로젝트 브랜치 규칙 추가 | 5절, .agents/skills/branch-workflow/ | 반복 실패 관찰(오래된 브랜치 시작→머지 충돌) + 사용자 인터뷰 확정(main 최신화→새 브랜치→PR 직전 rebase→PR 생성, 머지는 사용자) |
| 2026-07-13 | 12절 신설 — 하위 프로젝트 하네스 중앙 관리: 원본은 `.agents/projects/<이름>/` 단일, 미추적(설치처별), 배포 장치 없음(라우팅이 연결), 하위 전용 스킬·훅은 루트에 | 5·12절, .gitignore, metaskill, orchestrate, harness-review, harness-install, README | 사용자 확정 — 모든 하위 하네스는 루트 워크스페이스에서 관리하고, 컴퓨터마다 프로젝트가 다르므로 REGISTRY.md처럼 미추적. docs/adr/006 |
| 2026-07-13 | 레지스트리 컬럼 채우기 방식 확정 — 역할은 README 등 문서 직접 관찰, 연관 프로젝트는 실작업 관찰 시 갱신(초기 "미기록"), 경로 규약은 인터뷰 유지 | 1절, harness-install, orchestrate, metaskill | 사용자 확정 — 인터뷰 추측 대신 관찰 기반. 추측된 연관은 라우팅을 오염시킨다 |
| 2026-07-13 | 하위 스킬·에이전트 지연 생성 전환 — 초기 키트 폐지, 위치를 루트 접두어에서 `.agents/projects/<이름>/skills/`·`agents/`(미추적)로 변경, 하위 CLAUDE.md 규격 삭제 | 12절, metaskill, scaffold, .agents/projects/README | 사용자 확정 — 반복 관찰·등록 가치 발견 시에만 생성, 프로젝트 스킬은 git 미포함. docs/adr/007 |
| 2026-07-13 | 13절 신설(SDD+TDD 개발 방법론), doc-writer·team-review 스킬 신설 | 13절, .agents/skills/doc-writer/, .agents/skills/team-review/, README | 사용자 확정 — 스펙·테스트 주도 작업 규칙, 일관 형식 문서 작성, 규모 스케일링 팀 리뷰 |
| 2026-07-13 | 13절에 TDD 게이트 훅 추가(4항) — `.agents/githooks/tdd-gate.py`(+회귀 테스트·스펙) + `.claude/settings.json` 신설, 5절 추적 목록에 settings.json·.gitattributes·docs/specs 반영 | 5·13절, .agents/hooks/, .claude/settings.json, docs/specs/, .gitignore | 문서 규칙은 강제력이 없음 — 커밋 시점 차단으로 실행 계층 보증(사용자 승인, Superpowers 패턴). reviewer 1·2차 검증 F1~F12 반영. docs/adr/008 |
| 2026-07-13 | 14절 신설(세션 영속 작업 추적) + work-tracker 스킬 신설 | 14절, .agents/skills/work-tracker/, CLAUDE.md, README | `_workspace/`·세션 컨텍스트 미보존으로 세션 경계에서 작업 상태 유실 — ccpm 패턴 채택(사용자 승인). docs/adr/009 |
| 2026-07-13 | orchestrate 범용 게이트화 — 7절 3항 개정(구현·다단계 작업은 팀 필요성 판정 필수), orchestrate Phase 0-1 판정 4등급·검증 필수 규칙·하이브리드 표준 스켈레톤 신설, patterns.md 복합 패턴 절 확장, CLAUDE.md 스킬 우선순위 신설(특화>게이트, 하네스>외부), harness-review 우회 신호 확장, 규모 임계 단일 원본 지정 | 7절, .agents/skills/orchestrate/·harness-review/·team-review/·metaskill/, .agents/agents/implementer.md, CLAUDE.md, README | 사용자 요청 — 팀 필요 판정 주체 부재로 복잡 작업이 판정 없이 단일 컨텍스트 처리됨. reviewer 검증 F1~F11(외부 스킬 트리거 충돌 포함 — 사용자 지시 기준) 반영. docs/adr/010 |
| 2026-07-13 | PR 본문 템플릿 신설 — doc-writer 템플릿 5번 추가, branch-workflow 마무리 4단계가 참조(Closes #N으로 work-tracker 연결) | .agents/skills/doc-writer/, .agents/skills/branch-workflow/ | 사용자 요청 — PR도 문서처럼 고정 형식으로. 템플릿 단일 원본은 doc-writer. docs/adr/010 |
| 2026-07-13 | 표준 로스터 4종→7종 확장 — architect·troubleshooter·infra-specialist 신설, implementer Edit 서술 정정, orchestrate 로스터 표·스켈레톤 담당 갱신 | .agents/agents/, .agents/skills/orchestrate/, README | 사용자 승인 — 인기 하네스(BMAD·Superpowers 등) 로스터 대조 검토: 스켈레톤 Phase ② 공백(architect), 인과 확정 역할 부재(troubleshooter), 인프라 프로젝트 대응(infra-specialist). docs/adr/011 |
| 2026-07-13 | release 스킬 신설(머지 이후 배포 워크플로우 — 런북→단계별 확인→검증→work-tracker 마무리) + 5절 배포·릴리스 규칙 앵커 + team-review 관점 축에 UX·접근성 추가 | 5절, .agents/skills/release/, .agents/skills/team-review/, CLAUDE.md, README | 사용자 승인(풀스택 커버리지 점검) — PR 머지 이후 절차 공백은 가드레일 최중요 구간의 무절차, 프론트 diff에 접근성 검증 관점 부재. 5절 앵커는 Codex 세션 구속용(규칙은 문서가 운반 — reviewer phase6 F1) |
| 2026-07-14 | agentsview 연동 — harness-review 신호 실측(session search·stats)·secrets scan 사후 검증, harness-install 3단계(도구+finding-history 스킬 전역 설치) 신설, 11절 외부 도구 스킬 전역 설치 규칙 | 11절, harness-review, harness-install | 사용자 승인(kenn-io/agentsview 검토) — 주간 점검의 신호 관찰이 세션 기억 의존에서 세션 이력 실측으로. 프로젝트 모드 설치는 심링크 경유로 공용 저장소를 오염 |
| 2026-07-14 | agentsview-daemon SessionStart 훅 신설(스펙+회귀 테스트) — 세션 시작 시 동기화 데몬 자동 기동, 로컬 DB 고정·외부 동기화 금지 문구 보강 | .agents/hooks/, .claude/settings.json, docs/specs/, harness-install, README | 사용자 요청 — 동기화 수동 기동 제거. fail-open으로 미설치 환경 무간섭 |
| 2026-07-14 | harness-review-reminder SessionStart 훅 신설(스펙+회귀 테스트) — 마지막 점검 7일 경과 시 세션 시작에 실행 지시 주입, harness-review에 완료 마커 갱신 단계 추가 | 8절, .agents/hooks/, .claude/settings.json, docs/specs/, harness-review, README | 사용자 요청(주간 점검 cron화) — 제안 승인에 사람이 필요하고 신호 데이터가 로컬에 있어, 무인 cron 대신 세션 트리거 방식 채택 |
| 2026-07-14 | 점검 주기 2단 확장 — 일일 경량 모드(1일, 3신호 스캔만·무신호 시 한 줄) 신설, 리마인더 훅 마커 2개 판정으로 개정 | 8절, .agents/hooks/harness-review-reminder.py, harness-review, docs/specs | 사용자 확정 — "일주일은 너무 길다, 비효율은 매일 새 세션에서 점검" |
| 2026-07-14 | 설치처 프로필(개인/사내) 신설 — 사내는 추적 하네스 파일 수정·커밋·푸시 금지, 개선 사항은 `_workspace/harness-updates.md` 대기 큐 기록 후 개인 설치처에서 metaskill로 적용. 4절 `_workspace/` 미보존에 큐·마커 예외 명시 | 4·5절, harness-install, harness-review, metaskill, README | 사용자 확정 — 하네스를 사내에서도 쓰는데 저장소는 개인 소유라 사내에서 개인 git 푸시 불가. docs/adr/012 |
| 2026-07-14 | 훅 동작 가시화 — SessionStart 훅 2종이 기한 전·미설치에도 상태 한 줄을 남기고 첫 응답에서 사용자 보고, 점검·개선 내역은 `_workspace/harness-ops-log.md` 운영 로그에 누적(harness-review 4단계·metaskill 완료 절차) | 4절, .agents/hooks/ 2종(+테스트·스펙), harness-review, metaskill | 사용자 요청 — "동작을 했는지 채팅창에 남아야, 개선했으면 뭘 개선했는지도". 침묵 설계는 정상과 미실행을 구분 불가 |
| 2026-07-14 | 진화 트리거 4신호 확장 — ④ 수축·효율(3주+ 무호출 폐기 제안, 토큰 과소모 패턴 개선 제안, 주간 한정) 신설, ② 정의에 같은 정정 2회+ 포함, 주간 무결성에 선언-미구현·로딩 비대 점검 추가, 신호 4개 고정 | 8절, harness-review, metaskill, harness-review-reminder.py, README | 사용자 확정 — "신호 3개로 충분한가 + 토큰 낭비 감축". 확장 신호만으로는 비대·구조 결함을 못 본다. docs/adr/013 |
| 2026-07-14 | tdd-gate에 git commit-msg 계층 추가(13절 4항) — `.agents/githooks/` shim(게이트+로컬 훅 체인) + 전역 `core.hooksPath` 등록(harness-install 1단계), tdd-gate.py 이중 모드 리팩토링, 회귀 테스트 C14~C21 | 13절, .agents/githooks/, .agents/hooks/, harness-install, docs/specs/, docs/adr/014 | 사용자 요청 — Codex 세션은 `.claude/settings.json` 훅을 읽지 않아 게이트 미적용. 도구에 따라 강제력이 달라지는 비대칭 해소. ADR 008의 "Claude Code 한정" 결정 대체 |
| 2026-07-14 | tdd-gate git 계층 단일화(13절 4항) — PreToolUse 등록·stdin 명령 파싱 계층 제거, 스펙 R1~R7·C1~C13 재편, harness-review 주간 무결성에 전역 hooksPath 등록 점검 추가 | 13절, .claude/settings.json, .agents/hooks/, harness-review, docs/specs/, docs/adr/015 | 사용자 결정 — 이중 계층의 혼란 비용 > 중복 가치. 타 설치처는 최신 코드 기반 harness-install 재실행으로 등록, 공백은 주간 점검이 감시. ADR 014의 "계층 추가" 결정 대체 |
| 2026-07-14 | tdd-gate 스크립트·테스트를 `.agents/githooks/`로 이동 — hooks/는 Claude 세션 훅 전용으로 정리 | .agents/hooks/, .agents/githooks/, 13절 경로, README, 스펙 | 사용자 결정 — 단일화 후 hooks/에 git이 부르는 스크립트가 남는 것은 디렉토리 의미와 어긋남. 호출자 기준 배치(Claude 세션 훅=hooks/, git 훅=githooks/) |
| 2026-07-15 | 5절에 루트 원격 세션 PR 사이클 절차화 — 머지 확인 후에만 브랜치 재정렬, fast-forward 푸시, 머지 전 스택 시 PR 본문 갱신 | 5절 | 일일 점검 신호 ①(같은 요청 반복 — 하네스 개선 PR 하루 6건) 사용자 승인. 절차 부재로 매 세션 재유도, 머지 미확인 재정렬로 커밋 유실 실발생(2026-07-14) |
| 2026-07-15 | 훅 공통 부트스트랩 `_common.py` 신설 — stdio UTF-8 재구성 단일 원본화, 세션 훅 2종은 동일 디렉토리 import·githooks/tdd-gate는 교차 디렉토리 import 전환(유실 시 no-op 폴백), 스펙 템플릿에 훅 크로스 플랫폼 체크리스트 추가 | .agents/hooks/(+_common_test.py), .agents/githooks/tdd-gate.py, docs/specs/, doc-writer references/templates.md | 일일 점검 신호 ②(cp949 수정이 훅 3종 반복 적용된 fix 3연쇄) 사용자 승인. 복제된 공통 로직은 결함 수정 비용을 N배로 증폭 |
| 2026-07-15 | 15절 내부 통신 언어 신설 — 모델만 읽는 산출물(발행 프롬프트·팀 중간 리포트·P2P 본문)은 영어, 사용자가 읽는 것(채팅·PR·하네스 문서·판정 문서·제안서·운영 로그·트리거 키워드)은 한국어, 언어 섞임 방지 가드 3항 | 15절, 에이전트 정의 7종, orchestrate, team-review, doc-writer(+templates), harness-review | 사용자 승인(토큰 절약 질문 → 부작용 없는 적용 요청) — 한국어는 같은 내용에 ~1.5-2배 토큰, 중간 리포트는 쓰기 1회+읽기 N회. docs/adr/016 |
| 2026-07-16 | 16절 코드 최소주의(제품 코드) 신설 — ponytail 결정 사다리 7단계·예외 영역 이식, team-review에 "단순성/과설계" 관점 축 추가. 원칙+리뷰 관점만 이식(플러그인·Node 훅·6스킬은 비목표) | 16절, team-review, docs/proposals/2026-07-15-ponytail-adoption.md | 사용자 승인(ponytail 분석·설계 → 최소 채택 요청) — 제품 코드 분량 규율 공백을 채우되 규칙 운반체는 문서(11·12절)라 배송 기계는 복제하지 않음. docs/adr/017 |
| 2026-07-16 | claude-mem 최소 채택 — 3절에 `<private>` 외부 발행 제외 마커, 4절에 점진적 공개(2단) 리포트 규약 신설. worklog-reminder SessionStart 훅(+테스트·스펙)·explorer 출력·work-tracker 흐름 2에 반영 | 3·4절, .agents/hooks/worklog-reminder.py, .claude/settings.json, explorer.md, work-tracker, docs/specs/·proposals/ | 사용자 요청(claude-mem 분석 → 적용 검토·적용) — 세션 경계 진행-기록 유실·리포트 재독 토큰 공백을 무의존 착안 이식으로. 워커·벡터 DB·전수 캡처는 이식성(ADR 005)·최소주의(ADR 017)와 충돌해 기각. docs/adr/018 |
| 2026-07-16 | Codex SessionStart 훅 병행 — 리마인더 2종을 `.codex/hooks.json`(추적)에도 등록, 11절 병행 규칙·5절 추적 목록·harness-install 활성화 단계(`[features] codex_hooks`)·훅 스펙 2종 Codex 노트 갱신. 스크립트 무변경(도구 무관) | 5·11절, .codex/hooks.json, .gitignore, harness-install, docs/specs/(hook 2종), docs/adr/019 | 사용자 요청(맥북 Codex에서도 동작) — Codex v0.114+가 동일 포맷 SessionStart 훅 지원. agentsview-daemon은 Codex 관측 미확인으로 제외, 활성화는 머신 로컬(tdd-gate 선례). docs/adr/019 |
| 2026-07-16 | Codex 훅 활성화를 저장소 커밋으로 전환(항상 켜짐) — `.codex/config.toml`(추적) 신설·5·11절·harness-install·스펙 갱신, 머신 로컬 등록 단계 제거. #17532 리스크는 전역 config 폴백으로 수용 | 5·11절, .codex/config.toml, .gitignore, harness-install, docs/specs/(hook 2종), docs/adr/019 | 사용자 지시("Codex hook 항상 활성") — 머신마다 켜는 단계 없이 클론 즉시 동작. docs/adr/019 |
| 2026-07-16 | kepano/obsidian-skills 관례 최소 이식 — defuddle 스킬 신설(웹 본문 추출, 실패 시 WebFetch 폴백), description 저작 공식에 ④ 부정 트리거(`Do NOT use for …`) 추가, harness-install 3절에 defuddle 선택 설치(3b) 추가. 도메인 스킬 4종(obsidian-markdown·bases·json-canvas·cli)은 비목표(이 워크스페이스에 Obsidian 미사용) | .agents/skills/defuddle/, metaskill(스킬 공통 규칙 #2·agent-rules.md), harness-install(3·7절) | 사용자 검토·승인(obsidian-skills 적용성 검토) — defuddle은 도메인 독립 토큰 절감, 부정 트리거는 CLAUDE.md 변경 이력에 반복 관찰된 오라우팅 실패에 대한 저비용 처방. 얇은 래퍼·문서 운반 원칙 유지(플러그인 마켓플레이스 패키징은 사설 모노 워크스페이스라 비목표) |
| 2026-07-16 | claude-mem·obsidian·ponytail·Codex 라운드 정합 보완 — 6절에 제안(proposals) 문서 타입·문서 지도(MOC `docs/README.md`)·인덱스 동기화 의무 신설, 5절 추적 문서 열거에 proposals·README 추가, `docs/README.md` MOC 신설(ADR 19·스펙 5·제안 2, 대체 체인·상태), 점진적 공개를 reviewer·integrator·troubleshooter 출력에 반영, harness-review 무결성에 Codex 훅 등록 일관성·MOC 최신성 점검 추가, README 낡음 정리(worklog·defuddle·.codex·proposals·16절) | 5·6절, docs/README.md, reviewer·integrator·troubleshooter, harness-review, metaskill, README | 사용자 요청(추가 기법 전체 재검토·보완) — 빠른 다PR 확장으로 생긴 정합 공백: 19 ADR 무인덱스(obsidian 탐색성), proposals 미문서화, 점진적 공개 발견-다수 에이전트 미참조(선언-미구현 위험) |
| 2026-07-16 | k8s·인프라 매니페스트 변경 라우팅 강화 — 7절 3항에 continuation 게이트 재진입 규칙, 7절 5항 신설(k8s 매니페스트는 infra-specialist 경유), infra-specialist 2절에 admission 제약(LimitRange·ResourceQuota·StorageClass, 컴퓨트+스토리지) 선확인 pre-flight·7절 체크리스트 항목, orchestrate Phase 0-1에 continuation 게이트 재진입 규칙, CLAUDE.md 트리거 반영 | 7절, .agents/agents/infra-specialist.md, .agents/skills/orchestrate/SKILL.md, CLAUDE.md | 하네스 우회 신호③ + 실제 실패 — PVC 작업 continuation이 orchestrate 게이트를 건너뛰고 직접 매니페스트 수정, 네임스페이스 리소스 상한 미확인으로 pod FailedCreate. 사용자 승인(제안 검토 + infra-specialist 강화 요청) |
| 2026-07-16 | 하네스 자산 전체 검토 반영 — 스킬 공통 규칙 #2 description 길이 기준 상향(500자→800자 권장·하드캡 ~1024자·부정 트리거 우선), 부정 트리거 일괄 추가(explorer·troubleshooter·orchestrate·metaskill·harness-review·team-review·doc-writer), troubleshooter 접근 실패 재시도 절, severity 등급 단일 원본(integrator 2절) 인용(reviewer·architect·troubleshooter), 정확도 정정(harness-review 제목 2모드·team-review 관점 축 7개·metaskill 체크리스트 15항목·metaskill 점검 트리거 disambiguation) | .agents/agents/(explorer·troubleshooter·reviewer·architect), .agents/skills/(metaskill·orchestrate·harness-review·team-review·doc-writer) | reviewer 3종 팬아웃 자산 검토 — 부정 트리거 규약(2026-07-16 obsidian 이식)이 선언 후 defuddle에만 적용, 500자 고정이 그 규약과 충돌해 다수 스킬이 초과. 사용자 승인(모든 수정 적용) |
| 2026-07-16 | 인프라 도메인 리뷰 특화 — team-review 팀 모드에 인프라 특화 규약 신설(대상이 k8s·AWS·매니페스트면 정확성·보안 관점 중 인프라 해당분을 infra-specialist가 리뷰 모드=읽기 전용·Edit 금지·design 티어·비작성자 한정으로 담당) + 인프라 리뷰 체크리스트(리소스 상한/admission·rollout/PDB·시크릿 참조·스코프·롤백), infra-specialist 1절 ④·10절 티어 정합. 웹·앱·백엔드 도메인 리뷰어는 기각(16절 최소주의) | .agents/skills/team-review/SKILL.md, .agents/agents/infra-specialist.md | 사용자 관찰("구현 방향별로 reviewer도 특화 필요 없나") — build는 도메인 특화(infra-specialist)인데 review는 관점 특화만이라 비대칭. generic reviewer가 운영 지식(네임스페이스 상한·admission) 못 봄, k8s 매니페스트 리뷰 갭이 실패 증거. 새 에이전트 0개(infra-specialist 재사용). docs/adr/020 |
| 2026-07-16 | CLAUDE.md `@AGENTS.md` 임포트로 단일 원본 항상-온 주입 전환 + 변경 이력 이 파일로 분리 + CLAUDE.md 중복 트리거(continuation·k8s) 슬림·출력 언어 항상-온 앵커화 | CLAUDE.md, AGENTS.md 5·6·11·15절, docs/harness-changelog.md | 사용자 대면 출력이 영어로 새는 문제 — §15가 AGENTS.md(온디맨드)에만 있어 항상-온이 아님. Claude Code 공식 패턴(@AGENTS.md)으로 AGENTS.md 전문을 세션 시작 자동 주입(Codex 네이티브 로드와 패리티), 임포트 footprint는 변경 이력 분리로 상쇄. docs/adr/021 |
| 2026-07-16 | 리마인더 노이즈·타임존 정정 + ADR 021 후속 문구 정합 — harness-review-reminder 기한 전 상태 한 줄을 무출력화(C4 재개정), 경과 판정 KST(UTC+9) 신설(스펙 R7·C9·테스트), harness-review 마커 KST 명시, metaskill·harness-review의 변경 이력·CLAUDE.md 생성/점검 문구를 changelog 분리·@import에 맞춰 갱신 | .agents/hooks/harness-review-reminder.py(+테스트), docs/specs/2026-07-14-harness-review-reminder-hook.md, .agents/skills/(metaskill·harness-review) | 사용자 요청 — "오늘은 이미 했다" 채팅이 세션마다 노이즈, 날짜 기준이 KST 아님(컨테이너 UTC). + ADR 021 changelog 분리 후 남은 문구 정리 |
| 2026-07-16 | 하네스 전체 감사(리뷰어 4종 팬아웃) 반영 — ① tdd-gate 차단 exit 65 전용화(shim은 65만 차단 — 게이트 손상 시 머신 전역 차단 해소, C13b)·R3 allowlist(tests.py·conftest.py)·알려진 한계 2건 기재 ② agentsview-daemon nominal 침묵(재기동 한 줄만) ③ 리마인더 마커 읽기 실패=기록 없음(C10)·KST 결정성 테스트(C9 보강) ④ _common 보증 4종화(worklog 편입) ⑤ settings.json matcher(startup·resume·clear — compact 재실행 억제) ⑥ 문서 정합: ADR 001·004 대체 배너+MOC, ADR 014·015 경로 주의, 스펙 모순 정정(리마인더 비목표·agentsview 무출력·중복 C5), README(CLAUDE.md·harness-review 서술), metaskill 골격 문구, harness-install 침묵 정상화 안내, 5절 gitignore README 예외 표기 ⑦ 자산: troubleshooter description 인용, work-tracker 부정 트리거, infra-specialist 리뷰 모드 Edit 금지=프롬프트 수준 명시, orchestrate 로스터 티어 오버라이드 표기 | .agents/githooks/(tdd-gate.py·commit-msg·테스트), .agents/hooks/(agentsview-daemon·harness-review-reminder·_common + 테스트 3종), .claude/settings.json, docs/specs/ 4종, docs/adr/(001·004·014·015), docs/README.md, README.md, AGENTS.md 5절, .agents/skills/(metaskill·harness-install·work-tracker·orchestrate), .agents/agents/(troubleshooter·infra-specialist) | 사용자 요청("프로젝트 전체 개선점") → 리뷰어 4종 감사 25건(must-fix 1·should-fix 8·경미 15) 사용자 승인(agentsview는 침묵 결정). 판정 원본: `_workspace/harness-full-audit-2026-07-16/phase2_integrator_verdict.md` |
| 2026-07-17 | 2차 전체 감사 반영(14건) — implementer 매니페스트 범위 제거+infra-specialist 부정 경계(S1), release 웹 분기 k8s 한정+관리형 플랫폼(Vercel) 행+DB 마이그레이션 pre-flight 규칙(S2·S3), metaskill 공통 규칙 개수 정정 4→6(S4), MOC ADR 017 날짜(S5), 절 번호·포인터 정정 5건(4절 8절④·16절 12절·team-review·troubleshooter·architect 용어), defuddle 근거 없는 인용 제거·deep-research 내장 표기, branch-workflow에 orchestrate 게이트 선행 순서 명시 | 4·16절, implementer·architect·troubleshooter, release·metaskill·team-review·defuddle·harness-install·branch-workflow, docs/README.md | 사용자 요청 전체 검토 — 4축 병렬 감사(스킬·에이전트/훅·문서·풀스택 커버리지), must-fix 0·should-fix 5·watch 9. `_workspace/harness-audit-2026-07-16/final_report.md` |
| 2026-07-17 | superpowers 규율 착안 3건 문서 이식 — ① metaskill 스킬 공통 규칙 7 신설(압박 테스트 — 행동 규율 스킬은 baseline 위반을 서브에이전트로 관찰 후 작성·준수 확인, 참조형 제외) ② 13절 2항 신선한 증거 규율(완료·통과 주장 = 보고 직전 실행 출력만 인정, 추정·이전 결과·에이전트 success 보고 불인정) ③ implementer 4절 리뷰 수신 규율(검증 전 수용 금지·불명확 시 전체 중단 후 질문·미수용 사유 회신). 플러그인 통설치·나머지 11스킬 기각 | AGENTS.md 13절, .agents/skills/metaskill/SKILL.md, .agents/agents/implementer.md, docs/(adr/022, proposals/2026-07-17-superpowers-adoption.md, README.md) | 사용자 요청 — superpowers(obra, 14스킬) 도입 검토 → 원문 대조로 이식 후보 3건 선별 → "3개 다 적용" 승인. 구조(게이트·훅·독립 검증)가 못 잡는 잔여 공백만 문장 수준 보강 |
| 2026-07-17 | explorer 발행 유출 개선 — ① 7절 6항 신설(수집형 서브에이전트는 explorer 타입 발행, general-purpose·내장 Explore 금지, 원인 조사→troubleshooter·검증→reviewer) ② CLAUDE.md 라우팅 앵커에 발행 타입 1줄 ③ harness-review-reminder 훅 지시문에 explorer 타입 명시(테스트 C1·C2 단언 추가 RED→GREEN 21/21, 스펙 R3 갱신) | AGENTS.md 7절, CLAUDE.md, .agents/hooks/harness-review-reminder.py(+테스트), docs/specs/2026-07-14-harness-review-reminder-hook.md | 사용자 확인 요청("explorer 동작 안 하는 경우가 많은 것 같다") → agentsview 실측: 5일간 explorer 호출 1회, 수집형 발행 4건 중 3건 general-purpose 유출(그중 2건은 스킬 명문화 이후 — 신호 ③). description 트리거만으로는 발행 순간에 참조 안 됨 → 발행 지점(훅 문구·항상-온 앵커·7절)에 타입을 고정 |
| 2026-07-17 | 에이전트 호출 경계 감사 반영 — 부정 트리거 4종 신설(reviewer↔integrator 상호 경계: 단일 산출물 검증 vs 다중 리포트 팬인, architect→doc-writer 스킬·reviewer, infra-specialist→implementer 역방향) + explorer에 7절 6항 지정 타입 명시(description·재호출 지침 — general-purpose·내장 Explore 대체 금지). doc-writer에 architect 상호 경계 1줄(팀 모드 스펙 초안은 architect — reviewer 검증 F2). 10섹션·frontmatter·티어·tools는 전 에이전트 이상 없음(감사 결과) | .agents/agents/(explorer·reviewer·integrator·architect·infra-specialist), .agents/skills/doc-writer/SKILL.md | 사용자 요청("용도에 맞게 잘 호출되어야") — 부정 트리거가 3종(implementer·troubleshooter·explorer)에만 있어 나머지 경계가 겹침 매칭에 노출. 압박 테스트 6/6(병합+검증 분리·스킬 vs 에이전트·인프라/앱 분리 포함 — _workspace/agent-boundary-audit/pressure_test_results.md) |
| 2026-07-17 | 스킬 전수(11종) 외부 충돌 감사 반영 — ① orchestrate 트리거 확장(고쳐줘·기능 추가·리팩토링·refactor·debug 게이트 선행·continuation)+외부 스킬 선행 선언(sc:implement·investigate·qa 이름 지목) ② team-review 부정 트리거에 외부 리뷰 스킬 확장(gstack review·cso·sc:analyze — security audit 포함) ③ work-tracker에 checkpoint류 트리거 흡수 ④ branch-workflow·release에 자동 머지 금지 명시(ship·land-and-deploy 차단, 머지는 사용자) ⑤ CLAUDE.md 우선순위 매핑 확장(investigate·QA·security audit·land·checkpoint·머지 가드) ⑥ doc-writer·orchestrate·team-review 800자 압축(긍정 서술 압축, 부정 경계 유지) ⑦ 7절 7항 신설 — 외부 스킬 우선순위 원칙의 단일 원본(reviewer F2: 원칙이 CLAUDE.md에만 있어 §7 인용이 공중에 뜨고 Codex 세션엔 규칙 부재) + 자동 머지 위임 금지 | AGENTS.md 7절, .agents/skills/(orchestrate·team-review·work-tracker·branch-workflow·release·doc-writer), CLAUDE.md | 사용자 요청("orchestrate가 특히 잘 호출·내장 스킬 충돌 확인") — gstack investigate·ship·checkpoint·land-and-deploy의 선제 발동(proactively invoke) 지시가 하네스 트리거와 정면 충돌. 압박 테스트 7/7 하네스 승("명시 배제 없으면 investigate·qa로 유출 가능성 높음" 부연 — 이름 지목이 결정 요인, _workspace/skills-collision-audit/pressure_test_results.md) |
| 2026-07-17 | orchestrate Use 트리거에 "작업해줘" 추가(794자) — "작업 시작"→branch-workflow·"작업 등록"→work-tracker 복합어 우선 유지 확인 | .agents/skills/orchestrate/SKILL.md | 사용자 요청 — 한국어 포괄 트리거 보강. 단어 1건이라 reviewer 생략(충돌 축 직접 확인) |
| 2026-07-17 | 8절에 "실수 즉시 기록" 규율 신설(신호 ②의 사전 단계) — 재발 가치 있는 실수는 1회 발생 즉시 기록: Claude는 auto-memory(인덱스 줄은 발동 상황 형식), Codex는 같은 저장소 파일 직접 기록·조회, 공용 교훈은 하네스 규칙·훅으로 승격. 검색형 교훈 DB는 규모 임계 전 비도입(ADR 018 동일 판단) | AGENTS.md 8절 | 사용자 확정("실수 반복이 제일 비효율" → 상시 목표: 속도·무실수·토큰 절감) — 기존 신호 ②는 2회 반복을 기다려 2회차 실수를 못 막고, memory 기록이 재량이라 교훈이 세션과 증발(zsh 단어 분리 실사례). 압박 테스트 4/4(시간 압박 기록·사소 건 비기록·승격 제안 포함) |
| 2026-07-17 | AGENTS.md 다이어트 1차(39,503→36,616 bytes, -7.2%) + 크기 예산 규칙 신설 — 사례·실측 서술을 changelog/ADR 포인터로 이관(원격 PR 사이클·프로필 대기 큐 형식·continuation 실사례·7절 6항 실측·Codex 훅 병행 경위·점진적 공개 이유·ponytail 이식 경위·CLAUDE.md 로딩 역할·헤더/15절 중복 이중 서술), harness-review 주간 무결성에 "AGENTS.md 40k bytes 초과 시 다이어트 제안" 항목 추가 | AGENTS.md 전반, .agents/skills/harness-review/SKILL.md | 사용자 지적("AGENTS.md 너무 방대해지는 것 아닌가") — 항상-온 임포트라 성장이 곧 고정 토큰 비용 + 규칙 매몰. 규칙+이유는 유지, 증거만 포인터로(ADR 021 수법 2탄). -15~20% 추정 대비 미달(-7.5%)은 잔여 산문 대부분이 규칙·이유 본체이기 때문 — 이후는 예산 규칙이 자동 감시 |
| 2026-07-17 | 세션 중 토큰·시간 소모 처방 2건 — ① harness-review 일일 모드 범위·비용 상한(agentsview 질의·git log·경량 파일 확인만, 본문 재독 금지, 서브에이전트 3만 토큰 목표 — 무신호 보고에 112k 실측, 미설치 폴백 정의) ② metaskill 개선 모드 검증 비례성(규범 변경=reviewer 필수 / 비규범 diff=자체 검증+이력, 애매하면 검증 쪽, 알리바이 금지 — 품질 방침 메모리 동조 갱신) | .agents/skills/(harness-review·metaskill) | 사용자 승인("세션 중 방해·시간 소모 점검" → ①②) — 검증·점검 비용이 산출·위험과 무관하게 고정돼 3축(속도·무실수·토큰) 위배. 압박 테스트 6/6(트리거 키워드=규범 분류·압축 diff=검증행·피로 알리바이 기각), reviewer BLOCK 2건(신호표 정합·동커밋 이력) 반영 |
| 2026-07-18 | 3절 시크릿 금지에 실행 계층 게이트 신설 — secret-gate 훅(git commit-msg, 형식 확정 자격증명 패턴 차단, 루트·dev/ 예외 없음, 오탐은 사용자 확인 후 `[secret-ok]`), 기존 shim 체인에 연결(신규 머신 설정 불필요). 스펙 C1~C12·회귀 테스트 15항목, tdd-gate 회귀 29항목 유지 | AGENTS.md 3절, .agents/githooks/(secret-gate.py·secret-gate_test.py·commit-msg), docs/(specs·adr 023·proposals·README.md) | loop-engineering 검토(사용자 요청)에서 채택 1건 — 3절이 문서 규칙뿐이라 강제력 부재, 8절 훅 승격 원칙의 2번째 적용(제안서: 2026-07-18-loop-engineering-adoption, 나머지 기각 근거 포함). 사용자 승인 2026-07-18 |
| 2026-07-18 | tool-audit 스킬 신설 — 외부 도구(플러그인·MCP·스킬 팩) 사용 실측 감사 절차 고정: 3계열 검색어(명령·도구 호출명·산출물), 매칭 수/모수/최근 사용일 집계, 판정 기준표(0건+상시 비용→제거 등), 백업·복구 경로 필수. 판정은 제안까지 — 실행은 승인 후 metaskill | .agents/skills/tool-audit/SKILL.md, CLAUDE.md 라우팅 앵커 | 신호 ①(일일 점검 2026-07-18) — 같은 유형 감사 요청 누적 3회+(SuperClaude 제거·gstack·MCP·플러그인 감사), 세션마다 검색어·판정 기준 재발명. 사용자 승인 2026-07-18 |
| 2026-07-18 | ① harness-review 주간 무결성에 `_workspace/` 하우스키핑 신설(14일 경과 작업 디렉토리 삭제 후보 목록화, 운영 데이터 예외 3종 제외, 삭제는 3절 사용자 확인 후) ② orchestrate에 병렬 쓰기 격리 규칙 신설(같은 저장소 수정 에이전트 2기+ 동시 발행 시 `isolation: worktree`, 미지원 시 파일 범위 분할·직렬화) | .agents/skills/(harness-review·orchestrate) | ① 4절 미보존 원칙에 집행 장치 부재 — 세션 산출물 무기한 누적 관찰 ② loop-engineering 'Parallel Collision' 실패 모드 이식(제안서 2026-07-18 후속) — 병렬 쓰기 충돌 규칙 부재(worktree 언급 0건). 사용자 승인 2026-07-18 |
| 2026-07-18 | metaskill 스캐폴딩에 GitHub 레포 연동 신설 — 인터뷰 표에 레포 정책 항목(owner·공개범위), 2절에 스택 `.gitignore`+최초 커밋, 2.5절 신설(`gh repo create <owner>/<이름> --private\|--public --source=. --remote=origin --push`, owner·공개범위 매번 확인, 미인증 시 로컬만 두고 중단, 생성 후 remote·visibility 검증, work-tracker Issues 연동 안내) | .agents/skills/metaskill/references/scaffold.md | 사용자 요청 — 신규 프로젝트 생성 시 GitHub 레포 생성까지 자동화. 외부 발행이라 owner·공개범위 실행 전 확인 게이트 유지(3절), 개인 프로필 gh 인증(nohdol97). 문서 절차 확장+명령 문법·게이트 정합 자체 검증(비례성 — 61행) |
| 2026-07-18 | context7 활용 스킬 신설(라이브러리·프레임워크 버전별 최신 문서 조회 — context7 MCP 래퍼: resolve-library-id→query-docs, 미설치·실패 시 WebFetch/WebSearch 폴백, claude-api(Claude Code 내장)·defuddle 부정 트리거로 경계 확정) + harness-install 3c 신설(context7 MCP user 스코프 등록, SuperClaude 잔재 magic·sequential-thinking 발견 시 제거 안내) + 완료 체크리스트 1행 | .agents/skills/context7/, .agents/skills/harness-install/, CLAUDE.md 라우팅 앵커 | 사용자 결정 — 글로벌 MCP 감사에서 sequential-thinking(네이티브 확장 사고가 완전 대체)·magic(실사용 미확인) 제거, context7·playwright 유지. MCP의 약점은 능력이 아니라 발동 타이밍 → 활성화 스킬로 처방(claude-api 패턴). magic·sequential-thinking MCP 실제 제거(`claude mcp remove` user 스코프)+`.superclaude-metadata.json` 잔재 삭제 동반(하네스 외부 config, 백업 scratchpad) |
| 2026-07-18 | 13절 0항 clarify-first 신설(설계·아키텍처급 한정) — 착수 전 암묵 가정·빠진 제약·판단기준 열거 → 답에 따라 접근이 갈리는 것만 되묻기(AskUserQuestion) → 나머지 가정 선언 후 진행, 질문 남발 금지 가드(조회 가능·기본값 자명·답이 접근 불변인 것은 비질문). architect 5절(실행 주체 명문화)·orchestrate Phase 0-2 5항(architect 미경유 직접·생성-검증 entry 경로)·CLAUDE.md 항상-온 앵커 반영 | AGENTS.md 13절, .agents/agents/architect.md, .agents/skills/orchestrate/SKILL.md, CLAUDE.md | 사용자 요청("컨텍스트·아키텍처에서 프롬프트가 놓치는 것 방지 — 질문으로 프롬프트 개선 후 작업이 내재화됐나") — architect는 이미 내장(baseline 실측 확인)이나 직접·생성-검증 entry 경로엔 주체 부재. 압박 테스트: treatment가 갈림질문 우선·접근 보류로 flip(baseline은 접근 선점+질문 append). 범위 설계·아키텍처급 한정(사용자 확정) |
| 2026-07-18 | README.md 스테일 동기화 — 스킬 목록에 context7·tool-audit 추가(11→13종), githooks 서술에 secret-gate.py 추가(ADR 023), 설치 안내에 context7 MCP 등록 단계 반영 | README.md | 2026-07-18 신설 자산(context7·tool-audit 스킬, secret-gate 훅)이 README 최상위 인덱스에 미반영 — 탐색 인덱스가 현실과 어긋남(6절 동기화 의무). 문서 정합 수정(비규범 diff — 자체 검증·이력 갈음) |
| 2026-07-19 | oh-my-openagent 착안 5건 이식 — 완료 증거 4필드(§13·reviewer), untrusted 봉투(§3·defuddle·autoloop driver.py), 긍정형 제약+7요소 발행 골격(§10·orchestrate), 팀 런 교훈 노트(orchestrate), integrity-check 무결성 점검 훅 신설(harness-review) | §3·§10·§13, reviewer, defuddle·orchestrate·harness-review, .agents/hooks/integrity-check.py, autoloop driver.py | 검증·발행의 형식·기계화 공백 — 중복이라도 더 나은 형식은 채택(①②⑤), 순수 공백(③④). reviewer 2차 검증(설계·구현) 통과. docs/adr/026, 제안 2026-07-19-oh-my-openagent-adoption |
| 2026-07-19 | Codex 네이티브 custom-agent 병행 신설 — 표준 팀원 7종에 `.codex/agents/*.toml` 얇은 어댑터 추가(대응 `.agents/agents/*.md` 전문 선로드, 역할 원본 유지, 모델·sandbox 비고정), `.gitignore` 추적 예외, integrity-check R12·R7로 1:1·메타데이터·계약 참조·추가 설정·미추적 드리프트 검사(신규 회귀 6건), metaskill 생성 규칙·README·스펙·ADR 027 동기화 | AGENTS.md 5·10·11절, `.codex/agents/`, `.gitignore`, `.agents/hooks/integrity-check*`, `.agents/skills/metaskill/`, README.md, docs/(specs·adr·README) | 사용자 요청("여기에 있는 agent를 Codex에서 사용 가능하게 추가") — Codex skill은 `.agents/skills`를 네이티브 발견하지만 custom agent는 `.codex/agents/*.toml` 전용이라 기존 Markdown 정의가 이름 기반 type으로 노출되지 않던 형식 공백 해소. 역할 본문 복제는 단일 원본을 깨므로 얇은 선로드 어댑터로 결정(ADR 027). 회귀 24/24·무결성 36 pass·reviewer PASS; 실제 spawn은 로컬 인증 만료로 재로그인 후 C5 확인 대기 |
| 2026-07-19 | §4 정리 제외 목록에 `_workspace/carryover/` 추가 | 4절 | `carryover` 스킬 신설 — 세션 이월 노트가 정리로 지워지지 않게 예외 등록(harness-updates.md 등과 동일 취급) |
| 2026-07-19 | `dev/` 실험 공간 디렉토리 제거 — 실작업은 전부 `project/`. tdd-gate `dev/` 예외 분기·회귀 테스트(C6b)·스펙 R2에서 삭제, .gitignore·README·harness-install·REGISTRY·secret-gate 주석 정정, ADR 024 신설 + 002·008 부분 개정 배너 | 1·3·5·13절, .gitignore, README, tdd-gate.py·_test.py·secret-gate.py, docs/specs 2건, docs/adr 002·008·024, docs/README(MOC) | 사용자 확정(2026-07-19) — `dev/` 미사용(빈 디렉토리), 두 미추적 최상위 공간이 경로 규약·훅 예외 유지 비용만 발생. ADR 024 |
| 2026-07-19 | `wrapup` 스킬 신설 — clear 전 세션 마무리 관문(작업 훑기 → work-tracker/carryover 갈래질 위임 → /clear 안내). 슬래시 전용·자동 라우팅 미등록(carryover 전례) | .agents/skills/wrapup/, README.md 스킬 트리 | `/clear`는 CLI 내장 명령이라 실행 순간 컨텍스트가 비워져 모델이 끼어들 수 없음 — clear 직전 마무리 챙김을 관문 스킬로 제공. 저장 로직은 기존 두 스킬 재사용(중복 구현 금지, ADR 007·16절) |
| 2026-07-19 | `wrapup`에 세션 회고 단계 추가 — 개선 신호(확장 신호 ①~③·재발 방지 교훈·프로젝트 스킬 후보) 포착·기록·제안. 신호 있을 때만 가볍게, 반영은 승인 후 metaskill(자동 반영 금지) | .agents/skills/wrapup/SKILL.md, README.md 스킬 트리 | 세션 끝은 개선 신호가 가장 생생한 시점인데 포착 체크포인트가 없어 harness-review가 사후 재구성해야 했음 — wrapup 회고가 포착 계층으로 harness-review(주기 스캔·제안) 입력을 정확하게(상보). 반영을 wrapup이 직접 하면 3절 가드레일·metaskill 무단생성 금지와 충돌하므로 포착·제안까지로 한정 |
| 2026-07-19 | harness-install 3d 신설 — 컨텍스트 상시 표시 설정(Claude Code statusLine + Codex 기본-온 안내). 단일 원본 스크립트 `references/claude-statusline.py`(python3 stdlib, jq 미의존·§16 부합, 어떤 입력에도 `[claude]` 폴백) + 완료 체크리스트 1행. 설정은 전역 `~/.claude/`(머신별, hooksPath·MCP와 같은 계층), 스크립트만 추적 | .agents/skills/harness-install/SKILL.md·references/claude-statusline.py | 사용자 요청 — 세션 컨텍스트 누적을 상시 파악. Claude Code는 네이티브 토글 없어 statusLine 스크립트 필요, Codex는 TUI 푸터 기본-온. reviewer 독립 검증 통과(F1 크래시 방어 반영) |
| 2026-07-19 | carryover 디렉토리 2분할 — `backlog/`(대기)·`progress/`(이어가는 중). 저장은 backlog/에, 재개 로드 시 backlog→progress 이동(집은 노트가 "대기" 목록을 안 흐리게), 완료는 삭제. done/ 아카이브는 두지 않음(gitignore 스크래치 cruft 방지, 영속 기록은 work-tracker). 스캔에 상태 라벨·구버전 평면 노트 1회 마이그레이션 추가. §4 제외는 하위 디렉토리 포함으로 명시 | .agents/skills/carryover/SKILL.md | 사용자 요청("가져온 것 지우나 → 로드 시 이동 절충") — 완료 노트가 재개 목록을 흐리는 문제를, 로드-즉시-삭제(미완 유실 위험) 대신 상태 이동으로 해결. done 삭제는 사용자 확정(3분할 중 done은 cruft라 기각) |
| 2026-07-19 | wrapup 3·4단계 개정 — 저장 대상을 "매번 AskUserQuestion"에서 **범위 기반 자동 판단**으로 전환. 남길것없음·carryover는 무확인 자동(로컬·저비용), work-tracker는 자동 판단하되 **이슈 생성 직전 1회 확인**(외부 발행·이슈 무덤 방지 §14). 범위 애매 시에만 되물음. description·본문·with/without·README 스킬 트리 동기화 | .agents/skills/wrapup/SKILL.md, README.md | 사용자 요청("범위 보고 알아서 결정") — 자명한 경우의 반복 질문 제거. 외부 발행(GitHub 이슈)만 확인 유지는 base 가드레일(외부 발행 확인)과 정합. work-tracker 자동화 범위는 사용자 확정(이슈 직전 확인) |
| 2026-07-19 | harness-install 3d를 "Claude Code 전역 권장 설정" 우산으로 확장 — ② transcript 보존 연장(`cleanupPeriodDays`, 기본 30일→제안 90~365) 추가, 체크리스트 항목 2항 커버로 갱신. Codex 실태 병기(세션·히스토리 기본 무기한 보존이라 연장 설정 없음, 오히려 `sessions`·`codex-tui.log` 비대 주의) | .agents/skills/harness-install/SKILL.md | 사용자 요청("harness install 할 때도, codex도") — 보존 연장은 Claude Code에만 의미(Codex는 기본 전부 보존, 실측 sessions 437MB·tui log 339MB). 전역 설정 계층 정합 유지 |
| 2026-07-19 | autoloop 프롬프트 고정 지시문 6항 추가 — 러너가 허용 목록에 없으면 테스트 자가 실행에 턴을 쓰지 말고 드라이버 실측·검증 반복에 위임하라(done 주장은 실측으로만 확정됨을 고지). 스펙 R2 병기 | .agents/skills/autoloop/scripts/driver.py, docs/specs/2026-07-19-autoloop-driver.md | 시운전 관찰 — 세션이 python3 거부 후 자가 검증 불가로 done 보류, 반복 1회(≈$1.2) 추가 소비. 게이트·판정 로직 불변(비규범 문구, 테스트 25항목 통과)으로 자체 검증 갈음, 사용자 승인 |
| 2026-07-19 | autoloop 핸드오프 신뢰성 튜닝 — ① 프롬프트에 carryover 노트 **파일 경로 명시**(세션이 추측·검색 않고 갱신), ② 드라이버가 파싱한 직전 상태를 다음 프롬프트에 **보장 주입**(노트 갱신 재량에 비의존한 플로어). 스펙 R2·C4 갱신, 회귀 테스트 2건 추가(27항목) | .agents/skills/autoloop/scripts/driver.py·driver_test.py, docs/specs/2026-07-19-autoloop-driver.md | 다단계 시운전 관찰 — 1반복 즉시 done 시 노트가 비어 핸드오프 유실(경로 미고지가 원인). 라이브 재확인: 튜닝 후 노트 정상 충전. 안전 게이트·정지·검증 로직 불변(비안전 신뢰성 개선, 신규 2테스트+기존 전부 green·라이브 확인)으로 자체 검증 |
| 2026-07-19 | §4 정리 제외 목록에 `_workspace/autoloop/<작업명>/` 추가(carryover와 동렬 — R10 재개가 세션 넘김 생존을 요구), README `_workspace/` 트리 주석을 실제 구조(autoloop·carryover·ops 로그)로 갱신 | AGENTS.md §4, README.md | 사용자 지적 — autoloop 신설 후 문서가 새 산출물 위치를 반영 못함. 이미 존재하는 R10 재개 동작의 상태 위치를 명문화(비안전 문서 정합)로 자체 검증 |
| 2026-07-19 | autoloop `status` 동사에 튜닝 신호 포착·프로필 라우팅 추가 — 리뷰 시점에 driver.log의 비정상 exit·권한거부·무진전·BLOCK 반복을 §8 진화 신호로 취급, 개인 프로필=제안→metaskill / 사내 프로필=harness-updates.md 대기 큐(ops-log와 혼동 금지 명시). 코드 변경 없음(driver.log에 사실 이미 존재) | .agents/skills/autoloop/SKILL.md | 사용자 지적 — 헤드리스 루프는 자기 튜닝 필요를 진단 못해 개선점이 자동으로 안 남음. 리뷰 시점 대화형 세션을 기존 §8+§5 프로필 게이트에 연결(비코드 문서 wiring)로 자체 검증 |
| 2026-07-19 | autoloop 비용 경계 명시 — 한 컨텍스트에 들어가는 작업은 `/loop`·단일 세션이 더 쌈(`-p` 재확립 비용은 윈도우 초과 시에만 회수), SKILL.md 주의+start 안내·스펙 비목표에 기재. 모델 티어 분리는 미도입으로 확정(§9 티어가 이미 역할별 모델을 정함 — 임의 플래그로 중복 안 함, R6 주에 티어 정렬 형태만 남김) | .agents/skills/autoloop/SKILL.md, docs/specs/2026-07-19-autoloop-driver.md | 사용자 비용 질문 — 큰 작업은 압축 컨텍스트가 누적 세션보다 오히려 싸나 소규모는 재확립 비용이 낭비. #2(모델 분리)는 사용자 지적대로 티어 시스템이 이미 담당 |
| 2026-07-19 | autoloop 멀티 엔진(Codex) 확장 — 드라이버가 역할별로 `claude -p`/`codex exec`를 씀(`--engine`·`--implement-engine`·`--verify-engine`, "구현=claude 검증=codex" 교차 충족). Codex 안전=sandbox 레벨(구현 workspace-write·검증 read-only, bypass 금지), 출력은 `-o` 파일 파싱(비용 미제공). resolve_engine·build_codex_args·_run_session 분기, 스펙 R13~R15·C16·C17, ADR 025 결정 추가, 회귀 테스트 7건 추가(멀티엔진, 스위트 총 37항목 green — 병행 untrusted 봉투 테스트 포함) | .agents/skills/autoloop/scripts/driver.py·driver_test.py·SKILL.md, docs/specs/2026-07-19-autoloop-driver.md, docs/adr/025 | 사용자 요청("codex에서 codex로 진행, 명시 시 구현=claude·검증=codex") — 앞 행의 'Claude 전용' 한계를 해소. 안전 게이트 신규 엔진이라 reviewer 독립 검증(규범 변경) → PASS(F1 하네스로드 차이·F2/F3 카운트 반영) |
| 2026-07-19 | autoloop 엔진 = Claude Code CLI 전용 한계 명시 — SKILL.md·드라이버는 공용이라 Codex 기동은 되나 드라이버가 `claude -p`+Claude 권한 플래그를 써 루프는 Claude 구동(안전 게이트도 Claude 모델). Codex 네이티브(`codex exec`+샌드박스)는 별도 설계·안전 재검증 필요로 미지원, §11/ADR 019 패리티 명시적 예외(확장 시 `--engine` 분기). SKILL.md 주의·스펙 비목표 기재 | .agents/skills/autoloop/SKILL.md, docs/specs/2026-07-19-autoloop-driver.md | 사용자 질문("Codex에서도 되나") — 라우팅은 되나 실행 엔진이 Claude 전용임을 정직히 표기(그간 SKILL.md에 누락) |
| 2026-07-19 | autoloop 기능별 모델 티어 **선언·배선** — 구현 반복=implement 티어, 검증 세션=design 티어(§9). 드라이버에 `resolve_model`(역할→티어→모델)·`--implement-model`/`--verify-model` 추가, 코드엔 모델명 없이 값은 기동 세션이 라인업에서 해석해 전달(§9 탈모델명). SKILL.md start에 티어 해석 절차·경량 금지, 스펙 R1·R6·비목표·C15 갱신, 회귀 테스트 2건 추가(29항목) | .agents/skills/autoloop/scripts/driver.py·driver_test.py·SKILL.md, docs/specs/2026-07-19-autoloop-driver.md | 사용자 요청("autoloop에도 기능별 티어 명시") — 앞 행의 '미도입'을 뒤집어 티어를 명시적으로 선언·라우팅. 역할 분리 실익(검증만 최고 성능, 구현은 표준)을 §9 탈모델명 지키며 확보. 안전 게이트·정지·검증 판정 로직 불변(비안전 배선, 신규 2테스트+기존 green)으로 자체 검증 |
| 2026-07-20 | wrapup description 1041→770자 하드캡 수정 + integrity-check R13(SKILL.md description 1024자 초과 FAIL, 경계값 1024 통과) 신설 — c95ac51 커밋 시 누락된 changelog 보완 | .agents/hooks/integrity-check.py·integrity-check_test.py, .agents/skills/wrapup/SKILL.md, docs/specs/2026-07-19-integrity-check-script.md | Codex 로더가 description 1024자 초과 스킬을 거부해 wrapup이 로드되지 않던 실장애 수정. 전수 측정: autoloop 1021·carryover 938·harness-review 870은 하드캡 이내, wrapup만 초과였음. 기계로 막을 수 있는 규격 위반이라 8절 승격 원칙 적용. 테스트 26 green, integrity 36 pass |
| 2026-07-21 | 사내 대기 큐 2건 적용 — ① statusline 참조 스크립트 cp949 수정(stdout UTF-8 재구성 — cp949 콘솔에서 유니코드 막대·⚠가 UnicodeEncodeError로 [claude] 폴백되던 버그, 검증: cp949 시뮬레이션 정상 렌더) ② 서브에이전트 발행 규율 — 15절 '영어로 쓴다'에 4번째 채널(오케스트레이터 반환 최종 텍스트, integrator류 사용자 대면 예외 명시), agent-rules.md ③에 반환 텍스트 언어 명시 규칙, 에이전트 6종(architect·explorer·troubleshooter·implementer·reviewer·infra-specialist) §3 리트로핏(integrator는 한국어 예외 유지) | 15절, .agents/skills/harness-install/references/claude-statusline.py, .agents/skills/metaskill/references/agent-rules.md, .agents/agents/ 6종 | 사내 설치처 대기 큐(2026-07-20 statusline 버그, 2026-07-21 mx 작업 실측 — reviewer·infra-specialist가 오케스트레이터에게 한국어로 응답한 15절 위반 2건). 불릿 목록이 불완전해 에이전트 정의가 목록만 베껴 반환 채널 언어를 빠뜨린 구조적 원인 제거 |
| 2026-07-21 | 경량 리뷰 프로토콜 신설 — team-review 단독 모드를 소규모 diff 전용 경량 절차로 개정(발행자 입력 인라인 의무: diff 전문·스펙 기준 발췌·테스트 출력 / 관점 선별: 해당 축만·비해당 한 줄 스킵 / ≤2파일·저위험은 리포트 파일 생략·반환 텍스트 판정, must-fix 시에만 파일화 / run_in_background 발행), orchestrate 검증 필수 규칙에 경량 발행 규칙 연동(검증 필수 자체는 불변), reviewer §3에 인라인 입력 수용(관련 테스트 재실행은 유지 — 독립 증거)·경량 출력 예외 | .agents/skills/(team-review·orchestrate), .agents/agents/reviewer.md | 사용자 지적 — 작은 작업에도 리뷰가 수 분 소요(2026-07-16 감사 실측: 리뷰어당 4~10분·5万~13만 토큰). 원인은 diff 크기와 무관한 고정 오버헤드(재수집 왕복·7축 전체 순회·리포트 파일화). §9 티어·§13 검증 필수는 불변 — 검증을 깎지 않고 고정비만 깎는다 |
| 2026-07-21 | wrapup 5단계에 다음 세션 시작 프롬프트 신설 — /clear 안내 뒤 복붙용 재개 프롬프트를 저장 대상별로 생성(carryover → `/carryover 재개 — <슬러그>. 첫 행동: …`, work-tracker → `이어서 하자 — #N. 첫 행동: …`, 남길 것 없음이면 생략), 첫 행동은 저장된 노트·이슈에서 그대로 인용(지어내기 금지), carryover 저장 모드 보고에도 동일 형식 연동 | .agents/skills/wrapup/SKILL.md, .agents/skills/carryover/SKILL.md | 사용자 요청(2026-07-21) — 재개 경로를 사용자 기억에 의존시키던 마지막 틈 제거. 트리거 문구를 각 스킬 재실행 키워드와 일치시켜 새 세션 라우팅(§7)이 자동으로 걸리게 |
| 2026-07-21 | work-tracker 흐름 2(진행 기록)에도 다음 세션 시작 프롬프트 연동 — 기록 후 `이어서 하자 — #N <제목>. 첫 행동: <진행 로그 "다음" 항목>` 복붙 블록 제시(wrapup·carryover와 동일 형식, 첫 행동은 로그 인용 — 지어내기 금지) | .agents/skills/work-tracker/SKILL.md | 일관성 — wrapup 경유 시에만 프롬프트가 나오고 단독 "진행 상황 기록해줘"(흐름 2)는 코멘트만 남기고 끝나는 틈(사용자 확인 질문). 흐름 1(등록)은 세션이 계속되는 중이라 제외 |
| 2026-07-21 | 조회·검증 에이전트의 git 트리 불변 규율 신설(사내 큐 + 확장 검토) — agent-rules ⑨에 단일 원본(비교 목적 stash·checkout/restore·reset·clean 금지, 비교는 diff <ref>..<ref>·show <commit>:<path>, HEAD 이동 진단은 git worktree add 격리), reviewer §2·§9, troubleshooter §5·§9(**bisect는 공유 트리 금지·격리 worktree 필수** — 확장 검토에서 발견한 동류 갭: §9가 bisect를 허용하는데 bisect는 HEAD를 옮김), explorer §9, orchestrate 공통 팀원 규칙 ⑦(즉석 정의 팀원 커버, B모드 적용 목록에 포함) | .agents/skills/metaskill/references/agent-rules.md, .agents/agents/(reviewer·troubleshooter·explorer), .agents/skills/orchestrate/SKILL.md | 사내 대기 큐(2026-07-21 실측 2회 — 스택 브랜치 작업 중 리뷰어가 비교용 stash/pop 반복, 공유 트리에 병행 uncommitted 공존) + "더 넓게 검토" 지시 — stash는 한 사례일 뿐, 위험 부류는 공유 트리 상태 변경 전반 |
| 2026-07-21 | 구현 주체 규칙 명문화(A안) + 재개 경로 게이트 보강 — orchestrate 0-1 생성-검증 쌍 행 개정(3+파일·중위험 이상은 implementer 의무·메인 루프 직접 구현 금지, ≤2파일·저위험만 직접 구현+독립 reviewer 공식 인정 — 경량 리뷰 프로토콜과 동일 경계), 구현 주체 규칙 불릿 신설("내가 직접이 싸다" 정당화=우회 신호, 3+파일 성장 시 implementer 전환), continuation 불릿에 work-tracker·carryover 재개 포함 명시, work-tracker 흐름 3에 게이트 재진입 신설(기존 부재 — 실제 갭), carryover 재개 4단계에 구현 주체 판정 명시 | .agents/skills/(orchestrate·work-tracker·carryover) | 사용자 관찰 확인(2026-07-21) — 하위 프로젝트·재개(work-tracker/carryover) 세션에서 implementer 미발행·메인 루프 직접 구현 패턴. 판정표(쌍=implementer+reviewer)와 실무의 불일치를 경계 명문화로 해소 — 13절 보증의 본질(자기 검증 금지)은 불변 |
| 2026-07-21 | 경량 리뷰 프로토콜 ↔ 구현 주체 규칙 경계 정합 — ① 미정의 "중위험/위험도 중간" 등급 제거(위험도는 2단 낮음/높음 — 단일 원본 0-1 팀 행·team-review Phase 0, 높음은 파일 수 무관 팀 판정이므로 쌍 내부 경계는 파일 수 ≤2/3+뿐) ② orchestrate 경량 발행 규칙 2층 분리(입력 인라인·background는 쌍 검증 전반 기본, 리포트 생략만 ≤2파일) ③ team-review 상위 경계 문구 정정(3~5파일 저위험=전체 리포트, 6+/고위험=팀) ④ reviewer §3에 인라인 diff↔실제 저장소 diff 범위 대조 의무(발행자=작성자인 직접 구현 경로에서 누락 변경이 검증 밖으로 새는 틈) | .agents/skills/(orchestrate·team-review), .agents/agents/reviewer.md | 사용자 요청(경계 겹침 정합 검토) — 같은 날 들어간 두 규칙이 미정의 등급·미정의 "소규모"로 어긋남. 경계 단일화: 쌍 내부=파일 수, 위험도=2단 |
| 2026-07-19 | (백필) wrapup 규칙 인용 정정 — team-review 감사 F2·F3 반영 | .agents/skills/wrapup/SKILL.md | 커밋 36cfa61이 §5 동기 이력 규칙을 누락(2026-07-21 정합 감사 DOC-F2) — 소급 기록 |
| 2026-07-21 | (백필) 재개 스킬 2곳 잔여 "중위험" 문구 정정 | .agents/skills/(carryover·work-tracker) | 커밋 32e58b5가 §5 동기 이력 규칙을 누락(2026-07-21 정합 감사 DOC-F1) — 소급 기록 |
| 2026-07-21 | 사내 큐 2건 — ① AGENTS.md 40KB 예산 다이어트(42,592→39,823B: 사례·타문서 중복 상세를 ADR·스펙 포인터로 이관, 규칙·이유 유지 — §3·5·7·8·11·13·16) ② integrity-check R1 Windows 심링크 오탐 수정(readlink 백슬래시 정규화 + 회귀 테스트) | AGENTS.md, .agents/hooks/integrity-check.py(+테스트) | 주간 점검 실측(신호 ④ 로딩 비대·R1 오판정) — 항상-온 임포트 파일의 성장은 고정 토큰 비용 |
| 2026-07-21 | 정합 감사(리뷰어 2종) 반영 — 규칙: B모드 언어 규칙 ⑥ 적용 복원+반환 채널 추가(F3), agent-rules ⑨ 보칙 라벨(F1)·architect 예외 예시 정정(F5), 공통 규칙 ⑦ clean 추가(F2), integrator 반환 한국어 명시(F4), team-review에 diff 범위 대조 편입(F6), 직접 수행 행 인프라 매니페스트 제외(F7). 문서: autoloop 스펙 체크박스 18개 실측 체크(F3), 07-19 행 17개 표 재배치(F4), 567a37e 행 보강(F5), 누락 행 백필 2건(F1·F2) | .agents/skills/(orchestrate·team-review·metaskill/references), .agents/agents/integrator.md, docs/specs/2026-07-19-autoloop-driver.md, docs/harness-changelog.md | 사용자 요청(전체 정합성 재점검) — 리뷰어 2종 판정 PASS, Med 4·Low 8 전건 반영. 원본: _workspace/harness-consistency-2026-07-21/ |
| 2026-07-22 | gate-reminder 훅 신설(스펙+회귀 테스트 12항목, settings.json PostToolUse/PreToolUse 등록) — 조사성 Bash 관찰 세션의 제품 코드 첫 Edit/Write를 세션당 1회 차단하고 게이트 판정 선언 지시. 7절 3항·orchestrate continuation 항에 "진단→구현 전환점도 재판정 대상(첫 Edit/Write가 트리거)" 문구 추가(7절은 40KB 예산 내 압축 표기) | 7절, .agents/hooks/gate-reminder.py(+테스트), .claude/settings.json, .agents/skills/orchestrate/, docs/specs/, docs/adr/028, docs/README.md, README.md | 사내 대기 큐(신호 ② — 진단→수정 전환 우회가 한 세션 2회, 사용자 지적·자가 정정 후 재발). 8절 "기계적 차단 가능하면 훅 승격" 적용 — 문구는 상기 빈도만 높일 뿐 전환 시점을 못 잡음이 실측됨. Codex는 PreToolUse 상당 이벤트 부재로 미등록(문구 계층이 담당) |
| 2026-07-22 | branch-workflow 시작 절차 1항에 세션 resume·/model 전환 직후 `git branch --show-current` 재확인 추가 | .agents/skills/branch-workflow/ | 사내 대기 큐(실측) — 재개·모델 전환 직후 작업 디렉토리가 조용히 main으로 복귀해 있어 main 직커밋 직전에 발견. 저비용 예방 체크 |
| 2026-07-22 | gate-reminder 리뷰 반영(Low F1·F3) — R5 정리 테스트 추가(13항목), 4절 `_workspace/` 예외 목록에 `.gate-reminder/` 추가 + 예외 목록 압축(40KB 예산 39,959B, 여유 5→41B) | 4절, .agents/hooks/gate-reminder_test.py, docs/specs/ | reviewer 독립 검증 PASS(Critical~Med 0·Low 5) — F1 테스트 공백, F3 workspace 정리가 활성 세션 상태를 지울 수 있는 틈, F2 예산 여유 5B(트림 동봉). 원본: _workspace/gate-reminder-2026-07-22/ |
| 2026-07-22 | gate-reminder Codex 서술 정직화 — "PreToolUse 상당 이벤트 없음" 단정을 "미확인(검증된 이벤트는 SessionStart뿐 — ADR 019, 확인 시 파리티 검토)"으로 정정 | docs/adr/028, docs/specs/2026-07-22, .agents/hooks/gate-reminder.py(주석) | 사용자 질문("codex에도 적용되게 못해?")으로 재검토 — 07-16 조사가 검증한 것은 SessionStart뿐인데 부재를 사실로 기술했음. 도구 제약 서술 정직화 원칙(changelog 2026-07-12 F1~F11 전례) |
| 2026-07-22 | 세션 훅 Codex 파리티 기본값(ADR 029) — 11절 병행 규칙을 "신설 훅은 대칭 등록 기본값, 미검증 이벤트도 선제 등록(fail-open 전제)"으로 확장, gate-reminder를 .codex/hooks.json에 등록($PWD 경로, matcher 양 CLI 도구명 알터네이션) | 11절, .codex/hooks.json, docs/adr/(028·029), docs/specs/2026-07-22, .agents/hooks/gate-reminder.py(주석), docs/README.md | 사용자 지시(2026-07-22 — "앞으로 훅도 일단 Codex에서도") — 등록해 두고 확인하는 쪽이 방어 공백이 짧고, fail-open이라 미지원 시 무해. 동작 실검증은 Codex 설치 머신 몫. 2차 reviewer PASS(Low 2) |
| 2026-07-22 | 훅 전수 파리티 점검 — agentsview-daemon을 .codex/hooks.json에 편입(ADR 019 제외 결정 부분 대체 → 029 결정 5), 11절 "daemon 제외" 문구 삭제, ADR 019 부분 대체 배너·MOC 상태 갱신. git 훅(tdd-gate·secret-gate)은 git 계층이라 이미 도구 무관 확인 | 11절, .codex/hooks.json, docs/adr/(019·029), docs/README.md | 사용자 질문("나머지 훅들도 검토했어?") — 파리티 기본값 신설 후 기존 훅 잔여 공백 1건(데몬). 이중 기동 가드·미설치 fail-open 확인으로 선제 등록 안전 조건 충족, 기동은 관측 여부와 무관하게 무해·잠재 유익 |
| 2026-07-22 | 수술적 변경 이식(karpathy-guidelines 부분 채택) — 16절 금지 목록에 "요청과 무관한 인접 코드 개선" + 변경 줄 요청 소급성 원칙 추가(동반 트림: 리뷰 연결·왜 문서로만 불릿 압축), team-review 단순성 축에 out-of-scope 지적 형식 신설, 제안 문서·MOC 갱신 | 16절, .agents/skills/team-review/, docs/proposals/2026-07-22-karpathy-guidelines-adoption.md, docs/README.md | 사용자 승인(2026-07-22 검토 요청→부분 이식 승인) — 4원칙 중 3개는 기수렴(§13-0·§16·§13) 기각, 플러그인 설치 기각(always-on 중복 주입). ponytail 전례의 문서 이식(ADR 017 연장) |
| 2026-07-22 | rtk 조건부 채택 — harness-install 3e 신설(전역 설치·텔레메트리 명시 차단·훅 모드·Codex 모드 보류), 13절 2항에 출력 압축 프록시 증거 원문 규칙 추가(동반 트림: ADR 022 표기·13절 4항 중복 포인터 — 40KB 예산 39,953B, 여유 47B), 제안 문서·MOC | 13절, .agents/skills/harness-install/, docs/proposals/2026-07-22-rtk-adoption.md, docs/README.md | 사용자 검토·적용 요청 — 명령 출력 인제스천 계층은 기존 절감 축(§15·§4)에 없던 상보 축이고 안전 태세(fail-open·권한 존중·복합 명령 미재작성·tee 원문) 실측 확인. 절감 수치는 추정치라 3주 후 tool-audit 실측 판정 |
| 2026-07-22 | 케이브어(전보체) 내부 통신 전환 검토 — 기각 기록(제안 문서·MOC) | docs/proposals/2026-07-22-caveman-internal-comms.md, docs/README.md | 사용자 검토 요청 후 승인 — 손실 압축 실효 10~15% vs 발행 지시 모호화 재작업 비대칭. 기각도 기록해 재검토 반복 방지(제안=과정 원칙, §6) |
| 2026-07-22 | 리뷰어 비용 개선 2건 — ① integrity-check R14(AGENTS.md 40KB 예산)·R15(항상-온 문서 ADR 참조 실재) 신설(+회귀 테스트 5, 총 32 — 리뷰어 수동 확인 항목의 기계 이관) ② team-review 단독 모드 5항 신설(검증 범위=diff+포인터 대상 한정·작업 상한 명시·미검증은 4필드 ④ 정직 선언, 외부 대조 리뷰는 상한 상향) | .agents/hooks/integrity-check.py(+테스트), docs/specs/2026-07-19-integrity-check-script.md, .agents/skills/team-review/ | 사용자 승인 — 리뷰 5회 실측 회당 ~58k 토큰의 원인이 기계적 확인 반복+범위 무제한 탐색. 배칭(발견 지연)·리뷰 생략(가드 완화)은 트레이드오프 커서 기각, 이 2건은 검증 강도 유지 |
| 2026-07-22 | 경량 리뷰 입력 인라인에 10KB 임계 추가 — 초과 diff는 전문 인라인 대신 파일 경로 전달(단일 원본 team-review 단독 모드 1, orchestrate ① 층 동조) | .agents/skills/(team-review·orchestrate) | 사용자 승인(입력 토큰 절감) — 대형 diff 인라인은 발행 프롬프트+리뷰어 재독으로 이중 적재. 2026-07-22 실측: 53KB diff 포인터 방식 무결 작동(4차 리뷰) |
| 2026-07-22 | 5절 원격 PR 사이클 ②에 후속 푸시 전 머지 확인 추가(머지 시 ④→① 전환) + 원격 사이클·커밋 컨벤션 서술 압축(40KB 예산 내), orchestrate 공통 팀원 규칙 ⑧ "읽기는 좁혀서" 신설(B모드 적용 목록에 편입) | 5절, .agents/skills/orchestrate/ | 사용자 승인 — 머지 경합 3회 실발생(신호 ② — PR #32·34·36 각각 복구 PR 1개씩 추가 비용), 통파일 Read가 팀원 입력 토큰 최대 소비처(입력 절감 검토 2026-07-22) |
| 2026-07-23 | 모델-로드 하네스 자산 영어 단일 원본 전환(ADR 030) — AGENTS.md·CLAUDE.md·에이전트 정의 7종·스킬 16종(SKILL.md+references) 전량 영어화(§15 개정: 하네스 운영 자산=모델-read 재분류), 한글 뷰 3종 신설(AGENTS.ko.md 다이제스트·skills/agents README.ko.md 카탈로그, 편집금지 배너+source-hash), integrity-check 드리프트 가드 추가(뷰 스테일 기계 차단), metaskill 산출 언어 기본값 영어. 한국어 유지: 사용자 대면 출력 전부·트리거 키워드·ADR·스펙·changelog·루트 README·사용자 대면 템플릿 | 전 하네스 자산, AGENTS.ko.md, .agents/{skills,agents}/README.ko.md, .agents/hooks/integrity-check.py(+테스트), docs/adr/030, docs/README.md | 사용자 결정(2026-07-22) — 입력 토큰 절감 실측: AGENTS.md 한글 9,313자≈세션당 5–9k 토큰 초과 지출+R14 예산 여유 70B 포화, hot 스킬·에이전트 본문 재로드 누적. 전문 병렬 사본은 드리프트 부채라 기각, 생성 뷰+기계 가드로 이해도·단일 원본 양립. reviewer 독립 검증 동반 |
| 2026-07-23 | 한글 다이제스트 파일명 개명 AGENTS_KR.md → AGENTS.ko.md — 전 참조(§15·R16·테스트·ADR 030·README·changelog) 일괄 치환, 배너 source-hash 재생성 | AGENTS.ko.md, AGENTS.md 15절, .agents/hooks/integrity-check.py(+테스트), docs/adr/030, README.md | 사용자 요청 — README.ko.md와 .ko.md 접미 형태 통일 |
| 2026-07-23 | autoloop description 압축 1,023→729자 — 메커니즘 서술(3동사·게이트 상세) 제거, 트리거·한국어 키워드·부정 경계 15항목 바이트 유지(기계 검증) | .agents/skills/autoloop/SKILL.md | R13 하드캡(1024자) 여유 1자 → 295자 확보 — 07-20 wrapup 실파손 유형의 잠복 재현 해소(주간 점검 P1 축소판, 앵커링 우려로 절감 목적 전면 압축은 미실행) |
| 2026-07-23 | metaskill 스킬 공통 규칙 1(생성 직후 검증)에 description 바이트 길이 확인 추가 — `grep -m1 '^description:' \| wc -c`로 하드캡 ~1024B 이내 확인(한글 3바이트 주석 병기) | .agents/skills/metaskill/SKILL.md | 규칙 2에 길이 기준은 있으나 검증 절차에 없어 사후에 걸림 — wrapup description 1410B로 생성 후 2회 왕복 실사례(07-19 이월 노트 승인 반영) |
| 2026-07-23 | harness-install 3절 보조 도구를 전부-선택에서 기본-설치로 전환 — 미설치 항목은 항목별 재질문 없이 설치 진행(agentsview·defuddle·context7·전역 설정·rtk), 실패·오프라인·사용자 거절만 사유와 함께 스킵, 3d 전역 설정은 미설정 키만 기본 적용(기설정 값 보존, cleanupPeriodDays 기본 90), 7절 완료 기준에서 (Optional) 표기 제거 + rtk 항목 신설, defuddle 스킬의 "선택 도구 절차" 스테일 참조 동조 수정(reviewer L2) | .agents/skills/(harness-install·defuddle)/SKILL.md, .agents/skills/README.ko.md | 사용자 지시(2026-07-23 — "선택사항이 아니라 미설치되어 있으면 설치하게 해") — 선택 처리로 설치가 누락되면 폴백 경로로만 돌아 도구 절감 효과가 죽은 채 유지됨 |
| 2026-07-23 | tdd-gate_test.py 타임아웃 견고화 + C16 신설 — 공통 `run()` 헬퍼가 TimeoutExpired를 센티널(==·!= 모두 False)로 치환해 크래시 대신 해당 케이스만 FAIL 집계 후 완주, 타임아웃 `TDD_GATE_TEST_TIMEOUT` 환경변수화(기본 30초), 회귀 C16·C16b 추가(30/30) | .agents/githooks/tdd-gate_test.py, docs/specs/2026-07-13-tdd-gate-hook.md | 대기 큐 보고(2026-07-23, 사내 Windows) — AV/EDR 프로세스 생성 지연으로 C11b에서 TimeoutExpired 미포착 크래시, 이후 케이스 집계 전부 유실. 재실행 시 28/28 통과라 게이트 로직 결함 아닌 환경 지연으로 추정 — 방어적 가치(부분 결과 보존)로 ①·② 채택, ③(로직 대기 제거)은 원인 미확정이라 보류 |
| 2026-07-23 | 사내 프로필 커밋 금지 범위를 "루트 하네스 저장소 추적 파일 한정"으로 명시 — 5절 기본 커밋·푸시 문구의 무한정 예외를 범위 한정으로 교체, 프로필 항목·harness-install 인터뷰 설명에 "하위 프로젝트 저장소 무관(정상 커밋·푸시 흐름)" 명시, AGENTS.ko.md 다이제스트 동조 | 5절, .agents/skills/harness-install/SKILL.md, AGENTS.ko.md | 사용자 정정(2026-07-23, 신호 ②) — 사내 머신 세션이 프로필을 전면 커밋 금지로 오독해 하위 프로젝트 커밋까지 보류함. 금지의 근거(개인 원격 푸시 보안 위반)는 루트 하네스 저장소에만 성립 |
| 2026-07-23 | 13절 5항 다크 론치 게이트 신설 — 운영·공유 hot path의 동작변경·휴리스틱 기능(임계값 트리거·모델 라우팅·자동 승급)은 피처 플래그 기본 OFF 배포, 실환경 성능·발동빈도 측정 후에만 ON. 증거 4필드 ④(미검증)가 운영 동작에 영향 주면 그 자체가 활성화 게이트("배포 후 확인" 금지). release 스킬 Phase 1에 런북 대응 항목(플래그 ON=독립 단계) 추가, AGENTS.ko.md 동조 | 13절, .agents/skills/release/SKILL.md, AGENTS.ko.md | 대기 큐 보고(2026-07-23, 신호 ② — mx-ai-store#250) — 미측정 "보수적" 컨텍스트 길이 트리거(16000자)를 기본 ON으로 배포, 정상 턴마다 3050배 느린 heavy 모델로 오승급 → SSE idle 타임아웃 회귀. 유닛 테스트·리뷰 PASS는 로직 정확만 보장, 운영 발동 안전은 별개 |
| 2026-07-24 | harness-install 3f gh CLI 기본-설치 신설 — 설치(`brew`/apt·dnf/`winget`) + `gh auth status` 확인(인증은 사용자가 직접, 토큰 대리 추출 금지), 미설치 시 PR·이슈 절차 열화 명시, 7절 완료 기준 행 추가 | .agents/skills/harness-install/SKILL.md, .agents/skills/README.ko.md | 사내 세션 회고(2026-07-23) — branch-workflow·work-tracker·doc-writer가 gh를 전제하는데 설치 절차에 없어, gh 미설치 머신이 curl+~/.git-credentials 토큰 추출로 우회(자격증명 취급상 지양). 사용자 결정: gh 기본 설치로 통일 |
| 2026-07-24 | 서브에이전트 발행 백그라운드 기본을 결과 의존 작업까지 확장 — "다음 행동이 결과에 달렸으면 포그라운드" 예외를 "백그라운드 발행 후 턴 종료, 완료 알림에서 의존 단계 재개"로 교체(orchestrate 동시 발행 규칙·team-review 4항 동조), 포그라운드는 같은 응답에 결과를 인라인해야 하는 1분 이내 조회로 한정, 알림 재개는 대화형 세션 한정(headless `claude -p`는 동기 완료 — autoloop 고아 방지) | CLAUDE.md, .agents/skills/(orchestrate·team-review)/SKILL.md | 사용자 요청(2026-07-24) — 포그라운드 발행 중 세션이 막혀 대화 불가(이번 세션 reviewer 동기 실행 3회, 회당 2~3분). 백그라운드 reviewer 검증 BLOCK(F1 team-review 옛 예외 잔존·F2 headless 무보호·F3 이력 위치) 재작업 반영 |
| 2026-07-25 | 13절 0항 전면 개정(clarify-first → 인터뷰 우선) + PR 전 이해도 퀴즈 게이트 신설 — ① 인터뷰를 설계급 한정에서 전 작업(코드 여부 무관)으로 확대, 불확실 시 기본값을 "가정 선언"에서 "무조건 질문"으로 반전, 중도 블로커 사전 스캔(권한·환경·미정 값·완료 기준) 신설, **작업 시작 후 사용자 질문 전면 금지**(중도 분기는 자율 최선 선택 후 보고·퀴즈에서 회수; 생략 불가 카브아웃 = §3 확인 + 데이터 손실 방지 확인(미커밋 변경·rebase 충돌 방향)) ② branch-workflow 마무리 4단계에 객관식 퀴즈 게이트(2~4문, 목적·트레이드오프·영향·자율 결정 — 오답·이의 = 불일치 신호로 PR 보류·수정 루프), §5 원격 PR 사이클에도 적용(스킬의 하위 프로젝트 한정 범위에 명시 예외), orchestrate 게이트 5단계·architect·doc-writer 참조·한글 뷰 3종 동조 | 13절 0항, 5절, .agents/skills/(branch-workflow·orchestrate·doc-writer)/, .agents/agents/architect.md, AGENTS.ko.md, .agents/{skills,agents}/README.ko.md | 사용자 요청(2026-07-25) — 프롬프트 맹점·중도 막힘 제거(질문은 착수 전 1라운드 집중, 이후 무인 안전), 원치 않는 작업을 PR 전 최저비용 지점에서 검출. 형식·범위는 인터뷰로 확정(전 작업·객관식·즉시 적용). 백그라운드 reviewer BLOCK(F1 orchestrate 스테일·F2 데이터 손실 확인 카브아웃 공백 외 4건) 재작업 반영 |
| 2026-07-25 | 이해도 퀴즈 1차 실행 지점을 리뷰 창으로 이동 — 구현 완료 후 백그라운드 reviewer 발행 직후 같은 턴에 퀴즈 진행(리뷰와 병렬, orchestrate 검증 필수 문단(쌍·팀 공통)·team-review Phase 0(양 모드) — PR 대상 작업 한정), branch-workflow 마무리 4단계는 확인 게이트로 재편(리뷰 재작업으로 동작 변경 시 변경분만 델타 재퀴즈 1~2문, 리뷰 창 퀴즈 미실시 경로만 전체 퀴즈 — 퀴즈 스펙 단일 원본 유지), 한글 뷰 동조 | 13절 0항, .agents/skills/(branch-workflow·orchestrate·team-review)/, AGENTS.ko.md, .agents/skills/README.ko.md | 사용자 제안(2026-07-25 — "리뷰 중에 퀴즈 하는 건 어때") — 리뷰 대기 2~5분이 순수 유휴였는데 퀴즈를 병렬 배치하면 총 소요 단축 + 불일치를 rebase·push 전에 조기 검출. 리뷰 재작업으로 퀴즈가 낡는 문제는 델타 재퀴즈로 보완 |
| 2026-07-25 | agentmemory 검토 — 기각 기록(제안 문서·MOC) | docs/proposals/2026-07-25-agentmemory-review.md, docs/README.md | 사용자 검토 요청 후 기각 승인 — ADR 018(검색형 저장소 기각) 정면 충돌, 니치 기충족(agentsview·auto-memory·문서 4계층), 상시 비용 역행(세션당 2k 주입+매 호출 훅·LLM 압축), 외부 임베딩 §3 우려. 기각도 기록해 재검토 반복 방지(제안=과정, §6) |
| 2026-07-25 | Understand-Anything 채택 — harness-install 3g 신설(기본 설치·직접 호출 전용·사람 대면 한정) + CLAUDE.md 스킬맵 라우팅 노트, proposal·MOC. reviewer BLOCK 재작업: 3g에 §3 유출 완화(사내·독점 코드=로컬 모델 전제) 실행 문서 편입(F1)·`.ua/` 서브 gitignore 노트(N1), explorer·architect 정의에 '그래프는 진실 소스 아님' 경계 한 줄(F2). `understand-*`는 자동 라우팅 금지(사용자 `/understand…`만), 생성 그래프는 하네스 에이전트 진실 소스 금지(스테일=graphify 실패 모드, 라이브 코드 검증 힌트로만) | .agents/skills/harness-install/SKILL.md, CLAUDE.md, .agents/agents/(explorer·architect).md, docs/proposals/2026-07-25-understand-anything-adoption.md, docs/README.md, .agents/skills/README.ko.md | 사용자 결정(2026-07-25) — 설치는 harness-install 기본·트리거는 직접 호출 전용으로 두면 graphify/agentmemory 기각 근거(상시 비용·충돌)가 해소(opt-in-per-use). 3주 실측 게이트는 미설치 자연 방치 구조라 생략 |
| 2026-07-25 | Understand-Anything §3 임베딩 유출 완화를 프로필로 범위 한정 — harness-install 3g·proposal에서 "사내·독점 코드만 로컬 임베딩(all-MiniLM, CPU 전용 경량) 필수, 개인 프로필 본인·오픈 코드는 불필요"로 명확화 + 임베딩(경량)≠로컬 LLM(무거운 선택 태세) 혼동 정정 | .agents/skills/harness-install/SKILL.md, docs/proposals/2026-07-25-understand-anything-adoption.md | 사용자 지적(2026-07-25) — 개인 프로필은 §3 보호 대상이 아닌데 로컬 임베딩 강제는 불필요 마찰. 이전 문구가 임베딩과 로컬 LLM을 뭉뚱그려 고사양 오해 소지. 비규범 문구 정확화(자체 검증) |
| 2026-07-25 | claude-video(`/watch`) 채택 — harness-install 3h 신설(기본 설치·직접 호출 전용) + CLAUDE.md 스킬맵 라우팅, proposal·MOC. 순수 신규 역량(영상 이해, 중복 0)·opt-in-per-use. `/watch` 자동 라우팅 금지(사용자 명시 호출만), Whisper 폴백이 자막 없는 영상 오디오를 제3자(Groq/OpenAI) 전송 → 사내·민감 녹화는 `--no-whisper`/자막 전용(§3), 프레임→Claude는 기본 동작(추가 유출 아님) | .agents/skills/harness-install/SKILL.md, CLAUDE.md, docs/proposals/2026-07-25-claude-video-adoption.md, docs/README.md, .agents/skills/README.ko.md | 사용자 검토 요청 → 채택+직접 호출 전용 결정. Understand-Anything과 같은 직접 호출 전용 패턴, 유일 유출 지점(Whisper 오디오)은 사내 완화 전제로 차단 |
| 2026-07-25 | 3g·3h 사내 마켓플레이스 차단 대응 — `/plugin marketplace`가 사내에서 흔히 막히는 현실 반영. claude-video(3h)는 우회 경로 명시(`npx skills add -g`·수동 clone+심링크), Understand-Anything(3g)은 Claude Code 네이티브 플러그인 전용이라 마켓플레이스 차단 시 문서화된 우회 없음→사유와 함께 스킵(없어도 무해). 양쪽 강제 유지·차단은 자연 스킵(사용자 결정), 완료 기준·proposal 동조 | .agents/skills/harness-install/SKILL.md, docs/proposals/(understand-anything·claude-video)-adoption.md | 사용자 지적(2026-07-25) — 사내에서 plugin marketplace 설치 불가. 없는 우회 경로 지어내지 않고 정직 명시(3g는 스킵, 3h는 실경로 있음), 비규범 보강이라 자체 검증 |
| 2026-07-25 | 사용자 동반 성장 축 명문화 + 2장치 신설(가르치는 리포트·diff 읽기 가이드) — §13-0에 원칙(LLM이 다 하는 게 아니라 사용자 성장, 결정은 가르친다) + ① 드러낸 자율 결정·인터뷰 갈림길은 결정 공간(무엇 대 무엇·이 선택 이유)을 담고 ② 퀴즈 직전 diff 읽기 가이드(순서+왜 중요) 제시. doc-writer PR 템플릿 §5(변경 내용=읽기 가이드, 리뷰 포인트=결정 공간)·branch-workflow finish 4(퀴즈와 함께 읽기 가이드)·한글 뷰 동조. 둘 다 값싼 리포트 내용 추가(§16 자명한 결정 생략)이지 새 게이트 아님 | 13절 0항, .agents/skills/(doc-writer·branch-workflow)/, AGENTS.ko.md | 사용자 정의(2026-07-25 — "LLM이 다 만드는 게 우선이 아니라 사용자도 함께 성장, 그래서 퀴즈를 넣은 것") → 성장 장치 4종 제안 중 1·2 선택. 3(교습 모드)·4(학습 회고)는 미채택(무거움·부채 위험) |
| 2026-07-25 | "사용자 이해=완료 요건"을 §13 최상위 원칙으로 격상 + 이해도 퀴즈를 전달 게이트로 강화 — §13 인트로에 명명(정확성 증거 §13-2 + 사용자 이해 입증 = 완료의 두 반쪽, 이해 실패·생략은 실패 테스트처럼 PR 차단, §3·데이터 손실 카브아웃도 약화 못 함), branch-workflow finish 4를 "형식 아닌 하드 전달 게이트"로 명문화, before/with 표현 정합화(reviewer 정보성 노트). 한글 뷰 동조 | 13절, CLAUDE.md, .agents/skills/branch-workflow/, AGENTS.ko.md | 사용자 정의(2026-07-25 — "LLM이 작업한 걸 사용자가 확실히 이해하는 게 정말 중요한 원칙") → 최상위 원칙+항상-온 앵커+완료 요건으로 격상 선택. 흩어진 이해 장치(퀴즈)를 원칙으로 세워 형식적 격하·망각 방지 |
| 2026-07-25 | AGENTS.md 프로비넌스 제거 + 재축적 규율 신설 — 규칙 본문의 날짜 스탬프 12개·"누가 언제 확정"·실사례 서사(SSE 회귀 스토리 등)를 제거하거나 `(real case: changelog <날짜>)` 포인터로 압축, §6에 "이 항상-온 파일은 규칙+이유만·프로비넌스는 changelog"(ADR 021 연장) 규율 신설. 규칙·모든 Reason/why·파일·ADR·spec 네비게이션 포인터는 전량 유지. 38,694→38,526B(여유 1,306→1,474B) | AGENTS.md, AGENTS.ko.md(해시) | 사용자 검토 요청(2026-07-25 — "날짜·불필요한 내용 넣을 필요 있나, 더 가볍게") → 프로비넌스 제거+규율 선택. 실측: 파일은 날짜로 부푼 게 아니라 규칙으로 빽빽해 일회성 절감(168B)은 작고, 진짜 이득은 재축적 차단 규율 |
| 2026-07-25 | `tool-eval` 스킬 신설 — 외부 도구·플러그인 채택 평가 표준화(README §3-안전 수집 → 고정 평가 축(중복·상시/런타임 비용·§3 유출·기각 선례) → 채택/개인/기각 결정 → proposal·MOC·changelog → 채택 시 harness-install·라우팅·reviewer). 세션 교훈 인코딩: §3 완화는 실행 문서에·직접 호출 전용 기본·과신 아티팩트는 explorer/architect 정의에도 경계. tool-audit(설치 후 실측)와 부정 트리거 경계. CLAUDE.md 라우팅·README 트리·README.ko.md 동반 | .agents/skills/tool-eval/, CLAUDE.md, README.md, .agents/skills/README.ko.md | 신호 ①(외부 도구 검토·채택 3회: agentmemory·Understand-Anything·claude-video) — 매번 평가 축 재발명·교훈 유실 위험. 단독 스킬로 표준화(사용자 승인) |
| 2026-07-25 | Understand-Anything 철회(채택 당일) — harness-install 3g(UA) 블록·완료기준 행 제거 후 3h(claude-video)→3g 번호 정합, CLAUDE.md 라우팅 절·explorer/architect "코드 그래프 진실 소스 아님" 경계줄(UA 전용) 제거, proposal을 채택→철회로 전환(배너·재검토 조건), MOC 철회 표기, README.ko.md·claude-video proposal·tool-eval의 UA 참조 정리. 도구 자체 금지 아님(개인 사용 가능) | .agents/skills/(harness-install·tool-eval)/, CLAUDE.md, .agents/agents/(explorer·architect).md, docs/proposals/(understand-anything·claude-video), docs/README.md, .agents/skills/README.ko.md | 사용자 재검토(2026-07-25 — "understand는 이 하네스와 결이 안 맞는 것 같아") — 같은 세션에 세운 최상위 원칙(사용자 이해=완료 요건, 직접 읽기로 성장)과 정반대(코드를 대신 시각화해 우회)+graphify 범주. tool-eval "기각도 기록" 규율의 첫 적용 |
| 2026-07-25 | harness-review-reminder_test.py C6 fail-open 테스트 결함 수정 — `os.path.isfile` 패치(main이 호출하지 않는 no-op)를 `build_message` 패치로 교체해 예외를 실제 호출 경로에 주입, worklog-reminder_test.py C6과 동일 패턴으로 정합. 이전엔 예외 미주입으로 마커 신선 시 우연 통과·부재/노후 시 실패하는 비결정적 테스트였음(회귀 189개 중 유일 적색 원인) | .agents/hooks/harness-review-reminder_test.py | Codex 호환성 감사 M4(_workspace/codex-compat-audit-2026-07-25) — 188/189 적색의 원인. 수정 후 마커 신선·부재 양 조건에서 C6 통과, 전체 189/189. reviewer 독립 검증 동반 |
| 2026-07-25 | Codex 런타임 호환성 복구 — AGENTS.md 38,526→21,303B 압축(필수 규칙 유지), R14 32,000B 안전 한도·R18 config 계약 신설, `codex_hooks`→`hooks`, 프로젝트 64KiB 보조 한도, `.codex/hooks.json` 제거 후 config 인라인 SessionStart·PreToolUse·PostToolUse로 단일화, trust 계약·설치/리뷰 절차·ADR 031 동조. CLI 0.145.0에서 기본 32KiB prompt 끝단, 7 custom agents+reviewer spawn, SessionStart developer context, Post→Pre exit 2 차단 실측 | AGENTS.md·AGENTS.ko.md, `.codex/`, `.agents/hooks/`(+테스트), harness-install·harness-review, README, docs/adr/019·027·028·029·031, 관련 스펙·MOC | Codex 감사 M1~M3 및 Claude 교차 검토 후 실제 런타임 재측정. reviewer F1~F3를 반영해 Codex patch 경로 파싱·분리 trust 설치 단계·R18 handler 계약을 보강. Python 회귀 200/200·무결성 43/43·독립 재검토 PASS |
| 2026-07-22 | **누락 이력 보정(`5afc353`)** — integrity R15의 4자리 숫자 오탐 경계 추가, 스펙 완료 기준 제목 복원, team-review 도구 상한 안에서 테스트 재실행·diff 범위 대조 우선순위 명시 | `.agents/hooks/integrity-check.py`(+테스트), team-review, 무결성 스펙 | 당시 수정 커밋에 changelog 행이 빠져 §5 같은-커밋 규칙을 위반함. 2026-07-25 주간 무결성 점검에서 발견해 다음 하네스 변경에서 소급 기록 |
| 2026-07-25 | 토큰 효율형 오케스트레이션 적용 — Phase 0 Agent 호출 예산·동일 thread 델타 재사용, 자동 주입 루트 재독 금지·델타 패킷, 완료 경계 150k 세션 교체, `rtk` 탐색/원문 증거·출력 기본 예산, 7개 agent context-economy 계약, 17개 skill description 13,672→5,964B·CLAUDE.md 8,657→4,031B 압축, integrity R19(9,000B/5,500B)·행동 회귀 11종·ADR 032 신설, 한글 요약 뷰 동조, 안전·라우팅·테스트 3관점 독립 검토 PASS | CLAUDE.md, `.agents/skills/`, `.agents/agents/`, `.agents/{skills,agents}/README.ko.md`, `.agents/hooks/integrity-check.py`(+테스트), docs/adr/032, 관련 스펙·MOC | 사용자 전체 적용 승인 + 최근 28일 실측(Agent 92회, reviewer 49회, 고비용 세션 각 15회, 캐시 적중 96.6%). 검증 강도·라우팅 의미를 보존하면서 호출·재독·상시 노출 고정비를 우선 축소 |
| 2026-07-26 | 이해도 퀴즈 품질 기준 4항 신설(스펙 단일 원본 finish 4 내부) — ① 비자명(넘겨준 리포트·PR 본문·읽기 가이드로 축자/의역 매칭되거나 일반 상식으로 풀리면 실격, 공개한 결정은 "적용"을 묻는 형태로만 유효) ② 최소 1문 전이 문항(변경이 딛고 선 원리·대안·함정) ③ 오답지는 실제 기각한 대안·흔한 오해(기존 "strawman 금지" 구절 흡수) ④ 전부 정답이어도 문항별 1~2문장 해설(퀴즈와 같은 턴, ⓐ 분기는 반복 안 함), 통과 조건을 "해설 전달 후 진행"으로 개정. 한글 뷰 동조 | `.agents/skills/branch-workflow/`, `.agents/skills/README.ko.md` | 사용자 지적("반영 전 퀴즈는 의미 있고 많이 배울 수 있어야") — 기존 스펙이 소재·형식만 규정하고 비자명성 기준과 통과 후 학습 요소가 없어 실사용에서 자명 문항·무해설 통과가 발생(베이스라인 실측: 공통규칙 7). 인터뷰 결정 = 문항 수 2~4 유지·깊이 강화 + diff 밖 전이 문항 1문. 백그라운드 reviewer BLOCK(F1 ① 대 같은 단계 필수 소재 충돌·"verbatim" 회피 구멍, F2 일반 상식 문항이 ②를 통과, F5 중복, F6 ⓐ 분기 모호) 재작업 반영 |
| 2026-07-26 | 퀴즈 분량을 위험 축 기준으로 재정의 + 보완 질문 리스트 신설 — ① 커버리지 단위를 파일 수 → **독립 위험 축**(동작 변경·데이터/마이그레이션·인터페이스/신뢰 경계·되돌리기 어려운 결정), **종류 아닌 건수**로 집계, 축당 1문·위험 순 ② 4축 이하 1라운드(AskUserQuestion 4문 상한)·5~8축 2라운드(라운드마다 해설)·8축 초과는 PR 분할 신호 보고 ③ 축 목록·라운드 수를 1라운드와 같은 메시지에 공개(자체 채점 방지, 반박=불일치 신호) ④ 리뷰 결과 도중 도착 시 남은 라운드 완료 후 보고, 단 Critical·§3급은 즉시 보고·퀴즈 중단 ⑤ 해설 뒤 게이트 아닌 보완 질문 리스트 3개(오답 축·미표본 축 우선, 무응답 시 그대로 진행) ⑥ ⓑ 델타 재퀴즈를 "바뀐 축당 1문"으로 정합화(라운드 하한 미적용), finish 4를 라벨 서브블록 7개로 분해. 한글 뷰 동조 | `.agents/skills/branch-workflow/`, `.agents/skills/README.ko.md` | 사용자 지적("작업 크기에 따라 문항 수가 많아질 수 있지 않아, PR 때면 엄청 쌓여 있을 수도") — 기존 "2~4문 diff 크기 비례"가 큰 PR에서 표본을 무작위로 만들던 공백(공통규칙 7 베이스라인). 인터뷰 결정 = 위험 축 라운드 확장·리뷰보다 퀴즈 우선 완료·퀴즈 유지 + 보완 리스트 병행(전면 대체는 게이트 붕괴로 기각). reviewer 델타 BLOCK 4건(D1 축이 4종 분류로 읽혀 8축 규칙 사문화·공개 강제 부재, D2 축당 1문과 ② 전이 문항 충돌, D3 ⓑ 델타 경로가 라운드 하한 위반, D4 §3 Critical 즉시 보고 카브아웃 부재) 반영 |
| 2026-07-26 | metaskill 스킬 공통 규칙 8 신설 — "기존 블록에 절 추가·개정 시 사전 충돌 점검": 발화 조건은 **이미 절이 1개 이상 있는 블록에 절을 추가하거나 의미가 바뀌게 개정할 때**(신규 파일·절 삭제·문구 다듬기는 미발화, 단 의무·임계·면제·범위·발화 조건의 의미가 바뀌면 diff 크기와 무관하게 절 추가로 취급), 절차는 ① 블록의 현재 절 + 참조 포인터(타 스킬·에이전트 정의·CLAUDE.md 앵커·한글 뷰) 열거 ② 새·개정 절마다 (a) 모순 가능한 기존 절 (b) 발화 조건(충족 상황·위반 상황) 명시 ③ 발화 조건을 말할 수 없거나 스스로를 무력화하는 방식으로 충족되는 절은 미완성 판정. **reviewer 검증의 선행이지 대체 아님**을 명시(우회 차단), 규칙 7(압박 테스트)과의 경계 명시(7=실제 위반 겨냥 여부, 8=기존 규칙과의 정합). 기존 규칙 8→9 번호 이동 및 "all 8 rules" 참조 갱신, 한글 뷰 동조 | `.agents/skills/metaskill/SKILL.md`, `.agents/skills/README.ko.md` | 일일 점검 신호 ②(harness-review 2026-07-26) — branch-workflow finish 4 한 절이 이틀 3라운드 재작업, 사용자 정정 3회 + reviewer BLOCK 3라운드. 베이스라인 실측: 두 라운드 must-fix 8건 중 4건이 이 유형(F1 같은 단계 필수 소재 충돌, D1 분류가 자기 임계 규칙을 사문화, D2 축당 1문 대 전이 문항 충돌, D3 델타 경로가 2개 절 앞 하한 위반). 결함은 전부 reviewer가 잡아 사용자 도달 0건 — 신호는 재작업 비용. **공통 규칙 7(압박 테스트) 충족 근거**: 베이스라인 레그는 위 실측 4건, 준수 확인 레그는 이 규칙을 **자기 자신에게 적용해 발화한 것**으로 갈음한다 — 초안이 `문구 다듬기`를 객관 기준 없이 면제해 "개정을 다듬기로 자칭하면 점검을 건너뛰는" 자기 무력화 경로를 만들었고(reviewer B1, 규칙 자신의 D1 유형), 그 발견에 따라 면제를 의무·임계·면제·범위·발화 조건 기준으로 좁히고 트리거에 개정을 명시했다(B2). 합성 압박 시나리오는 개선 모드 검증 비례성·§16에 비추어 불균형이라 미실시 |
| 2026-07-26 | 지식 소스 라우팅 편입 — §7 8항 신설(REGISTRY.md **지식 소스 절**에 기록된 경우에만 발동, 신규 프로젝트·기능 스펙·tool-eval·**의무·임계·면제·범위·발동 조건을 바꾸는** metaskill 변경 착수 전 그 절을 읽고 진입점 조회, 큐레이션 노트(요약 포함)는 스크랩·모델 생성물이라 증거 아님 → 원문 URL 확인 후 사용·출처 없는 노트는 인용 불가, 발췌 주입 시 §3 봉투, **바깥 방향 기본 `<private>`** — 저장소 밖 산출물에 인용 가능한 것은 따라간 1차 출처뿐, orchestrate 게이트 대체 금지, 미기록·미확인·확인된 부재는 미발동·무고지 / 기록됐는데 읽기 실패한 경우에만 경로·실패를 한 줄로 밝히고 진행). REGISTRY.md 지식 소스 절 신설(3-상태 연결됨/없음(확인함)/미확인 + 진입점 2종 + 사용 계약 + 가용성 캐비앗), harness-install 4곳 개정(4단계 3계단 감지 — 링크→해석→실제 바이트, 5단계 연결 의사만 질의·임의 경로 수용·사내 프로필 고지, 6단계 생성 구조에 지식 소스 절 + 안정 심볼릭 링크 우선, 7단계 완료 기준 1행), ADR 033 신설, 한글 뷰 동조 | AGENTS.md(§7), AGENTS.ko.md, REGISTRY.md, `.agents/skills/harness-install/SKILL.md`, `.agents/skills/tool-eval/SKILL.md`, `.agents/skills/metaskill/SKILL.md`, docs/adr/033, docs/README.md | 사용자 검토 요청("vault wiki를 RAG처럼 활용해 project 내부 신규 제작 시 참고") + 실측. 값이 2종으로 갈림 — ① 설계 입력(사용자가 정리한 축·후보군, 모델 학습 데이터에 없음) ② cutoff 이후 보완(실측 시점 기준 55파일 340KB, "에이전트 평가" 검색 시 모델이 알 수 없는 벤치마크·검증 도구 3건 즉시 노출). 벡터 인덱스는 기각(큐레이션 계층 전체 rg 0.04초 — 해결할 병목 없음, 반대로 외부 임베딩 전송이 §3 위험·지식 루트 `.env` 실사례 확인). 트리거 범위 "넓게"(metaskill 포함)의 근거는 적용 직전 실제 조회에서 하네스 엔지니어링 고득점 6건 + §11과 같은 문제를 푸는 외부 도구 1건이 나온 실증. 인터뷰 결정 = 임의 경로 수용(특정 노트 앱·특정 하네스 결박 시 다른 설치처 배제) + 넓게. 규칙 8 사전 충돌 점검에서 3건 자체 해소(§7-3 게이트 우회 구실, §13-2 증거 오용, §3 외부 텍스트 미적용). **독립 reviewer BLOCK 재작업 반영** — must-fix 2건: F1 바깥 방향 미규율(트리거가 전부 문서 생성 작업이고 산출물은 PR로 나가는데 §3 `<private>`은 태그된 것만 보호, 연결 루트에 업무 맥락 디렉터리 실재 → 지식 루트를 기본 `<private>`로, 인용 가능한 것은 따라간 1차 출처뿐), F2 fail-open이 자기 설계와 모순(AGENTS.md는 미기록도 고지 대상인데 SKILL·REGISTRY·ADR은 '발동하지 않음' → 지식 루트 없는 모든 설치처가 트리거마다 영구 고지하는 fire-and-fail. 미기록·확인된 부재=미발동·무고지 / 기록됐는데 읽기 실패=경로+실패 한 줄로 분기). 권고 반영: F3 §3 봉투 의무 명문화, F4 고지에 경로·관찰된 실패 요구 + 확인 행위를 'REGISTRY 절 읽기'로 명시, F6 '규범적' 자칭 차단(metaskill 규칙 8 경계 인용), F7 도달 경로 배선(tool-eval 1단계·metaskill 개선 모드), F8 출처 없는 노트 대응을 생성 지침에 이식, F5 미확인 실행자 명시, F9 사내 고지를 '유출 아님' 대신 실제 결정 내용으로, F10~F18(탐침 무조건화·timeout·경로 일반화·수치 정정·한글 뷰 주어 확대·ADR 032 상호참조·읽기 범위 dotfile 제외). F16(계약 삼중화)은 reviewer가 defensible로 판정해 유지. ADR 수치 정정: 일자별 큐레이션 176→170건(월 인덱스 6건 오흡수), 조회 근거에 실제 명령·날짜 기록 |
| 2026-07-26 | harness-install 보조 도구에 **3h. jq** 신설(기본 설치) + 완료 체크리스트 1행 + 한글 뷰 동조 — OS별 설치 명령, 실패 시 무해(python3 폴백), 스킵 사유는 완료 보고 기록. 공통 규칙 8 사전 점검에서 걸러낸 충돌 2건을 절 안에 명시 처리: ① **상시 실행 스크립트의 무의존 불변식 불완화** — statusline과 git 게이트 체인(`.agents/githooks/` POSIX-sh 심 → `.agents/hooks/*.py`)은 지금의 셸·표준 라이브러리 python3를 유지(기본 설치는 최선 노력이라 스킵된 기계에서 게이트·렌더가 조용히 죽지 않아야 함), 3d①에도 역방향 포인터 신설 ② **3e rtk 경계** — 둘 다 파일을 읽으므로 축은 훑기 대 추출(rtk `json`은 전체를 훑기용으로 축소·`--keys-only`가 값 마스킹, jq는 아는 키를 짚어 질의), rtk 훅 명령 목록에 jq가 없어 재작성 간섭 없음. §3 절은 **도구가 아니라 경로**에 걸리도록 기술(전체 덤프는 어느 도구로 출력해도 `env`/`headers` 노출) | `.agents/skills/harness-install/SKILL.md`, `.agents/skills/README.ko.md` | 2026-07-26 `/doctor` 실행 중 jq 부재로 설정 조회를 python 스크립트 5,010자로 대체(회당 800~1,000 출력 토큰). 사용자가 이득 확인 후 설치 지시 → 설치처마다 재발하지 않도록 기본 설치 편입 지시. 상시 컨텍스트 비용 0(플러그인·스킬이 아닌 일반 CLI 바이너리)이라 유지 결정된 미사용 확장들과 성격이 다름. 지식 소스 조회(§7 8항)는 선택 불변 — 제약이 속도가 아니라 질의 작성 토큰이라 대안 후보가 판단을 바꾸지 않음. reviewer 독립 검증 BLOCK 반영 6건: F1 지식 소스 항목명이 이력 행으로 유출(REGISTRY.md 계약상 따라간 1차 출처만 인용 가능) → 식별자 삭제, F2 rtk 경계 오기(`rtk json <FILE>`은 파일 리더라 "명령 출력 압축"이 틀림) → 훑기/추출 축으로 교정, F3 §3 주의가 jq에만 걸려 `rtk json` 전체 덤프 누락(실측: API 키 평문 출력) → 경로 기준으로 확대, F4 불변식이 statusline 1건에만·역방향 포인터 부재 → 상시 실행 스크립트 전체로 일반화, F5 `~/.claude.json` 조회 근거를 tool-audit·harness-review로 잘못 귀속(실제로는 agentsview 사용) → 근거 교정, F6 체크리스트가 설치만 확인 → 형제 행처럼 규율 고지까지 포함. 델타 재검증 PASS 후 저강도 2건 추가 반영: N1 `.agents/githooks/`는 POSIX-sh 심이 python3 게이트를 호출하는 구조라 "전부 표준 라이브러리 python3" 표현 정정, N2 3c가 프로젝트 `.mcp.json` 생성을 금지하므로 파싱 대상 예시에서 제외 |
| 2026-07-26 | metaskill 공통 규칙 **9 「주장 출처」 신설** — 편집 대상 파일 **바깥**의 사실을 단언할 때(타 도구·CLI 동작, 타 스킬·에이전트의 절, 파일 구조, 명령 출력) 실제로 실행한 명령이나 연 파일 경로+절을 **변경 기록**(이력 행·커밋 메시지·제안)에 명시. **절 본문 인라인 금지**(상시 로드 비대 — 규칙 3·루트 §16). 근거를 댈 수 없는 문장은 규칙 8과 동일하게 미완성 판정. **검사 기준은 "확인 가능한가"**이며, 의무문으로 표현했다고 빠져나가지 못한다(처방문 안에 든 전제도 발화 — "X로 Y를 해라"는 X가 Y를 한다는 단언을 포함). 변경 기록은 저장소 밖으로 나가므로 **§3·루트 §7 8항이 그대로 적용** — 출처 자체를 인용할 수 없으면 식별자 없이 "확인했고 무엇이 결정됐는지"만 남긴다. 축 구분 명시 — 규칙 4는 "왜 이 규칙이 있나", 규칙 9는 "그 사실을 어떻게 아나", 규칙 8은 "기존 절과 정합하나". reviewer 검증의 선행이지 대체 아님. 기존 규칙 9(영어 우선)는 **10으로 이동**, `all 9 "skill common rules"` 참조를 10으로 갱신, 한글 뷰 동조 | `.agents/skills/metaskill/SKILL.md`, `.agents/skills/README.ko.md` | 주간 전체 점검 신호 ②(2026-07-26, F3) — 최근 하네스 변경 다수가 reviewer BLOCK/REWORK를 거쳐 착지(이력 행 137~141·172·174, 이 중 139·172는 같은 커밋). 규칙 8이 jq 변경(`6307e28`)보다 **먼저** 들어가 실제로 발동했는데도 **BLOCK(must-fix 1 + should-fix 5)**이 나왔고(판정 원본 `_workspace/jq-default-install/phase1_reviewer_report.md`), 그중 **2건**이 규칙 8이 겨냥하지 않는 **외부 사실 계열**이었다: ① `rtk json`을 `--help` 확인 없이 "명령 출력 압축"으로 단정(실제는 `rtk json <FILE>` 파일 리더) ② `~/.claude.json` 조회 필요 근거를 tool-audit·harness-review에 귀속(두 스킬은 `SKILL.md`에서 agentsview를 규정). **커버 범위는 6건 중 2건**이고 나머지 4건(범위·계약·일반화 계열)은 잡지 못한다 — 그 이상을 주장하는 것 자체가 이 규칙이 막으려는 미확인 단언이다. 결함은 전부 reviewer가 잡아 사용자 도달 0건이므로 겨냥 대상은 정확성이 아니라 **재작업 라운드** |
| 2026-07-26 | **사내 반입 경계(개인 PC 골조 → 사내 후속) 배선 신설** — ① REGISTRY.md에 「사내 반입 경계」 절(분류표 5행, 판정 문장 "사내에 이미 구축된 상태를 봐야 정할 수 있는가?", 인계 원칙 3 = 사내 가정 비하드코딩·어댑터 이음매·미구현부 시끄러운 실패+README 반입 체크리스트, 반출 주의) + 레지스트리 표 "반입" 컬럼(`사내`/`로컬`/`미확인`, 추측 금지) ② doc-writer 스펙 템플릿에 반입 대상 조건부 지침 — **절 추가 없이** 기존 비목표·인터페이스·완료 기준에 배치 ③ metaskill scaffold §1.1 신설(생성 시 1회 질의, 배포 방식 항목의 **상위 분기**) + §4 표 컬럼 ④ harness-install 5·6·7단계(설치처 1회 질의, 절 생성 구조, 완료 기준 1행) | REGISTRY.md, `.agents/skills/doc-writer/references/templates.md`, `.agents/skills/metaskill/references/scaffold.md`, `.agents/skills/harness-install/SKILL.md` | 사용자 요청 — 개인 PC에서 골조를 잡고 사내에서 후속하는 패턴이 반복될 예정인데, 경계를 매 프롬프트에 길게 서술하는 비용이 큼(실사례: agent-eval-gate 기동 프롬프트가 사내 후속 항목 5종을 자연어로 나열). **자연어 서술은 무인 루프에서 반복마다 재해석되므로 스펙에 실려야 살아남는다**는 것이 배선의 핵심 근거. **규칙 8 사전 충돌 점검 3건 자체 해소**: ① scaffold 인터뷰의 기존 "Deployment method" 항목과 중복·모순(배포=k8s + 반입=사내 조합에 승자 규칙 없음) → 반입 질문을 상위 분기로 규정하고 `사내`일 때 배포 항목을 "사내 후속(미결정)"으로 대체 ② templates.md 3행 "Section structure and order are fixed" 및 doc-writer SKILL.md "Do not add sections arbitrarily"와 충돌 → 새 절을 만들지 않고 기존 3개 절에 조건부 지침만 부착 ③ 절 부재 시 조건부 스킵이 항상 발동해 규칙이 사문화 → 절 생성 주체를 harness-install로 배선하고 **소비처 2곳**(scaffold §1.1 질의, doc-writer 스펙 지침)을 "절 없으면 조용히 스킵"으로 통일(부재가 곧 off 스위치. harness-install은 생성자이지 소비처가 아니다). **규칙 9 출처**: doc-writer 템플릿 절 구조·scaffold 인터뷰 4항목·harness-install 5~7단계는 각 파일 직접 열람, 프로필/미추적 여부는 REGISTRY.md와 AGENTS.md §1·§5 확인, 실증 형태(어댑터 계약 테스트·"미구현, 사내 후속" 후 exit 1·README 반입 체크리스트)는 agent-eval-gate 스펙 본문 확인. §7 8항 지식 소스 조회 수행 — 사용자가 프롬프트에서 댄 5종(LLM 연결·온프렘 k8s·ArgoCD·CI/CD·타 앱 연동) 외에 **인증/SSO와 관측성 백엔드** 축이 빠져 있음을 확인해 분류표에 반영(REGISTRY.md 계약상 노트 식별자는 미기재). **독립 reviewer BLOCK 재작업 반영** — must-fix 3건: F6(High) 반출 술어가 양방향 만족 가능("회사 특정 스택명"을 좁게 읽으면 어떤 OSS 이름도 안 걸려 규칙이 사문화, 넓게 읽으면 이 변경 자신의 이력 행과 agent-eval-gate 스펙이 위반) → **회사를 식별하는 고유값**(호스트명·내부 URL·클러스터/네임스페이스명·사내 앱명·조직명·계정)으로 금지 대상을 좁히고 공개 제품 일반명은 명시적 허용, 판정 문장("우리 회사의 그 인스턴스를 짚는가?")·발동 지점(저장소 밖으로 나가는 텍스트 작성 순간)·소급 미적용 명문화. F1 templates.md가 컬럼만 보고 절 부재를 안 봐서 절 없는 설치처에서 stale 컬럼이 doc-writer만 발화시킴 → 두 신호를 순서대로 읽도록 개정, 소비처 수 3→2 정정. F5 `체크리스트가 README에 있다`가 존재형이라 빈 제목으로 충족(autoloop의 기계 게이트는 완료 기준 절 존재뿐) → 비목표 열거 항목과 **건수 일치**·경로당 1케이스로 셀 수 있게 교체. 저강도 반영: F2·F3 `미확인` 해소 경로 명시(스펙 작성 시 1회 질의 / harness-install 재실행), F4 묶음 등록 행의 발화 조건(전체 동일값일 때만 기재, 섞이면 하위 표로). reviewer가 건전 판정한 것: 절 추가 없음·비목표 개제목이 구조 규칙 위반 아님, scaffold 상위 분기 승자 명확, 신규 clone 순서 정상, §7 8항 준수. **델타 재검증도 BLOCK — 2건 모두 "같은 결함이 재작업이 쓸지 않은 두 번째 지점에서 재발"**: D1 폐기한 술어("never internal stack names")가 harness-install **6단계**에 잔존, 그 단계가 미래의 모든 REGISTRY.md를 생성하는 지시문이라 새 설치처마다 모호한 술어가 재생산될 자리였음(5단계만 고쳐져 있었다) → 열거된 금지 + 명시적 허용 + 판정 문장으로 교체. D2 `미확인` 해소자로 지목한 `harness-install` 재실행이 같은 파일의 "Do not ask this per project"와 모순 → 해소 경로를 doc-writer 1개로 축소하고 스펙 없는 프로젝트가 미확인으로 남는 것이 결함 아님을 명시(규칙 자체가 스펙을 통해서만 발동). 저강도 3건 동반 수정: D3 묶음 등록 행의 의도적 `미확인`을 doc-writer 쓰기백이 평탄화하는 경로 차단(하위 표로 유도), D5 비목표 열거가 비면 두 완료 기준이 공허해지는 상태를 `로컬` 오분류 신호로 규정, D6 소급 미적용의 판정 단위를 문서가 아니라 **작성 행위**로 명확화(오래된 스펙이 면제 구역이 되지 않게). F7~F9·F11은 재작업으로 지지 하중이 생기지 않아 관찰 기록으로 유지, F10은 델타로 해소 |
| 2026-07-26 | **하위 하네스 secret gist 미러 배선** — 반입 대상 하위 AGENTS.md를 gist로 이송하는 경로를 규칙화. ① scaffold §1.1에 생성 절차(gist 등록 + REGISTRY 이송 표 행 + 파일 자신에 갱신·수신 명령 기재) ② metaskill 반입 시나리오 2단계를 "손으로 복사"에서 gist 수신으로 교체(미기록 = 구 프로젝트 → 수동 폴백) ③ 완료 체크리스트에 "미러된 파일을 고쳤으면 같은 작업에서 gist 갱신" 1행 ④ ADR 006에 **의식적 예외** 행 — 라우팅은 여전히 `.agents/projects/`만 읽고 gist는 이송 수단이지 연결 장치가 아니지만, "배포본은 어긋나는 순간부터 부채"라는 원 근거는 그대로 유효하므로 드리프트를 **수용된 잔여 위험**으로 명시(완화책 2종 모두 비기계적: 동시 갱신 의무 + 변경 이력 마지막 행 대조) | `.agents/skills/metaskill/SKILL.md`, `.agents/skills/metaskill/references/scaffold.md`, `docs/adr/006-subproject-harness-central-management.md`, REGISTRY.md·`.agents/projects/agent-eval-gate/AGENTS.md`(미추적) | 사용자 지시 — 반입할 때마다 이송 방법을 다시 정하지 않도록. **루트 하네스 저장소가 public임을 실측 확인**(`gh repo view nohdol97/nohdol-harness --json visibility` → `PUBLIC`)해 "하네스 저장소에 실어 pull로 받는" 경로가 원천 불가임이 확정됐고, 이것이 gist를 택한 직접 근거다. **규칙 9 출처 — 명령 3건 전부 실측**: `gh gist create`에 `--secret` 플래그는 **없다**(기본 secret, `--public`이 반대 플래그 — `--help` 확인 전 사용해 1회 실패), `gh gist edit <id> <로컬경로>` exit 0 후 내용 일치 확인, **`gh gist view <id> --raw`는 description과 빈 줄을 출력 앞에 붙인다**(127행 대 로컬 125행) → `-f <파일명>` 필수이며 그때 완전 일치. 세 번째 함정은 받은 파일 맨 위에 잡줄 2행을 조용히 넣고 마크다운이라 경고도 없어, 규칙 문면에 `-f` 필수를 근거와 함께 명시 |
| 2026-07-26 | **remote 명명 규약 고정 + 바깥 방향 push 기계적 차단** — ① 반입 프로젝트의 remote 이름을 규약으로 못박음(`origin`=이쪽/사내, `upstream`=저쪽/사외 pull 전용). 이름만 보고 어느 쪽인지 알 수 있어야 나중 세션이 반입 시 올바른 이름으로 배선한다 ② `git remote set-url --push upstream DISABLED` 제안을 반입 시나리오 1단계·하위 AGENTS.md §5에 편입, 사내 프로필 하네스 클론에는 `origin` 대상으로 harness-install 5단계에 편입 ③ **도달 범위를 정확히 기술** — push만 막고 편집·커밋은 막지 않으므로 사내 프로필의 "수정 금지" 절반은 여전히 문서 규율이다(되돌릴 수 없는 단계만 닫는 것). 실행은 사용자 몫(제안만) | `.agents/skills/metaskill/SKILL.md`, `.agents/skills/harness-install/SKILL.md`, `.agents/projects/agent-eval-gate/AGENTS.md`(미추적) | 사용자 지시 — 사외 변경분을 사내로 받는 가장 편한 경로가 하네스와 동일한 `git pull`(사내에서 개인 GitHub pull이 이미 운영 중)로 확정되면서, `origin`(사내)·`upstream`(사외)이 한 저장소에 공존하게 됨. 이 상태에서 `git push upstream` 한 번이면 사내 커밋이 개인 계정으로 나간다(§3). "pull 전용"은 문서 규칙이라 강제력이 없어 실행 계층으로 내림. **규칙 9 출처**: 임시 저장소를 만들어 실측 — `set-url --push … DISABLED` 후 `git push upstream main` → **exit 128**(`'DISABLED' does not appear to be a git repository`), `git fetch` → **exit 0**, `git pull` → **exit 0**, git 2.39.5. 차단 전 같은 저장소에서 push가 정상 동작함도 확인해 대조군을 세움 |
| 2026-07-26 | **하위 하네스 부재 시 라우팅 정지** 신설(§7 2항) + 3라운드 reviewer BLOCK 3건 반영 — ① 레지스트리 행이 하네스를 기록했는데 파일이 없으면 **제품 변경 전 정지**(조회·진단은 열어 둠 — 복구에 필요, Edit·Write·커밋은 복구까지 보류). 부재가 조용하고 프로젝트 규칙·게이트를 **한꺼번에** 내리므로 "덜 아는 상태"가 아니라 "규칙 없는 상태"가 된다. §13-0의 "멈추지 말고 선택"보다 우선함을 §2 근거로 명시(선택을 판정할 규칙 자체가 없는 §3급) ② **R3-1** 반입 시나리오가 실행되는 기계에서 발화 불가였음 — 「사내 반입 경계」 절은 **수출 측** 질문("여기서 만든 걸 사내로 가져가나")으로 생성돼 반입 측에서는 정직한 답이 "아니오"라 절이 없고, 그 절을 조건으로 걸면 시나리오가 바로 그 자리에서 죽는다 → 발화를 **반입 측 신호**(사용자 요청 / 레지스트리는 하네스를 적었는데 이 기계에 없음 / 레지스트리 행 없는 `.agents/projects/<이름>/` 도착)로 교체 ③ **R3-2** 모드 분기가 반입을 신규 생성으로 오인 — 하위 AGENTS.md 부재가 신설 모드 조건과 정확히 일치해 히스토리·remote가 이미 있는 트리에 `git init`과 스캐폴더를 실행할 경로였음 → 분기 **앞에** 반입 확인 절 추가 ④ **R3-3** 기록 수치 3건 재현 실패 정정(아래 행) ⑤ should-fix 3건: 프로필-기계 매핑에 실제 기계 우선 오버라이드(§5가 이미 원격 PR 세션을 개인으로 읽는 선례), `README.md § 사내 반입 체크리스트`를 사내 소유로 예외 처리(게이트가 이 기능이 존재하는 이유인 워크플로를 막고 있었음), "명시 지시"를 **보고 이후** 지시로 한정(정지를 유발한 요청 자신이 오버라이드가 되면 어떤 요청도 멈추지 않음 — `[no-test]`·`[secret-ok]`와 같은 침식) + 애매한 중간 케이스 처리(기운 쪽과 이유를 말하고 명확한 부분만 수행) | AGENTS.md(§7), AGENTS.ko.md, `.agents/skills/metaskill/SKILL.md`, `.agents/projects/agent-eval-gate/AGENTS.md`(미추적) | 사용자 제안 — "하위 하네스가 없다면 작업을 진행 안 하게 제한을 거는 건 어때". 수동 이송의 취약점(잊으면 조용히 실패)을 게이트로 전환. 같은 신호가 R3-1·R3-2도 해소한다 — 셋 다 "프로젝트는 있는데 하위 하네스가 없다"는 한 상태를 어떻게 감지·처리하느냐의 문제였다 |
| 2026-07-26 | ADR 006에 **"프로젝트 저장소를 깨끗이 두는 이유"** 행 보강 + metaskill 반입 시나리오 2단계에 같은 근거 1문장 (결정은 무변경 — 하위 하네스는 계속 `.agents/projects/` 유일본, 반입 시 수동 이송) | `docs/adr/006-subproject-harness-central-management.md`, `.agents/skills/metaskill/SKILL.md` | 사용자 확정 — **프로젝트 저장소는 우리 것이 아닐 수 있다**: 사내에서 합류하는 저장소에 이미 **다른 사람이 만들어 관리 중인 AGENTS.md**가 있을 수 있고, 우리 하네스 파일을 얹으면 남의 규약을 덮거나 두 규약이 한 파일에 섞인다. 원 ADR은 "하위 프로젝트 저장소가 언제나 깨끗하다"를 **결과로만** 적어 왜 깨끗해야 하는지가 빠져 있었고, 그 공백 때문에 이 세션에서 "프로젝트 저장소에 두면 git이 동기화하니 더 낫지 않나"는 재검토가 실제로 제기됐다(반입 자동화·이력 복구·양쪽 규칙 동조라는 이득이 실재해 뒤집힐 뻔했다). 근거가 문서에 없으면 같은 논의가 재발하므로 결정이 아니라 **근거**를 기록 |
| 2026-07-26 | **작업 위치 게이트 + 프로젝트 반입 시나리오 신설** — 경계 규칙이 스펙 작성 시점에만 발화하고 **작업 요청 시점에는 아무것도 안 하던 공백**을 메움. ① 하위 AGENTS.md에 「작업 위치」 절(현재 기계는 REGISTRY.md **설치처 프로필**로 판정 — `개인`=사외·`사내`=사내라 새 표식 불필요, 영역→파일 지도에서 사내 열은 **새 파일만**, 미등재 항목은 레지스트리 판정 문장으로, **양방향 대칭 정지-보고 게이트**: 기계와 요청이 어긋나면 파일·능력을 짚어 어느 쪽 소유인지 말하고 더 짧은 경로를 제시한 뒤 **명시 지시로만 진행**, 우회 시 후속 항목 기록). 기존 §4 Git은 §5로 이동하며 두-remote 배선(`origin`=이쪽, `upstream`=저쪽 pull 전용) 흡수 ② metaskill에 「프로젝트 반입 시나리오」(저장소 이동은 사용자 몫 §3, **하위 하네스 수동 이송** — 미추적이라 어느 clone·압축에도 안 담기고 빠뜨리면 프로젝트 규칙 없이 조용히 작업, REGISTRY 행, 작업 위치 절 존치 확인, 이력 2행). **루트에 둔 이유는 순서** — 반입 시점에 하위 하네스는 그 기계에 아직 없다 ③ scaffold §1.1에 `사내`일 때 작업 위치 절 생성 의무 ④ description에 반입 트리거 추가(474B/1,024B, 카탈로그 R19 파서 기준 6,091B/9,000B — 원시 행 합계는 6,363B로 스킬 17개 × 16B(`description: `·따옴표·개행)만큼 크다. 예산은 파싱값 기준이므로 파싱값으로 적는다) | `.agents/skills/metaskill/SKILL.md`, `.agents/skills/metaskill/references/scaffold.md`, `.agents/skills/README.ko.md`, `.agents/projects/agent-eval-gate/AGENTS.md`(미추적) | 사용자 요청 — "사외에서 사내용 프로젝트를 만들 때도 들어가야 하고, 사내에서 가져오라고 했을 때 절차를 밟아야 하고, 작업을 요청하면 이건 어디서 할 일인지 LLM이 판단해야 한다". 직전 배선은 doc-writer 스펙 작성 경로에만 걸려 있어 **이미 만들어진 프로젝트에서 작업을 요청하는 흐름**에 게이트가 없었다. 게이트 강도는 인터뷰 결정 = **양방향 모두 정지**(사외→사내: 사내 상태를 추측해 만든 산출물은 어차피 사내에서 재작업이라 절약이 아니라 중복. 사내→사외: 저쪽이 소유한 파일을 고치면 이후 모든 `git pull`이 충돌하고 정본이 둘로 갈라짐). **규칙 8 사전 충돌 점검 3건**: ① 하위 AGENTS.md 기존 §4(Git)와 번호 충돌 → 신설 절을 §4로 두고 Git을 §5로 이동(remote 배선이 새 절과 같은 축이라 인접 배치가 맞음) ② metaskill 기존 「신규 프로젝트 생성 시나리오」와 트리거 혼동 → "저장소가 이미 히스토리째 존재하므로 생성하는 것은 하네스 쪽뿐"으로 구분 명시 ③ orchestrate 게이트와의 층위 — 이 게이트는 **장소**를, orchestrate는 **실행 형태**를 정하므로 충돌 아니나 순서상 장소 판정이 선행(사내 작업이면 orchestrate 자체가 무의미). **규칙 9 출처**: 파일 지도는 `git ls-files`로 추적 파일 **62개** 확인(초안은 44로 적었다 — `head -45`로 잘린 출력을 전수로 세어 넣은 오류, reviewer R3-3이 재현 실패로 검출), 하위 AGENTS.md 절 구조·metaskill 시나리오 목록은 직접 열람, description 예산은 `wc -c`와 R19 파서 양쪽 실측 |
| 2026-07-26 | ADR 032 「한계」에 150k 세션 경계의 **강제 장치 부재·실측·재검토 조건** 명문화 (규칙 문구는 무변경 — 본문 11·32행이 이미 "권고"로 적고 있어 격하할 것이 없었다) | `docs/adr/032-token-efficient-orchestration.md` | 주간 전체 점검 신호 ④(2026-07-26, F8) — 규칙 적용 커밋(`73c4b68`, 2026-07-25T05:42Z) **이후 시작된** 세션 **10건 중 임계 초과 4건**(202,910 / 216,110 / 290,518 / 351,848, 전부 07-26), 이하 6건(최대 128,545) = **초과율 40%**. 07-25의 241,717·228,516은 커밋 이전 시작이라 규칙 미적재로 제외(reviewer M2 — 초안은 이 둘을 포함해 "6건"으로 셌다). **분모는 reviewer 델타 N1로 재정정** — round-1 M2가 제시한 post-rule 5건을 그대로 구현했으나 커밋 이후 시작된 07-25 세션 5건이 누락돼 있었고, 피크 0 세션도 분모에 포함하기로 규칙을 명시했다. 수치는 시점 스냅샷(진행 중 세션은 값이 오름). 출처 `agentsview session list --project nohdol_harness --date-from 2026-07-19 --json` + `git show -s --format=%cI 73c4b68`. 사용자 결정: 고지 장치 신설(선택지 B) 아닌 **권고 유지(선택지 A)**. **사유에서 "기계 판정 불가라 훅 승격 불가"는 삭제** — `gate-reminder` 훅이 이미 휴리스틱 조건 위에서 세션당 1회·fail-open으로 동작하는 반례이고(reviewer M3), tdd-gate도 판정 불가를 fail-open으로 통과시킨다. 즉 장치는 기술적으로 가능하며 이번 결정은 그 선택을 하지 않은 것일 뿐이다. 재검토 조건도 "기계 판정 가능해질 때"에서 **"임계 초과가 실제 손실(이월 실패·재작업)로 이어진 사례가 관측될 때"**로 교체 — 전자는 gate-reminder 계열 경로를 영구히 봉쇄한다. 관측 지점은 harness-review 주간 점검(신호 ②·④)으로 명시해 조건이 발화 가능하게 했다 |
| 2026-07-27 | **머지·배포 상태 단정 전 실측 의무** 신설(§5, 기존 「루트 원격 PR 세션」 항 바로 다음) — PR 머지·배포·GitOps sync·이슈·파이프라인 상태를 **질문이 아닌 모든 형태로 단정하기 전에**(완충 표현 포함) 지금 확인하도록 요구한다. 확인 표면을 셋으로 분리: ① 로컬 체크아웃은 `git fetch` **후 원격 추적 ref를 읽는다**(`git status -sb`·`git log origin/<브랜치>`·`git show origin/<브랜치>:<경로>`) ② 원격 호스팅 상태는 gh·curl ③ 클러스터 반영은 `release`의 rollout 관측. 대화 최근성·미갱신 읽기 추론 금지, 확인 불가 시 **미확인** 명시. 기존 §5 「루트 원격 PR 세션」의 fetch 확인은 루트 하네스 자체 PR 사이클의 후속 **작업** 전에만 걸리므로, 이를 전 프로젝트(하위 포함)의 상태 **발언** 전체로 일반화한 것이다 | AGENTS.md(§5), AGENTS.ko.md | 사용자 제안(사내 설치처 대기 큐 항목 이관) — 신호 ②(같은 실패 2회). ① 이슈 #250 T5 중 PR #261이 이미 머지됐는데 확인 없이 "아직 머지·배포 전"이라고 단언 — **확인 자체를 스킵**한 경우 ② 2026-07-27 mx-ai-store PR #269 관련, 다른 세션이 미리 클론해 둔 로컬 GitOps 체크아웃을 `git fetch` 없이 읽고 "아직 배포 전"이라고 단언 — **확인은 했으나 그 확인이 최신이 아니었던** 경우. 두 사례 모두 사용자 정정을 받았다. auto-memory(`state-claims-need-fresh-check.md`)가 이미 "재발 시 규칙 승격"을 예고했으나 memory는 회상에 의존해 재발을 막지 못했으므로, §8의 "교차 도구 교훈은 승인된 metaskill로 하네스 규칙에 승격" 경로를 따라 상시 로드 규칙으로 올렸다. **문안은 사용자 지정 원문에서 개정됨 — reviewer BLOCK 반영**: 원문은 "`git fetch` 후 로컬 체크아웃을 읽어라"였는데 **fetch는 원격 추적 ref만 갱신하고 워킹트리·`git log -1`은 낡은 채로 남아** 사례 ②를 그대로 재현시키는 문안이었다(2저장소 재현 실측: fetch 직후 `cat state.txt`=`v1`·`git log -1`=`init`인데 `git show origin/main:state.txt`=`v2-DEPLOYED`). 일반화하려던 원 조항이 "fetch **and check**"로 더 정확했고 신설안이 check를 떨어뜨린 것 — 읽을 표면을 명시해 교정. 함께 반영: GitOps sync를 git 표면으로 판정하려던 과잉 주장 제거(저장소는 desired state뿐 — `release` 스킬이 이미 "GitOps sync → observe rollout"으로 분리), "이번 세션에서 만들지 않은 체크아웃" 면제 삭제(장시간·autoloop 세션에선 세션 초 클론도 낡는다), 승격 원본 memory가 덮던 이슈·파이프라인 상태 회복, 확인 불가 시 미확인 명시, 완충 표현도 트리거에 포함. **규칙 8 사전 충돌 점검**: 기존 §5 PR 사이클 항과는 발동 지점이 다르고(행동 전 vs 발언 전), §13-2 신선한 최종 검증과는 대상이 다르다(완료·통과 주장 vs 상태 단정 — §13은 헤더가 코드 작업으로 범위를 한정해 상속되지 않는다). reviewer가 §5 포인터 15곳 이상을 훑어 모순 0건 확인. **규칙 9 출처**: `gh --version`(2.86.0) 실측, fetch 동작은 위 2저장소 재현 실측, release 절차는 `.agents/skills/release/SKILL.md` 직접 열람, 크기는 integrity-check `PASS R14`. **이 행 자체가 규칙 위반을 한 번 저질렀다(기록으로 남긴다)**: reviewer의 "changelog 행 없음" 지적을 `grep -c`=1이라는 **현재 읽기**로 "오탐"이라 반박했는데, reviewer가 그 파일을 읽던 시점에는 행이 실제로 없었다(발행 순서: reviewer 발행 → AGENTS.ko.md 편집 → changelog 행 추가. `git show e0f21b1:docs/harness-changelog.md | grep -c` = 0, 현재도 미커밋 `M`). 현재 상태로 과거 상태를 단정한 것이며, **바로 이 조항이 막으려는 실패 유형**이다 — 델타 라운드에서 reviewer가 검출해 정정했다. reviewer 측 근거에도 오류가 있었다(표 마지막 행을 `:190`으로 적었으나 `## AGENTS.md` 절은 `:5`–`:150`, 절 구분 없이 파일 전체를 grep한 결과 — 결론은 옳았고 행 번호가 틀렸다). **델타 라운드 추가 반영 2건**: `release` 위임을 표면 직접 지목으로 교체(그 스킬에는 rollout 조회 명령이 없고 description이 배포 실행으로 범위를 한정한다 — `.agents/skills/release/SKILL.md:3·24·43` 확인, 대체 표면 `kubectl` v1.34.1 실측), `git status -sb`를 후순위로 내리고 한정(detached HEAD에서 `## HEAD (no branch)`라는 **깨끗해 보이는 줄**만 반환해 조용히 무용 — 재현 실측). **3라운드 반영**: 클러스터 표면 두 개가 서로 다른 질문에 답하는데 나열형으로 묶여 있어, 판정력이 없는 쪽이 구체적이라는 비대칭이 F3를 그 수정 안에서 재도입할 뻔했다 — sync 상태는 **리비전 동일성**("내 커밋이 적용됐나")을, `rollout status`는 **건강성**("클러스터에 있는 스펙이 롤아웃을 끝냈나")만 답한다. 미sync 상태에서는 이전 리비전이 정상이라 초록으로 읽힌다. `kubectl rollout status --help`(v1.34.1) 실측: 커밋 identity를 받는 입력이 없고 `--revision`은 "Pin to a specific revision … Defaults to 0 (last revision)"인 ReplicaSet 리비전이며, `-w/--watch` 기본값이 `true`·`--timeout=0s`가 무한이라 맨 명령은 무인 autoloop에서 무한 블로킹 감시가 된다 → 판정 주체를 컨트롤러 sync 상태로 명시하고 `--watch=false`를 붙였다. Argo CD·Flux 구체 명령은 **적지 않았다** — 이 기계에 두 CLI도 클러스터도 없어 문법을 쓰면 이 조항이 이미 두 번 고친 바로 그 미검증 주장이 된다. 대신 컨트롤러 무관 판정 기준(실행 중 revision·이미지가 주장 대상 커밋과 일치하는가)으로 적었다 |
| 2026-07-27 | **반입 시나리오에 배치 인터뷰 1회 지점 신설 + 하위 하네스 부재 추론 금지** — ① 시나리오 머리에 **유일한 질문 지점**을 두고 ⓐ 저쪽 기계의 하위 하네스 존재·도착 경로 ⓑ 사내 remote 존재·URL을 함께 묻는다(§13-0) ② 2단계: 저쪽 REGISTRY.md는 그 기계에만 있어 읽을 수 없으므로 **내 레지스트리의 침묵은 부재의 근거가 아니다**. 반입은 어느 쪽이든 하네스가 있는 상태로 끝나므로 답을 분기시킨다 — 있음→손 복사 / 저쪽에도 없음→4단계로 지금 작성 / 답 못 받음→`미확인`(맨 `없음` 금지) + 보고 표시 ③ 1단계: 인터뷰가 준 URL만 배선하고, 추측+`git ls-remote` 확인은 **사용자에게 알려 확인받은 뒤에만** `origin`이 된다 ④ 3단계에 `미확인` 분기의 행 명명 규칙 ⑤ REGISTRY.md 등록 규약에 「하네스 유무」 3-상태 정의 신설 ⑥ **§7 2항 발동 조건에 `미확인` 추가**(델타 D1 — 아래) | AGENTS.md(§7 2항), AGENTS.ko.md, `.agents/skills/metaskill/SKILL.md`, `.agents/skills/README.ko.md`, `REGISTRY.md`(미추적) | 사내 설치처 대기 큐 항목 이관 — 반입 중 세션이 저쪽 하네스 존재를 판단할 수 없는데도 묻지 않고 "하네스 없음"으로 조용히 결론짓고 레지스트리에 기록한 실패(2026-07-27, 다른 설치처 사용자 보고). 하네스는 실재했다. **원문 그대로 적용 불가 — 대상 문장이 이미 철회됨**: 큐 항목은 2단계의 "Fetch it from the secret gist recorded in the exporting site's REGISTRY.md transfer table"를 고치라고 지정했으나 그 gist 미러는 `100891f`로 배선됐다 `e0f21b1`로 같은 날 철회됐고 현재 2단계는 정반대("There is deliberately no automated channel")를 못박는다(`grep -c 'secret gist'`·`'transfer table'` 각 0). 대기 큐 절차의 "파일이 어긋났으면 원문 적용 말고 의도를 현재 구조로 번역"에 따라 **이송 수단 중립 문안**으로 옮겼고 사용자가 승인했다 — 핵심("다른 기기에만 있는 정보를 이 세션이 판단할 수 있다는 가정")은 gist 유무와 무관하게 유효하다. **reviewer BLOCK 반영 6건**: F1 절이 준수되면서도 원 실패를 그대로 저지를 수 있었다(답을 못 받은 경우의 분기가 없어 `없음`으로 귀결) — 4단계가 이미 "없으면 지금 작성"이라 `없음`은 애초에 정당한 반입 결과가 아니었고, 레지스트리의 "확인한 부재 ≠ 확인하지 않음" 3-상태 규약도 이 컬럼엔 적용돼 있지 않았다. F2 두 삽입이 서로를 가리키는 순환 참조에 위반 시점만 3단계로 적혀 있어 "작업 시작 후 질문 금지"를 어기고도 문면을 충족할 수 있었다 → 단일 질문 지점으로 교체. F3 1단계 삽입이 유일하게 **권한을 넓히는** 조항이었는데 §13-0의 "선택하고 **알린다**" 중 알림이 빠져 추측 URL이 확인 없이 push 대상이 될 수 있었다. F4 `Measured 2026-07-27`은 다른 기계 세션의 전언이라 이 파일의 다른 `Measured`(실행한 명령+버전)와 등급이 다르다 → `Observed (user report)`로 재귀속. F6 `.agents/skills/README.ko.md` 미재생성(§15) — R17은 제목 집합만 대조해 본문 드리프트를 못 잡는다(`integrity-check.py:533`); 같은 편집에서 발화 조건이 낡아 있던 기존 드리프트(「사내 반입 경계」 절 유무를 조건으로 걸던 문구, 2026-07-26 R3-1로 이미 폐기됨)도 정정. F7 "by what route it would arrive"가 바로 아래 "no automated channel"과 오독 여지 → 축소. **규칙 9 출처**: `git ls-remote`는 이 스킬이 새로 도입한 명령이라 형제 조항들과 달리 실측 근거가 없었다 — 문안을 "떠보기 결과는 사용자 확인 전 배선 근거가 못 된다"로 바꿔 명령 동작에 대한 단언을 **보수적 형태로 축소**했다(제거는 아니다 — 델타 D8). `.gitignore:8`(REGISTRY.md 미추적)·`AGENTS.md` §7 2항·`SKILL.md` 4단계·`REGISTRY.md` 3-상태 규약은 직접 열람 확인. **델타 라운드 BLOCK 4건 추가 반영**: **D1(High)** — `없음` 대신 `미확인`을 적는 것만으로는 구멍이 안 막혔다. §7 2항이 "하네스를 기록했는데 파일이 없을 때"만 발동해 `미확인` 행은 **다음 세션에서 정지를 못 걸고** 그대로 규칙 없이 작업하게 된다(실패가 "기록된 거짓 부재"에서 "기록된 정직한 불확실"로 옮겨갔을 뿐 잡히지는 않음). §7 2항을 `미확인` 포함으로 개정하고 REGISTRY 등록 규약·스킬 2단계·한국어 뷰에 같은 내용을 실었다. **D2** — "반입은 어느 쪽이든 하네스가 있는 상태로 끝난다"가 세 분기 중 `미확인`에서 거짓이었고, 그 문장을 내면화한 세션은 모순을 풀려고 여기서 하네스를 지어내 정본을 둘로 만들 수 있었다 → "부재를 가정한 채로 끝나지 않는다"로 교체 + 지어내기 금지 명시. **D3** — 새 `미확인`이 반입 컬럼의 기존 `미확인`과 한 파일에 공존하게 되면서 "해소 경로는 하나다"(doc-writer)가 양쪽을 덮는 것으로 오독될 여지 → 반입 컬럼 한정으로 수식하고 하네스 유무에는 도착 트리거를 해소 경로로 부여(스킬 발화 조건 3번을 `미확인` 행 포함으로 확장 — D6). **D4** — REGISTRY.md 자체 변경 이력 행 누락(그 파일은 미추적이라 이 changelog가 대신할 수 없다, 스킬 5단계가 요구). **should-fix 반영**: D5 "아직 없음"이 정상 경로인데 분기가 없던 것(원격은 원래 사용자가 지금 만든다), D7 트리거가 이미 답한 질문은 건너뛰기. **3라운드 BLOCK 2건 — 개정한 §7 2항 자체의 결함**: **R1** `미확인`은 하네스 유무·반입 **두 컬럼의 합법 값**인데 상시 로드 규칙에서 컬럼을 한정하지 않아, 반입=`미확인`·하네스 유무=`없음(확인함)` 행이 "확인된 부재"임에도 정지에 걸렸다 → 하네스 유무로 한정. **R2(더 큰 쪽)** 반입 범위가 없어 **로컬 전용 프로젝트까지 정지 대상**이 됐다 — `harness-install`은 `.agents/projects/<이름>/` 존재만으로 판단하고 반입 질문을 설치처 단위로 한 번만 하므로(그 스킬 116·130행) 부재 시 쓸 값이 `미확인`뿐인데, 다른 기계가 없는 프로젝트에는 **해소 경로가 없어 충족 불가능한 §3급 게이트**가 된다. 규칙 8의 판정 기준대로 **발동 조건이 자기 근거를 넘어선 것**이다 — 근거가 "선택을 판정할 규칙이 없다"인데 로컬 프로젝트는 루트 하네스가 온전히 관장하므로 빠진 규칙이 없다. 충족 불가능한 가드레일은 정지시키는 것보다 나쁘다(§3을 우회해 추론하도록 학습시킨다) → 발동 조건을 **반입이 `로컬`이 아닌 행**으로 한정하고, `harness-install`에 부재 시 `미확인`을 적는다는 규칙과 그 한정을 함께 기재. **should-fix 반영**: R3 재진입 시 이미 끝난 단계를 되풀이하지 않도록 재개 지점 명시(+`git remote -v`로 알 수 있는 것은 묻지 않기, 4·5단계는 그대로 — 갓 도착한 하위 하네스의 경계 절 확인이 재진입에서 가장 중요하다), R4 묶음 행의 `미확인`은 다른 의미(묶음 내 값 혼재)이므로 이 정의와 분리하고 해소는 하위 프로젝트 표에서 한다고 명시. **4라운드 BLOCK 1건 — R2 수정이 문제를 옮겼을 뿐이었다**: 반입 한정("`로컬`이 아닌 행")은 `로컬`이라고 **명시된** 행만 구제하는데, `harness-install`은 반입을 프로젝트별로 묻지 않으므로(그 스킬 130행 — "설치처 단위로 한 번만") 신규 설치가 만드는 행은 하네스 유무·반입 **둘 다 `미확인`**이고 정지가 그대로 걸린다. 이번엔 탈출구가 있었지만(스펙을 쓰면 doc-writer가 반입을 묻는다) 로컬 프로젝트를 고치려고 사내 반입 스펙을 먼저 쓰는 경로를 밟을 사람은 없다 → **규칙과 데이터 양쪽에서 닫았다**: 발동 조건에 「사내 반입 경계」 절 존재를 추가하고(절이 없는 설치처는 반입 자체를 안 하므로 발동 불가), `harness-install`에는 **반입 질문에 "아니오"면 전 행을 `로컬`로 적는다**는 규칙을 넣었다 — 그 질문의 비대칭("예"는 프로젝트별로 결정하지 못하지만 "아니오"는 전부 결정한다)이 그때까지 미사용이었다. 같은 라운드에서 이 두 지점의 "로컬 프로젝트는 영향 없다"는 문장이 자기 스킬이 만드는 상태에 대해 거짓이었던 것도 정정(규칙 9 — 처방문에 든 확인 가능한 단언). **5라운드 BLOCK 1건 — 그 「사내 반입 경계」 절 게이트가 정지를 뒤집었다(reviewer가 자기 4라운드 제안을 철회)**: 그 절은 **수출 측** 질문("여기서 만든 프로젝트가 사내로 가는가")의 답이라 **받는 기계에는 정당하게 없다** — 바로 그 점을 `metaskill` 반입 시나리오 발화 조건이 2026-07-26에 이미 명문화해 뒀는데(`SKILL.md:48` "gating on it would make this scenario unreachable exactly where it runs"), 같은 실수를 한 층 위인 라우팅 규칙에서 되풀이했다. 결과적으로 이 정지가 **실패가 실제로 일어난 사내 기계에서만 꺼지고** 실패가 없던 개인 기계에서만 켜지는 상태였다 → 절 게이트를 제거하고 반입 한정만 남겼으며, 같은 실수 재발을 막기 위해 "이 조건을 덧붙이지 말라"는 이유를 조항 안에 명시했다. 반입 없는 설치처는 `harness-install`의 "아니오→전 행 `로컬`" 규칙이 데이터 쪽에서 막고, 그 규칙 이전에 쓰인 낡은 `미확인`/`미확인` 행은 `harness-install` 재실행으로 정리된다고 REGISTRY에 기재. **should-fix**: 정지에 걸린 세션이 스펙을 기다리지 않고 반입 질문을 바로 물어 해소할 수 있게 함(§3급 게이트의 유일한 출구가 "사내 반입 스펙 먼저 쓰기"면 아무도 안 밟는다), 반입 시나리오 트리거 (b)의 §7 설명이 첫 번째 limb만 가리키던 것을 양쪽으로 확장. **인터프리터 주의**: 이 세션의 `python3`는 3.14.6이라 무결성이 `44 pass 0 fail`이지만 `/usr/bin/python3`(3.9.6)에서는 `36 pass 0 fail 8 skip`이다 — R12×7·R18이 `tomllib` 부재로 SKIP된다. 결론(0 fail)은 같으나 기록에 수를 적을 때는 인터프리터를 함께 적는다 |
| 2026-07-28 | **`vault-write` 스킬 신설 — vault 쓰기 경로 + 노트 계약 참조 위임(ADR 034)** ① 루트 스킬은 **절차만** 담고 스키마·index 정책·증거 프로토콜은 실행 시점에 `nohdol-study` 하네스의 `note-writer/references/` 3종을 읽어 적용한다(사본 없음) ② study 하네스 루트는 REGISTRY.md에 이미 있는 vault 경로의 **부모**로 도출하고 `<parent>/AGENTS.md` 실재로 검증 ③ references 부재·vault 읽기 실패 시 **정지**(폴백 없음) ④ **Phase 3 기록 필수** — `index.md` 「최근 갱신」 한 줄(약 5건 상한) + 새 주제일 때만 「주제」에 허브 한 줄, `log.md` 맨 위 한 행(기존 행 불변), `hot.md` 갱신 ⑤ 접근 표면을 `wiki/`·`raw/`·3파일로 한정(dotfile·`.obsidian`·벡터 캐시 제외와 루트 전체 grep 금지는 유지) ⑥ **§7 8항의 fail-open을 읽기 방향으로 한정**(`AGENTS.md`·`AGENTS.ko.md`) — 지식 소스가 안 읽히면 "한 줄 밝히고 진행"이 무방향으로 적혀 있어 쓰기 경로에도 걸렸다. REGISTRY.md 「가용성」만 방향별로 나누면 상시 로드 원본과 설치처 데이터가 어긋나므로 같은 술어를 양쪽에 반영했다(델타 D6) | `.agents/skills/vault-write/SKILL.md`(신설), **`.agents/skills/harness-install/SKILL.md`**, `.agents/skills/README.ko.md`, **`AGENTS.md`(§7 8항)**, **`AGENTS.ko.md`**, `README.md`, `CLAUDE.md`, `docs/adr/034-vault-write-delegation.md`(신설), `docs/README.md`, `REGISTRY.md`(미추적) | 사용자 요청 — "vault에 작성하라고 할 때, index에 어떤 걸 작성하는지 남기고 문서 형식을 잘 맞춰서 작성하도록". ADR 033이 vault를 **읽기** 소스로만 편입해 두어 쓰기 요청에는 계약이 없었고, 그 상태에서는 형식을 **가장 최근에 읽은 파일 기준으로** 정하게 된다 — 아래 기준선이 실측한 결과, 깨지는 것은 기록 3면이 아니라 노트 계약이다. `nohdol-study`에 `note-writer`가 이미 있으나 **스킬은 세션 cwd 기준으로만 로드돼** 루트 세션에서 보이지 않는 것이 공백의 원인이다(§12가 하위 하네스에서 관측한 성질과 동일). **인터뷰 3문 사용자 결정**(§13-0): 설치 위치=루트 신설(nohdol-study 개선 아님 — 발동 지점이 루트다), 규칙 정본=참조 위임(사본 금지), 반출 경계=§3 시크릿 금지만(vault는 개인 지식 루트이지 발행물이 아님). **규칙 8 사전 충돌 점검 5건**: ⓪ **REGISTRY.md를 생성하는 `harness-install`이 이 블록의 포인터다** — 그 스킬이 4항까지만 출력하면 재실행 한 번으로 5항이 복구 불가능하게 사라지고(REGISTRY.md는 미추적, ADR 005) 4항이 다시 쓰기 표면을 금지한다. 초안이 이 포인터를 열거하지 않아 독립 검증 F1에서 High로 걸렸고, 생성기의 조항 수(본문·완료 체크리스트)와 5항 본문을 함께 개정했다. 델타 D3으로 「가용성」 항목도 같은 처지임이 드러나 생성기가 읽기/쓰기 분리까지 출력하도록 했다 ① REGISTRY.md 사용 계약 4항(읽기 범위를 진입점으로 한정)과 충돌 — 쓰기는 더 넓은 표면을 요구하므로 4항을 완화하지 않고 **5항을 신설해 쓰기 방향을 분리**하고, 4항이 실제로 막던 것(dotfile·벡터 캐시·루트 전체 grep = §3 시크릿 유출)은 쓰기 경로에도 그대로 상속시켰다 ② 같은 절 2항의 `<private>` 기본값과의 충돌 여부 — 2항은 **vault에서 밖으로** 나가는 방향이고 이 스킬은 **안으로** 들어가는 방향이라 발동 지점이 겹치지 않음(5항에 명시) ③ `doc-writer`와의 트리거 중복 — 대상 저장소가 다르므로(프로젝트 `docs/` vs vault) description의 `Not for`로 경계 고정, `carryover`(세션 이월 로컬 노트)도 같은 축으로 분리 ④ index 정책 자체의 자기모순 회피 — "작성한 것을 index에 남긴다"를 원자 노트 열거로 구현하면 index-policy의 「목록이 아니라 진입점」 규칙을 정면으로 위반하므로, 기록을 「최근 갱신」(상한 5)과 「주제」(허브만)로 **분리**하고 영구 기록은 `log.md`에 넘겼다. **규칙 9 출처**: references 3종은 직접 열람(`note-schema.md`·`index-policy.md`·`evidence-check.md`), 스크립트 실재는 `ls`로 확인(`diagram/scripts/check.py` 18.1K, `vault-gardening/scripts/garden.py` 8.4K), vault 레이아웃·`index.md` 「최근 갱신」 5건 상한·`log.md` 표 구조(최신 위, append-only)·`hot.md` ≤500토큰은 실제 파일 열람, Google Drive 2차 기록자 주의는 `nohdol-study/AGENTS.md` §5 열람, description는 **파싱값 509B**/1,024B(원시 행 525B), 카탈로그 **파싱 합계 6,600B**/9,000B(R19 예산 기준), 본문 258행/500행 — `python3` 3.14.6로 실측. 무결성 45 pass/0 fail(`R3 skill vault-write`·`R19` 포함). **규칙 7 압박 기준선 — 결과가 초안의 전제를 뒤집었다**: vault 사본(scratchpad, `index.md`·`log.md`·`hot.md` + wiki 3개)에 스킬 없는 `implementer`를 같은 과제로 발행해 실측했다. **기록 3면은 전부 지켰다** — 「최근 갱신」 맨 위 추가·5건 상한 유지, `log.md` 맨 위 행 추가·기존 행 불변, `hot.md` 3개 절 갱신, 그리고 index 「주제」에는 **스스로 넣지 않기로 판단**했다(그 절의 허브 규칙을 읽고). 셋 중 `index.md`·`log.md`는 본문에 자기 규칙을 적고 있지만 `hot.md`는 아닌데도 갱신됐다(D5 — 설명은 불완전했고 관측만 유효하다). 따라서 초안의 「the part that is always skipped」와 with/without의 "index가 노트를 모른다"는 **근거 없는 단언이라 삭제**했다(규칙 9 — 독립 검증 F2가 지적한 바로 그 문장). **실제로 깨진 곳은 노트 계약이었고, 전부 이웃 노트 모사였다**: `tags` 추가(스키마 제외 필드), `checked` 누락(버전 의존 주장인데도), 첫 기록에 `status: evergreen`(스키마는 `seed`), `sources`에 URL·`raw/` 경로 대신 산문. 에이전트 자기보고가 이유를 "house style"이라 적었고, 샌드박스에 넣은 이웃 3개가 계약 이전 노트라 정확히 그 결함을 물려받았다. **한계**: 샘플 3개가 전부 낡은 노트라 모사 압력이 실제 vault보다 셌을 수 있다(실 vault엔 계약 준수 노트도 있다). 이 결과로 Phase 2에 "계약은 references이지 옆 노트가 아니다" 절과 Phase 4의 8필드 정확 일치 검사를 신설했고, 기준선이 스스로 해낸 인바운드 링크 확보(vault `log.md`의 인바운드 0 사례를 근거로 든 판단)를 **요구사항으로 승격**했다. **독립 검증 5라운드(1~4 BLOCK, 5 PASS)**. **2라운드 BLOCK 반영(High 1·Med 3·Low 5)**: **D1(High)** — 기준선이 반증한 전제("index가 노트를 모른다·`log.md` 결번")가 5지점 중 3곳에 그대로 남아 있었는데 이 이력 행은 "삭제했다"고 적고 있었다(SKILL.md Why 절·ADR 배경·이 행의 사유 컬럼). 자기 파일에 대한 grep 한 번으로 반증되는 완료 주장이며, **오늘 일일 점검이 신호 ②로 잡은 "must-fix 절반만 반영"이 한 층 위에서 재발한 것**이다 — 전 지점 스윕으로 교체하고 스킬의 가치 주장을 "없으면 무너진다"에서 **"Phase 4가 착지를 검증한다(보증)"**로 정정했다. **D2** 이 행의 대상 컬럼과 규칙 8 기록에 `harness-install` 동반 개정이 빠져 있었다(항목 수도 3건으로 적고 4건을 열거). **D3(Med)** 생성기에 「가용성」 항목 자체가 없어 F3의 읽기/쓰기 분리가 재생성에서 재현되지 않았다 — 5항에 대해 지적된 결함이 같은 라운드에 손댄 항목에 그대로 있었다. 생성기가 방향별 분리를 출력하도록 하고, 반출 경계는 설치처별 사용자 결정이므로 **묻고 5항 안에 기록**하도록 지시를 넣었다(D8). **D4(Med)** 신설한 인바운드 링크 요구가 레거시 노트 게이트와 충돌 — study `AGENTS.md` §5의 한정("installation or normalization")을 옮기며 떨어뜨린 탓이었고, 실측상 231개 중 214개가 레거시 표식이라 기증자는 대개 레거시 노트다(백링크 추가는 허용, 계약에 맞춰 개조가 금지). **Low** D5 `hot.md`는 본문 지시가 없는데도 갱신됐다(설명이 불완전했지 관측이 틀린 것은 아님), D6 §7 8항의 fail-open을 읽기 방향으로 한정(같은 술어가 한쪽에만 반영되는 실패의 재발 방지 — AGENTS.md·ko 동시), D7 `garden.py` 예산 설명이 「최근 갱신」 링크를 부당하게 배제, D9 `updated` 갱신이 파괴적 편집 게이트에 걸리던 문면. **검증이 확인한 것**: F1 3지점 전부 착지, F10 4개 수치 재측정 일치, 기준선은 리포트가 아니라 샌드박스 산출물에서 재현 확인, 기록된 한계는 오히려 안전한 방향으로 과소 진술(실 vault 214/231이 레거시). **3라운드(E1 Med)**: D5 정정("세 파일이 자기 규칙을 적고 있다" → 실은 둘, `hot.md`는 없다)이 4지점 중 2곳에만 착지했고, 남은 하나는 **이 행의 같은 줄 안**이었다(앞에서 X, 뒤에서 not-X). 행수 기록 낡음(E2), `harness-install` 인터뷰가 "연결 여부만 묻는다"인데 생성 단계에 질문이 추가돼 §13-0 배치 위반(E3 — 질문을 5단계 인터뷰로 이동), ADR 영향에 3라운드 생성기 변경 누락(E4). **4라운드(G1 Med)**: 이 행과 ADR의 **대상 목록이 `git status`와 불일치** — 3라운드에 고친 `AGENTS.md`·`AGENTS.ko.md`가 두 기록 어디에도 없었고 `docs/README.md`도 ADR에서 빠졌다. 수정 후 `git status` 전 경로를 두 기록과 기계 대조했다(잔여는 자기 참조뿐이며 인접 4행의 관례와 일치). **5라운드 PASS**(Low 2건 비차단도 반영: ADR 영향에 changelog 추가, 라운드 귀속 표기). **이 4연속(F1→D1→E1→G1)은 §8 신호 ②로 별도 제안 대상이다** — "여러 지점 수정이 일부에만 착지하고 변경 기록은 완료로 적힌다"는 같은 형태이며, 같은 날 일일 점검이 다른 변경에서 3회 관측한 것과 동일 유형이다. 기계적 검사(새 changelog 행의 대상 컬럼이 `git status` 전 경로를 포함하는가) 하나로 F1·D2·G1이 잡혔을 것이다 |
| 2026-07-28 | `vault-write` Phase 3a 「주제」 조건 명확화 — 트리거를 "새 주제"가 아니라 **허브 없이 노트가 2~3개를 넘은 시점**으로 못 박고, 새 주제의 첫 노트는 「주제」 줄을 받지 않음을 명시(한글 뷰 동반). 본문 258행/500행 | `.agents/skills/vault-write/SKILL.md`, `.agents/skills/README.ko.md` | **이해도 퀴즈 오답**(§13) — 「주제」는 "새 주제일 때만"이라는 문면이 "새 주제면 항상 준다"로 읽혀, 노트 하나짜리 주제에도 허브를 만들어 등록하는 답이 나왔다. index-policy의 2~3개 임계는 뒤쪽 종속절에만 있었다. 오답 자체가 문서 결함의 신호였고, 노트 하나짜리 허브는 index를 다시 노트 수만큼 자라게 해 정책이 막으려던 상태를 그대로 만든다 |
| 2026-07-28 | **`vault-write`에서 노트 계약 재진술 제거 — ADR 034 위반 시정** ① Phase 3a가 `index-policy.md`의 규칙 1·2·3·4를 사실상 재진술(일부는 거의 축자)하고 있던 것을 **의무(매 작성 「최근 갱신」 한 줄)와 순서, 그리고 정책이 다루지 않는 한 가지(「주제」 제한이 「최근 갱신」을 취소하지 않음)**만 남기고 걷어냈다 ② Phase 2의 「스키마 함정 2종」을 규칙 재진술에서 **지목 포인터**로 축소 ③ Phase 4·품질 게이트의 `eight fields` 하드코딩을 "reference에서 센 필드 집합"으로 교체 ④ **독립 검증 F1(High)** — `eight fields`를 지웠는데 **필드 이름 8개를 나열한 Phase 2 첫 줄**이 89행 위에 남아 있었다(지운 것보다 강한 하드코딩이고, 초안 작성 시점에 읽히는 바로 그 줄이다). 스키마 참조로 교체 ⑤ F2·F3 — `garden.py` 예산 해설과 index 품질 게이트가 `index-policy.md`를 재진술하고 있어 포인터로 교체 ⑤' **F4는 이 라운드에 닫히지 않았다** — 증거 등급 항목을 고치면서 Phase 4의 상태 열거(F7b)를 손대고 F4로 기록했는데, 실제 F4 대상인 with/without 표의 「weakest claim」은 그대로였다. 다음 라운드 G2로 검출돼 이제 닫았다. **이 항목 자체가 이 체인의 서명 실패를 한 번 더 저지른 기록이다** ⑤'' **G1(High) — 포인터 전용 설계가 틀릴 수 있는 유일한 방식**: F4를 고치며 "note-level 값은 `evidence-check.md`의 규칙을 따른다"고 적었는데 그 파일에는 per-claim 5등급만 있고 집계 규칙은 `note-schema.md`에 있다. 재진술을 없앤 자리에 **참조 파일 내용에 대한 거짓 단언**이 들어간 것이라, 고치기 전 문장(재진술이지만 참)보다 나빴다 ⑤''' G3·G4·G6 — Phase 4의 `created`/`updated` 규칙 재진술, `sources` 형식 재진술, `invent` 금지의 잘못된 귀속(`evidence-check.md` → 실제로는 study `AGENTS.md` §4)을 정정. **§4 확인 과정에서 F6과 같은 구멍이 하나 더 드러나 Phase 0 독서 범위를 §2~§5로 넓혔다** — `invent` 금지는 어느 reference에도 없고 §4에만 있는데 그때까지 §4가 로드되지 않았다 ⑥ **F6 — `hot.md` 크기·용도의 원본도 Phase 0 독서 목록 밖에 있었다**(어느 reference에도 없고 `hot.md` 본문에도 없다). Phase 0을 study `AGENTS.md` §2·§3으로 확장하고 3c는 그 기준을 가리키게 했다 — 이건 재진술이 아니라 **실제 구멍**이었다 ⑦ G5 — Phase 3c가 §2·§3이 규정하지 않는 "contents"까지 가리키던 것을 크기·용도로 축소 ⑧ H3 — Phase 4의 착지 검증이 `index.md`·`log.md` **둘만** 보고 `hot.md`를 빼먹고 있었는데(with/without 표는 셋 다 검증한다고 적고 있었다) 셋 다 보도록 정정 ⑨ 한글 뷰 동반 갱신(F5·H1). **H1도 같은 서명 실패였다** — 같은 라운드에 그 한글 줄 안에서 `§5`→`§2~§5`는 고치고 `크기·내용`의 "내용"은 안 고쳤다. R17은 제목 집합만 대조해 본문 드리프트를 못 잡는다. 본문 258행/500행 | `.agents/skills/vault-write/SKILL.md`, `.agents/skills/README.ko.md` | 사용자 질문("nohdol-study에 index-policy.md 추가해야 돼?")이 짚어낸 결함. 파일은 이미 있었고 **문제는 그 내용이 루트에 복사돼 있던 것**이다 — ADR 034가 금지한 상태이며("index 정책의 정본은 여기 적지 않는다"), `nohdol-study`가 정책을 고치면 루트가 다른 말을 하게 된다. **직전 커밋 `5b432e5`가 이 사본을 더 키웠다**: 퀴즈 오답의 원인이 원본 결함이 아니라 **내 패러프레이즈**였는데(원본 규칙 2는 2~3개 임계를 처음부터 정확히 적고 있었다), 사본을 고치는 방향으로 대응해 사본이 원본보다 나빠진 사례를 하나 더 만들었다. `eight fields`는 더 직접적인 모순이었다 — 같은 퀴즈 1번의 정답이 "references에 9번째 필드가 생겨도 루트는 할 일이 없다"인데, Phase 4가 8을 박아 두어 그 답을 스스로 반증하고 있었다. ADR·changelog의 "8필드" 서술은 시점 기록이라 그대로 둔다(실행되는 문서와 읽히는 역사의 구분) |
| 2026-07-28 | **metaskill 공통 규칙 11~13 신설 — 기계적 검사 3종** ⑪ **변경 이력 대상 컬럼 = 그 변경이 건드린 전 경로**(명령으로 뽑는다 — 미커밋은 `git status --porcelain`, 커밋됐으면 `git diff --name-only <base>..HEAD`. **둘 다 gitignore된 경로는 안 보여주므로** 손댄 설치 파일 `REGISTRY.md`·`.agents/projects/**`는 손으로 더한다. 가르는 기준은 추적 여부가 아니라 무시 여부다. 면제는 기록이 자기 자신을 빠뜨리는 경우 하나뿐) ⑫ **must-fix 반영은 구성 지점을 먼저 열거하고 개별 확인한 뒤** 델타 라운드 요청(같은 술어는 보통 2~6곳이고 **영어 규칙+한글 뷰 2곳이 가장 흔하다** — 규칙·한글 뷰·그것을 생성하는 지시문·변경 기록·ADR·요약 표. 안 고친 지점은 사유와 함께 밝힌다. 라운드를 더 강제하지 않는다 — 이미 요청할 라운드가 새 것을 검사하게 만들 뿐) ⑬ **오탐 반박은 리뷰어가 읽은 상태로 판정** — 커밋이면 `git show <커밋>:<경로>`, **미커밋 워킹트리면**(하네스 리뷰는 대개 이쪽이다) 리뷰어 리포트의 `file:line` 인용문과 대조한다. 둘 다 없으면 기각이 아니라 미해결. 반박 자체는 정당하다 + **⑪은 ADR이 딸린 변경이면 그 「영향」 목록도 같은 검사 대상으로 포함한다**(`_workspace/vault-write-skill/phase4_reviewer_delta.md`의 G1이 그 목록이 짧은 것을 이미 잡았다) + 블록 말미에 설계 지침 1줄(이 블록에 규칙을 더할 때는 판정이 아니라 명령으로 끝나는 형태를 택한다) | `.agents/skills/metaskill/SKILL.md`, `.agents/skills/README.ko.md`, `.agents/agents/implementer.md` | 사용자 승인(2026-07-28) — 신호 ②가 임계를 크게 초과. `vault-write` 한 스킬의 독립 검증 **두 체인 9라운드 중 7라운드에서** "수정이 일부 지점에만 착지 + 변경 기록은 완료로 적힘"이 나왔고(F1→D1→E1→G1→H1 — `_workspace/vault-write-skill/phase{1,3,4,8}_reviewer_*.md`, 두 번은 **편집하던 바로 그 줄 안에서**, 한 번은 한 지적을 고치고 다른 지적으로 기록), 같은 날 일일 점검이 **다른 변경**에서 3회를 독립 관측했다. auto-memory `term-revision-sweep-all-sites`가 2026-07-26에 이미 이 교훈을 기록했으나 **막지 못했다** — 회상 의존 장치의 한계가 실측된 셈이라 기계적 검사로 승격했다. ⑬의 근거는 별건 2회(2026-07-27, 현재 트리 `grep`으로 오탐이라 반박했으나 리뷰 시점엔 그 행이 없었다). **§7 8항 지식 소스 조회 실시**(의무·발동 조건을 바꾸는 metaskill 변경) — 설계 축을 하나 얻었다: 검증 가능한 것만 자동화된다는 프레임에 따라 세 규칙 모두 **판정이 아니라 실행 가능한 명령으로 끝나게** 설계했다(1차 출처 미추적이므로 인용 없이 판단에만 반영 — 사용 계약 1·2항). **규칙 8 사전 충돌 점검**: ⑪↔ADR 「영향」 목록(범위 확장이므로 규칙 8이 절 추가로 분류한다 — 발동은 ADR이 딸린 변경일 때, 위반은 영향 목록이 건드린 경로보다 짧은 상태. 근거는 `_workspace/vault-write-skill/phase4_reviewer_delta.md` G1), ⑬↔`implementer` 정의(검증 F3 — 초안이 놓쳤다. 「리뷰 수령 규율」 ①이 "코드베이스 현실과 대조"라고만 적어 현재 트리로 읽혔고, 이는 ⑬이 금지하는 바로 그 동작을 의무로 적은 상태였다. ①에 "현실=리뷰어가 읽은 상태"를 박고 ④로 ⑫를 연결했다), ⑪↔루트 §5(이력 행의 *존재* 요구 vs 이 규칙은 *완전성* 요구 — 확장), ⑪↔§6(컬럼 정의 vs 내용 제약 — 양립), ⑪↔자기 참조 관례(파일 전수 측정으로 면제 명문화 — 안 그러면 충족 불가능한 규칙. 수치는 아래 규칙 9 출처), ⑪↔gitignore된 파일(`git status`가 안 보여주므로 손으로 더하라고 명시 — 안 그러면 `REGISTRY.md` 수정이 구조적으로 누락), ⑪↔루트 §5의 "같은 커밋" 요구(검증 H6 — `git diff --name-only <base>..HEAD`는 이미 커밋된 변경을 겨냥하므로 §5와 어긋나 보인다. 실제로는 어긋나지 않는다: §5는 이력 갱신을 같은 커밋에 넣으라는 것이고, 이 명령은 **여러 커밋으로 나뉜 한 변경**의 후속 커밋이 자기 몫의 경로를 빠짐없이 적기 위한 것이다. 이번 `vault-write`가 3커밋(`af6c508`·`5b432e5`·`d6488cd`)으로 나뉜 사례이며, 셋 다 changelog를 함께 건드려 §5를 지켰다), ⑫↔규칙 8(초안 전 정합성 vs 리뷰 후 반영 — 발동 시점 다름), ⑫↔개선 모드 비례성(검증 여부 vs 다음 라운드 요청 전 행동), ⑫↔§13-2(서브에이전트 주장 불신 vs **자기 완료 주장** 불신 — 반대 방향 보완), ⑬↔루트 §5(같은 실패 계열이나 §5가 열거한 표면 5종에 리뷰 루프가 없음 — 발동 지점 다름), ⑬↔반박 금지 오독(리뷰어 오류 실례가 이 세션에도 있어 정당한 반박을 보존하도록 명시). **규칙 9 출처**: 세 주제의 기존 커버리지를 `rg`로 확인했으나 **`implementer` 정의를 빠뜨려 0건 주장이 거짓이었다**(검증 F3 — must-fix를 받는 당사자 에이전트인데 스윕 대상에서 빠졌다). 재스윕 결과 「코드베이스 현실」 술어는 **3지점**이며(`implementer.md:31`·`docs/adr/022:14`·`docs/proposals/2026-07-17-superpowers-adoption.md:41`), **실행되는 `implementer.md`만 고치고 ADR·proposal은 시점 기록이라 그대로 둔다**(이 파일 앞 행의 같은 판단과 동일 근거). 자기 참조 관례는 **파일 전수 측정**이다 — 이력 행 183개(HEAD 182) 중 대상 컬럼에 자기 파일을 넣은 행은 `:50`·`:98` **2개뿐이고 둘 다 그 파일 자체가 주제였던 행**이다(1라운드가 제시한 171은 재측정 없이 옮겨 적은 값이라 폐기 — 규칙 9가 금지하는 바로 그 전재였다). ⑬의 2회 실사례와 일일 점검 3건의 출처는 `_workspace/harness-ops-log.md:92`·`:93`·`:95`. `git show worktree:<경로>`가 `fatal: invalid object name 'worktree'`로 죽는 것은 직접 실행 확인(그래서 ⑬이 두 경우로 갈린다). **초안이 두 곳에서 "미추적 파일은 `git status`에 안 잡힌다"고 적은 것은 거짓이었다**(검증 H2) — 별도 저장소 실측: 미추적 파일은 `?? untracked.txt`로 **나오고** 숨는 것은 gitignore된 것뿐이다. 규칙 본문은 이미 "가르는 기준은 추적 여부가 아니라 무시 여부"로 고쳤는데 기록 2지점이 안 따라온 것이며, 대상 경로가 실제로 무시되는지는 `git check-ignore -v`로 확인한다. **규칙 7**: 기준선 단계는 **이미 관측된 실패 10건**(이 체인 7 + 일일 점검 3)으로 충족한다 — 규칙 7의 취지가 "상상한 위반이 아니라 실제 위반을 막아라"이기 때문이다. **다만 초안은 여기서 멈춰 「준수 확인」 단계를 조용히 빠뜨렸고, 그것이 F1을 통과시켰다** — 규칙 13을 한 번 따라 해 봤다면 `git show worktree:` 가 없다는 것이 즉시 드러났다(검증 지적, `SKILL.md:18`이 바로 그 알리바이를 금지한다). 세 규칙 모두 이 커밋 안에서 1회씩 실행해 확인했다: ⑪은 이 행의 대상 컬럼을 `git status`와 대조 — **1회차에 자기 위반을 잡았다**(`implementer.md`가 대상에서 빠졌고, 추적해 보니 스크립트 치환이 같은 문자열을 가진 **2026-07-26 이력 행에 착지해 과거 기록을 조용히 고쳐 쓴** 상태였다. 누락보다 나쁜 결과이며 사람 눈으로는 안 잡힌다. 원복 후 재적용해 통과), ⑫는 이번 지적 반영 시 항목별 구성 지점을 먼저 열거해 적용, ⑬은 F1·F2·F3를 리뷰어 리포트의 `file:line` 인용문으로 대조(전건 성립, 반박 0건) |

## CLAUDE.md

| 날짜 | 변경 내용 | 대상 | 사유 |
|---|---|---|---|
| 2026-07-12 | 초기 구성 | CLAUDE.md | 루트 하네스 구축, AGENTS.md 포인터 수립 |
| 2026-07-13 | 트리거 절에 team-review·doc-writer·harness-install 라우팅 추가 | CLAUDE.md | "리뷰해줘"가 내장 스킬·인라인 리뷰로 새는 실패 관찰 — 항상 로드되는 포인터에 라우팅 명시 |
| 2026-07-13 | 트리거 절에 work-tracker 라우팅 추가 | CLAUDE.md | 세션 영속 작업 추적 신설(14절, ADR 009) — 재개 요청이 라우팅되도록 포인터에 명시 |
| 2026-07-13 | orchestrate 트리거를 구현·다단계 작업 전반으로 확대 + 스킬 우선순위 절 신설(특화>게이트, 하네스>내장·플러그인·sc:*) | CLAUDE.md | orchestrate 범용 게이트화(ADR 010) — 판정 대상 요청이 스킬에 도달해야 하고, 트리거 확대로 생긴 내부 이중 매칭·외부 스킬 충돌(reviewer 검증 F3·F8~F10)은 항상 로드되는 포인터가 우선순위로 해소 |
| 2026-07-13 | 우선순위 매핑의 배포 항목을 release 스킬로 갱신 | CLAUDE.md | release 스킬 신설 — 배포 요청이 infra-specialist 단독 경유가 아니라 워크플로우(런북→확인→검증) 전체로 라우팅되어야 함 |
| 2026-07-16 | 트리거 절에 continuation 게이트 재진입 + k8s 매니페스트 infra-specialist 경유·리소스 상한 선확인 명시 | CLAUDE.md | 구현·인프라 continuation('진행해줘')이 orchestrate 게이트를 우회해 네임스페이스 리소스 상한 미확인 실패 발생(우회 신호③) — 항상 로드되는 포인터에 continuation 재진입을 명시(루트 7절·infra-specialist 강화와 동조) |
| 2026-07-16 | `@AGENTS.md`를 실제 1행으로 이동("첫 줄" 서술과 일치) + 라우팅 맵에 project-status·defuddle 추가 | CLAUDE.md | 하네스 전체 감사 — STRUCT-F2(문서 서술 vs 실제 3행 드리프트), ASSET-F02(두 스킬이 항상-온 맵 밖이라 스킬 우선순위 보호를 못 받음) |
| 2026-07-18 | 라우팅 맵에 tool-audit 추가(주기 점검→harness-review, 제거 실행→metaskill 경계 병기) | CLAUDE.md | tool-audit 스킬 신설(신호 ①) — 항상-온 맵 밖 스킬은 우선순위 보호를 못 받음(ASSET-F02 동일 근거) |
| 2026-07-18 | 라우팅 맵에 context7 추가(라이브러리·프레임워크·SDK 사용법·문서 조회; Anthropic/Claude API→claude-api(내장), 미설치·특정 URL→WebFetch/defuddle 경계 병기) | CLAUDE.md | context7 활용 스킬 신설 — 항상-온 맵 밖 스킬은 우선순위 보호를 못 받음(ASSET-F02 동일 근거) |
| 2026-07-19 | `autoloop` 스킬+드라이버 신설 — 세션 외부 Python 드라이버가 headless `claude -p` 반복 기동(새 프로세스=자동 clear), carryover 노트 핸드오프, 게이트 3종(안전: acceptEdits+allow/disallow·bypass 금지 / 검증: 드라이버 실측 테스트+reviewer PASS / 정지: 7종 조건 유한 종료). CLAUDE.md 앵커에 라우팅 추가, 스펙·ADR 025·회귀 테스트 25항목 동반(reviewer 독립 검증 BLOCK→재작업→PASS) | .agents/skills/autoloop/, CLAUDE.md, docs/specs/2026-07-19-autoloop-driver.md, docs/adr/025, docs/README.md, README.md | 사용자 요청(자율 루프: 프롬프트 자동 개선·질의 제거·자동 wrapup→clear→재개) — /clear는 CLI 명령이라 세션 안에서 불가, 세션 외부 드라이버로 구조 해결. 안전 범위(읽기·편집만 무인)는 사용자 확정 2026-07-19 |
| 2026-07-21 | 라우팅 앵커에 독립 트랙 동시 처리 시 orchestrate 실제 재호출(동시 발행 규약) 문구 추가 | CLAUDE.md | 사내 대기 큐(2026-07-21 실측) — continuation 재게이트 규칙이 있는데도 스킬 호출을 생략하고 판정만 자체 서술해 병렬·run_in_background 발행 규약을 놓침(인프라 서브에이전트 포그라운드 대기) |
| 2026-07-21 | 서브에이전트 발행 기본값 run_in_background 앵커 추가(독립 트랙 문장 보강 — 병행 작업 유무와 무관, 결과 즉시 필요할 때만 포그라운드) | CLAUDE.md | 사내 대기 큐(2026-07-21 실측, 같은 세션 3회 반복: infra-specialist 1건+reviewer 2건) — 기존 앵커의 '독립 트랙 동시 처리 시' 조건이 좁아 '병행 작업 없음'을 이유로 우회됨. 기준을 병행 유무가 아니라 결과 즉시 필요 여부로 일반화 |
| 2026-07-21 | 앵커에 구현 주체 규칙 추가 — 하위 프로젝트 구현 기본 주체=implementer(3+파일·중위험), 직접 구현은 ≤2파일·저위험+독립 reviewer만, 재개(work-tracker·carryover) 구현도 게이트 재진입 | CLAUDE.md | 사용자 관찰(2026-07-21) — 규칙이 스킬 본문에만 있으면 재개 직후 직행을 못 막음(background 기본값과 같은 salience 처방) |
| 2026-07-21 | 앵커 구현 주체 문구의 "중위험" 제거 — 3+파일 기준으로 정정(위험도 높음은 팀 판정) | CLAUDE.md | 경계 정합 검토 — 미정의 등급이 항상-온 앵커에 남으면 매 세션 오판정 씨앗 |
| 2026-07-22 | 앵커 다이어트 — 라우팅 불릿의 실사례 서사를 포인터(ADR 028·changelog 07-21)로 압축, 규칙·트리거 문구는 전량 유지(7,024→약 6.3KB, 불릿 4,139→약 3.4KB) | CLAUDE.md | 사용자 승인(입력 토큰 절감 실측) — 항상-온 이중 적재에서 사례 서사는 changelog·ADR과 순중복이고, 최대 건(진단→구현 전환)은 gate-reminder 훅 승격으로 앵커 서사의 역할이 끝남. ADR 021 재비대 방지 규율 회복 |
| 2026-07-22 | continuation 앵커에 진단→구현 전환점 문구 추가 — 읽기 전용 진단(로그·kubectl·원인 조사)이 코드 수정으로 넘어가는 전환점도 재판정 대상, 명시적 '구현해줘' 없이도 제품 코드 첫 Edit/Write가 게이트 트리거(gate-reminder 훅 병기, ADR 028) | CLAUDE.md | 사내 대기 큐(신호 ②) — 기존 continuation 문구가 사용자 발화 트리거("진행해줘" 등)에 초점이라, 트리거 발화 없는 diagnosis→fix 슬라이드에 무언으로 적용되지 않음(자가 정정도 같은 조건에 갇혀 재발) |
| 2026-07-24 | 라우팅 앵커의 발행 규칙 개정 — 백그라운드 기본을 결과 의존 작업까지 확장(턴 종료 후 완료 알림 재개), 포그라운드는 1분 이내 인라인 조회 한정 | CLAUDE.md | 사용자 요청(2026-07-24) — 상세는 AGENTS.md 표 같은 날짜 행. 07-21 앵커의 포그라운드 예외를 대체 |
| 2026-07-25 | clarify-first 앵커를 인터뷰 우선으로 교체 — 전 작업 확대·불확실 시 무조건 질문·작업 중 질문 금지·PR 전 이해도 퀴즈 요약 | CLAUDE.md | 사용자 요청(2026-07-25) — 상세는 AGENTS.md 표 같은 날짜 행 |
| 2026-07-25 | 퀴즈 앵커 갱신 — 1차 실행 지점 리뷰 창(백그라운드 reviewer와 병렬) + PR 직전 확인·델타 재퀴즈로 요약 교체 | CLAUDE.md | 사용자 제안(2026-07-25) — 상세는 AGENTS.md 표 같은 날짜 행 |
| 2026-07-25 | 스킬맵에 Understand-Anything 라우팅 추가 — 직접 호출 전용(`/understand…`만, 자동 라우팅 금지), 그래프는 사람 대면 내비게이션 한정(하네스 진실 소스 금지) | CLAUDE.md | 사용자 결정(2026-07-25) — 상세는 AGENTS.md 표 같은 날짜 행·proposal |
| 2026-07-25 | 스킬맵에 claude-video `/watch` 라우팅 추가 — 직접 호출 전용(자동 라우팅 금지), Whisper 오디오 제3자 전송이라 사내·민감 녹화는 `--no-whisper`/자막 전용(§3) | CLAUDE.md | 사용자 결정(2026-07-25) — 상세는 AGENTS.md 표 같은 날짜 행·proposal |
| 2026-07-25 | 앵커 신설 — "사용자 이해=완료 요건"(§13 최상위 원칙): 이해 못 한 작업은 완료 아님, 퀴즈는 하드 전달 게이트(실패·생략 시 PR 차단), 결정은 가르치고 읽기 가이드 동반 | CLAUDE.md | 사용자 정의(2026-07-25) — 상세는 AGENTS.md 표 같은 날짜 행 |
| 2026-07-25 | Hermes Agent(Nous) 검토 — 기각 기록(제안 문서·MOC) | docs/proposals/2026-07-25-hermes-agent-review.md, docs/README.md | 사용자 검토 요청("hermes 쓰는 편이 낫지 않아?") 후 기각 승인 — 범주 불일치(채택 아니라 Claude Code+Codex 이탈), 간판 기능(자율 스킬 생성·대화 검색 DB)이 ADR 007·018과 정면 충돌, 니치 기충족(orchestrate·autoloop·agentsview·auto-memory·rtk), Nous Portal 클라우드 경유 §3 유출. agentskills.io 이식형 SKILL.md 표준만 관찰 보류. 기각도 기록해 재검토 반복 방지(제안=과정, §6) |
| 2026-07-26 | Clew(Custos) 검토 — 기각 기록(제안 문서·MOC) + ADR 032 사유에 증폭 비용 근거 1문단 추가(규칙 무변경). "재독 금지의 비용은 1회분이 아니라 `낭비 토큰 × 이후 턴 수` 증폭분이며 같은 중복도 세션 초반일수록 비싸다 → 델타 패킷은 착수 시점부터". 수치는 구조적 상한 추정이지 측정값이 아님을 명시 | docs/proposals/2026-07-26-clew-review.md, docs/README.md, docs/adr/032-token-efficient-orchestration.md | 사용자 검토 요청("이 하네스에 적용할 만한 점이 있어?") → 실측 우선 결정 → 결과 확인 후 기각 승인. 실측(57세션·3,759 툴 스팬, append-only라 시점 스냅샷): 하네스 세션 낭비 0건, 전체 0.05%(2건)이고 그 2건도 도구 리포트가 정당한 재실행 가능성 명시, 절감 상한 $0.50. 배포 결함 발견 — `[detect]` 경량 설치가 낭비 검출 시 `ImportError` 크래시(exit 1)라 실질 757MB 강제, 조용한 실패로 배치 스캔이 "전부 0"을 냄. 규율 4종(사전 등록·공개 철회·"구현됨≠관측됨"·동결 파라미터)은 §13-1/13-2/13-3·§6로 기수렴. 증폭 비용의 계산 구조만 채택. reviewer 독립 검증 REWORK 3건(F1 chain 루트를 툴 스팬에 합산, F2 상한 추정을 실측으로 서술하며 위양성 스팬을 근거로 인용, F3 재검토 조건 OR/AND 모순) 반영 |
| 2026-07-26 | Claude 5 세대 컨텍스트 엔지니어링 규칙 대조 — 검토 문서·MOC 기록만(규칙 무변경, 적용은 승인 후 별건) | docs/proposals/2026-07-26-context-engineering-rules-review.md, docs/README.md | 이슈 #41 우선순위 A-1. §7 8항대로 GeekNews 큐레이션을 근거로 쓰지 않고 1차 출처(claude.com/blog/the-new-rules-of-context-engineering…)를 따라감. 실측: 상시 로드 32,637B(AGENTS.md 22,370 + CLAUDE.md 4,031 + 스킬 description 6,236) : 지연 로드 197,590B = 1:6.1. 원문 8축 중 6축 기수렴. **"80% 감축"은 제품 시스템 프롬프트의 행동 미시 제어 규칙(주석 금지 등 원문 인용) 대상이라 라우팅·가드레일·규약에 옮기면 범주 오적용 — 목표 수치로 기각.** AGENTS.md의 스킬 분산도 §11·§12(스킬은 세션 cwd 상대 로드 → Codex 가드레일 소실)로 기각. 남은 여지는 CLAUDE.md 순수 중복 앵커 3개(Skill priority·Interview-first·User comprehension, 1,039B=상시의 3.2%)뿐이며, ADR 021 앵커 근거 2개 중 원문이 무효화하는 것은 "주목도 희석 방어"(구형 모델 position bias) 일반론이고 "관측된 실패 방어"(2026-07-16 §15 영어 누수)는 무효화되지 않아 Output language 앵커는 유지. 부수 발견: Skill priority 앵커가 미설치 도구(SuperClaude 2026-07-18 제거·gstack 설치 흔적 0)를 상시 지목 중 |
| 2026-07-26 | Skill priority 앵커의 미설치 도구 고유명 제거 — "built-in, plugin, SuperClaude, and gstack skills" → "built-in, plugin, and any other external skill or command pack" | CLAUDE.md | 이슈 #41 A-1 검토의 Layer 1, 사용자 승인. 실측 재확인(2026-07-26): `~/.claude/skills/`에 gstack 스위트 전부 부재(설치분 34종, 2026-07-17 제거)·`~/.gstack/` 부재·`installed_plugins.json`은 document-skills·codex 2건뿐, SuperClaude도 2026-07-17 제거(운영 로그 같은 날짜 2행). 즉 항상-온 앵커가 이 머신에 없는 도구 2종을 매 세션 지목하고 있었다. reviewer watch 반영으로 "or command pack"을 덧붙였다 — SuperClaude는 스킬 팩이 아니라 프레임워크+`/sc:*` 명령 17개였으므로 "external skill"만으로는 같은 형태의 재설치를 엄밀히 포섭하지 못한다. 남긴 일반 범주("built-in, plugin, and any other external skill")가 재설치·타 머신 사례를 포함하므로 규칙 적용 범위는 불변 — 발화 조건(같은 트리거에 하네스 스킬과 비-하네스 스킬이 함께 걸릴 때 발화, 하네스 스킬 라우팅이 충족·외부 스킬 라우팅이 위반)도 그대로다. **2026-07-17 gstack 제거 시 "스킬 description의 gstack 이름 지목 배제 문구는 재설치·타 머신 방어로 유지" 결정과 충돌하지 않게 범위를 항상-온 파일로 한정** — harness-review ③ 신호 예시·release·tool-audit의 gstack 언급은 지연 로드라 비용이 0에 가깝고 그 결정이 보존 대상으로 지목한 자리이므로 손대지 않았다. AGENTS.md §7 7항 원문에는 도구 고유명이 없어 동조 불필요, `AGENTS.ko.md`도 무영향 |
| 2026-07-26 | 하네스 설계 자료 5건 대조 — 검토 문서·MOC 기록만(규칙 무변경, 후보 1건은 관측 후 규칙화로 보류 — 843eac4에서 결정, 같은 커밋에 이력 갱신이 빠져 2026-07-26 주간 점검 F10으로 사후 정정) | docs/proposals/2026-07-26-harness-design-sources-review.md, docs/README.md | 이슈 #41 우선순위 A-2~A-6. §7 8항대로 큐레이션을 근거로 쓰지 않고 항목마다 1차 출처를 따라감. **5건 중 3건에서 큐레이션 전제가 틀렸음이 드러남** — A-3은 "GAN 기반"이 아니라 영감받음(적대적 학습·그래디언트·판별자 전무), A-4는 논문 LaTeX 전문에 `harness` 0회라 "에이전트가 스스로 하네스를 설계"가 블로거 자기 프레임(큐레이션이 지목한 원문 자체가 2차 논평이고 진짜 1차는 arXiv:2603.19461), A-5는 "두뇌와 손"이 모델 대 모델이 아니라 하네스 대 샌드박스 분리라 §9 근거로 인용 불가. A-2는 §11 Codex 축 미접촉(`hook`·`Claude`·`multi-CLI`·`sandbox` 0회)이고 핵심 관행(에이전트 자기 PR 머지·게이트 최소화·flaky 재실행)이 §5·§13-2·§13-4와 정면 충돌하며 원문 스스로 고처리량 한정이라 기각. A-6은 실축이 §9가 아니라 §7이고 orchestrate와 기수렴하며, 원문의 런타임 모델 선택은 정의 시점 대 실행 시점 층위 차이라 §9와 충돌 아님. **유일한 규칙 변경 후보: A-3(2026-03-24)과 A-5(2026-04-08)가 서로 인용 없이 독립 도달한 "모델 결함 우회 장치는 모델이 좋아지면 순비용이 된다" — §8 신호 ④는 사용 횟수·토큰 기준이라 여전히 호출되는 장치는 영원히 안 걸린다. §8의 "신호 집합 고정"을 지켜 신호 신설이 아니라 harness-review 주간 ④ 안에 모델 메이저 버전 변경 발화 조건으로 편입 제안(미적용, 승인 대기).** 관찰 4건에는 각각 재검토 조건 부여 |
| 2026-07-26 | 도구 후보 4건 tool-eval — 전건 미채택 기록(제안 문서·MOC) | docs/proposals/2026-07-26-harness-tool-candidates-review.md, docs/README.md | 이슈 #41 우선순위 B. 사용자 결정: 전부 기록만, 개인 설치도 안 함. 어떤 도구도 설치·실행하지 않고 저장소 소스 판독으로 판정. **B-1 VHK가 새 기각 축을 만듦 — 구조 침해**: `src/commands/sync.ts:512`의 `SYNC_TARGETS`에 `AGENTS.md`가 순수 미러로 들어 있어 `vhk sync` 1회로 단일 진실 소스가 RULES.md 파생물로 강등되고(헤더가 "직접 수정 금지"를 박음), `.agents/rules/`에 쓰며(§11 공용 원본 예약 영역), `vhk init`이 `core.hooksPath`를 점유해 tdd-gate·secret-gate가 조용히 죽는다. 셋 다 README가 아니라 소스에서 확인됐고 큐레이션은 뒤 둘을 언급하지 않음. B-2 DeerFlow·B-4 OpenHarness는 Hermes 선례와 같은 범주 불일치(자체 Gateway·nginx·DB·Docker를 요구하는 독립 LangGraph 런타임 / `pyproject.toml`이 스스로 "Python port of Claude Code"라 명시하고 `~/.claude/.credentials.json`·`~/.codex/auth.json`을 읽어 세션을 Feishu·Slack 등으로 중계). B-3 Impeccable은 프론트엔드 전용이 **추정이 아니라 원문으로 확인**(SKILL.md "Not for backend-only or non-UI tasks", 플레이북 34개 전부 UI, 탐지 규칙 60개가 HTML/CSS/DOM 대상)이고 레지스트리 등록 프로젝트 0건이라 대상 부재로 연기 — 재평가 시 `impeccable.style/api` 익명 핑·`settings.local.json` 훅 매니페스트 설치를 §3으로 함께 볼 것. 일반화 후보(사례 1건뿐이라 미규칙화): AGENTS.md·CLAUDE.md·`.agents/`·`core.hooksPath` 중 하나라도 쓰기 대상인 외부 도구는 기능 평가 이전에 구조 침해로 기각 |
| 2026-07-26 | autoloop `start`에 종료 신호 등록 신설(4단계) — 기동 시 `driver.pid` 기록 후 백그라운드 감시(`run_in_background`+`until`, Monitor 아님 — 단발 알림에 무한 명령은 이벤트 후에도 무장 유지)를 걸어 기동 세션이 루프 종료를 통보받는다. 감시 대상은 **`launch.log`의 `[autoloop] 종료:` 줄**(7개 정지 사유 전부 발동) + pid 소멸(크래시 대비). 드라이버는 계속 `nohup` detach — 감시는 소모품, 루프는 아님. 스펙 R8·R11 갱신, 드라이버 코드 무변경(계약 테스트 green — 이 시점 37건, 이후 동시 진행된 `ffe9da4`가 1건 추가해 현재 38건) | .agents/skills/autoloop/SKILL.md, docs/specs/2026-07-19-autoloop-driver.md | 실사례(agent-eval-gate 런) — 기동 후 launch.log 1회 확인이 끝이라 루프가 끝나도 통보 경로가 없어 사용자가 물어야만 완료를 알 수 있었음(신호 ②). reviewer 독립 검증 BLOCK 반영: F1 `driver.log`는 append 전용이라 R10 재개 시 이전 EXIT로 감시가 자기충족(구/신 조건 실측 대조로 확인) → 매 기동 truncate되는 `launch.log`로 전환, F2 pid 부재·cwd 오류가 조용히 "완료"로 흐르던 것 → 절대 cd+빈 pid 가드로 실패를 명시 통보, F3 감시의 세션 수명 한계 명시, F6 스펙 R8에 산출물 기재, F7 `kill -0`이 무신호 존재 확인임을 명시(stop 동사 no-kill과 무충돌). F5(백그라운드 실행 시간 상한 — Bash 계약의 120s 기본/600s 최대가 백그라운드에도 걸리면 10분 초과 런에서 장치가 무용)는 700초 프로브 실측으로 해소: 700s 생존 후 정상 종료(exit 0)·알림 도착 확인, 즉 상한 미적용. 이 축이 장치 성립 여부를 가르므로 문서 문구가 아니라 실측으로 확정 |
| 2026-07-26 | **autoloop 안전 게이트 실효화** — Claude 헤드리스 호출에 `--setting-sources project` 추가(스펙 R3-1 신설·R14 표 반영·완료 기준 C19), **Codex 경로 미해소 갭을 R3-2로 명문화**(C20·ADR 025 정정 주석 동반), driver.py 목록 주석에 전제 명시, driver_test.py 회귀 1건 추가, SKILL.md 기동 전 점검·게이트 서술에 상속 안 함 명시 | `.agents/skills/autoloop/scripts/driver.py`, `.agents/skills/autoloop/scripts/driver_test.py`, `.agents/skills/autoloop/SKILL.md`, `docs/specs/2026-07-19-autoloop-driver.md`, `docs/adr/025-autoloop-driver.md` | `/autoloop status` 검토에서 잡은 튜닝 신호(권한 반복 거부 3세션) 추적 결과 **게이트가 양방향으로 무효**임을 실측 확인 — 헤드리스 프로브에서 SAFE_ALLOW의 `ls`·`git status`·`git add`가 DENIED, 목록에 없는 `python3 -c`가 OK로 역전. 원인 2개를 기계 확인: ① 사용자 전역 `permissions.allow`가 병합돼 R3이 금지한 bare 인터프리터 그랜트 부활(무인 세션의 유일한 탈출구가 임의 코드 실행이 됨 — agent-eval-gate 루프가 3세션 연속 `python3 -c "import subprocess"`로 커밋한 실사례) ② `PreToolUse` Bash 훅의 명령 재작성으로 허용·차단 양쪽 다수 패턴이 사정권을 벗어남(`git push --force`·`git push -f` 포함) — 블랙리스트가 방어한 게 아니라 재작성형이 허용 목록에도 없어 우연히 fail-closed였던 상태. 훅은 재작성만이 아니라 `permissionDecision: allow`로 **허용 부여**도 하므로(러너 2종 실측) 목록보다 상위의 판정 권한이 얹혀 있었다. **규칙 9 출처**: 훅에 PreToolUse payload를 직접 투입해 전수 확인 — **기준은 패턴 내부 문자열 그대로(인자 없음)**, SAFE_ALLOW 17/22·DESTRUCTIVE 6/21 이탈이며 후자는 `git push --force`·`git push -f`·`helm`·`gcloud`·`prisma migrate`·`psql`(인자를 붙이면 개수가 달라져 기준을 명시. 독립 reviewer가 같은 기준으로 동일 수치 재현). 전역 설정은 `permissions` 키 직접 파싱, `--setting-sources` 동작은 CLI `--help` 확인 후 4개 변형(user 포함/`project`/빈 값) 실행 비교 — 빈 값은 하네스 항상-온이 로드되지 않아(§12) `project` 단독으로 확정. 수정 후 프로브 9/9 기대치 일치(허용 3·차단 3·검증세션 3), driver_test 38건 통과. **독립 reviewer BLOCK 반영** — must-fix F1: 완료 기준 C19가 "Codex는 sandbox가 게이트라 해당 없음"이라 단정했으나 Codex도 같은 상속 문제를 가짐(`build_codex_args`가 `-c`/`--profile` 미전달, 설치처 `~/.codex/config.toml`이 `[sandbox_workspace_write] network_access = true` — 직접 확인). `--sandbox`가 `sandbox_mode`는 덮지만 이 테이블은 안 덮으므로 "네트워크 기본 차단으로 원격 파괴 봉쇄" 전제가 이 설치처에서 거짓 → **R3-2(미해소 갭)로 명문화**하고 스펙 비목표 절·SKILL.md 2곳의 단정을 조건부로 정정, 코드 고정(`-c sandbox_workspace_write.network_access=false`)은 런타임 미측정·키 경로 어긋나면 조용히 무효라는 이유로 **미적용**하고 사용 가드레일(Codex 선택 전 설정 확인)로 대체. should-fix F2 표본 기준 병기(위), F3 사용자·플러그인 스킬 동반 손실을 R3-1 부수 효과로 명시(실측: `orca-cli`·`watch` 미가용, `orchestrate` 유지 — 루트 §11상 무인 세션에서 외부 도구 스킬이 전부 불가). F6 잔여 지적을 불변식으로 흡수(project 소스에도 Bash 재작성·허용 부여 훅 금지). **미검증 범위(§13-2 ④)**: Codex 런타임 격리는 설정·인자 수준까지만 확인, 실행 재현 없음 |
| 2026-07-26 | autoloop 종료 감시 2라운드 수정 — 스펙 R11의 감시 대상을 `launch.log` 종료 줄로 정정(N1, R8과 불일치 해소)·스펙 이력 행 보완, pid 가드를 `case` 숫자 검사 + **무장 시점 생존 확인**으로 강화(N2), `감시 실패` 알림은 루프 결과가 아니라 감시 실패로 보고하도록 규정(N4), 판정은 `launch.log` 종료 줄에서 읽고 `driver.log` tail은 맥락으로만(N3), 이전 행의 테스트 수 표기 정정(N5) | .agents/skills/autoloop/SKILL.md, docs/specs/2026-07-19-autoloop-driver.md | reviewer 2라운드 BLOCK. N1이 핵심 — R8만 고치고 R11에 옛 지시(`EXIT` 줄)가 남아 한 스펙이 서로 다른 파일을 가리켰고, 이 스펙 자체가 autoloop 대상이라 결함 요구사항이 다음 루프의 오라클이 된다. N2는 1라운드 F1과 같은 형태가 다른 문에서 재발한 것 — `driver.pid`는 아무도 지우지 않아 낡은 죽은 pid가 남고, 그러면 `!kill -0`이 즉시 참이 되어 감시가 0초에 "완료"를 보고한다. 무장 전 죽은 pid는 종료가 아니라 설정 실패라는 구분을 넣어 해소(6개 경우 실측: 파일없음·빈값·공백·비숫자·죽은pid 전부 실패 통보, 살아있는 pid만 무장) |
| 2026-07-26 | autoloop 종료 감시 3라운드 — 무장 시점 검사를 "종료 줄 부재 **and** pid 부재"로 정정(N6), `감시 실패` 후 재무장 규정(N7 잔여). 스펙 R11 동반 수정 | .agents/skills/autoloop/SKILL.md, docs/specs/2026-07-19-autoloop-driver.md | reviewer 3라운드 PASS + should-fix 1건. 2라운드에서 낡은 pid를 막으려 넣은 무장 시점 생존 확인이 순서를 잃어, **무장 전에 정상 종료한 런**(R7⑦ 프로세스 에러는 1초 미만)을 "낡은 driver.pid"로 오보했다. 실측 3경우로 확인: 빠른 종료+죽은 pid→완료 보고, 종료 줄 없음+죽은 pid→감시 실패, 종료 줄 없음+살아있는 pid→대기 유지. 같은 라운드에 reviewer가 2라운드 N5(테스트 수)를 자기 오류로 철회 — `git log -1`의 최신 커밋이 내 작업보다 앞선다고 날짜 확인 없이 가정한 것으로, 규칙 9가 겨냥하는 오류 유형 |
| 2026-07-26 | 반입 경계 4라운드 검증 BLOCK 수정 — 반입 시나리오에 **gist 단방향(개인 → 사내) 규칙** 명시(4·5단계는 로컬 편집, 반영은 `harness-updates.md` 대기 큐)·완료 체크리스트 항목에 방향 조건 추가, 수신 단계에 gist id 획득 경로와 `mkdir -p` 보강, `set-url --push DISABLED` 절에 `branch.<name>.remote` 재지정 처방 추가, scaffold 「작업 위치」 생성기를 3라운드 인스턴스 사양(프로필 오버라이드·예외 행·"보고 후 지시"·양쪽 아님 처리)으로 동기화 | .agents/skills/metaskill/SKILL.md, .agents/skills/metaskill/references/scaffold.md | 갱신 의무에 방향이 없어 사내 세션이 사내 내용을 개인 계정 gist로 내보내는 경로가 규칙 형태로 열려 있었다(R4-1 High, §3). 수신 경로는 받는 기계에 없는 파일을 gist id 출처로 지목해 순환이었고(R4-2), `git remote rename origin upstream`이 `branch.remote`까지 옮겨 맨 `git push`가 exit 128로 막히는데 문서는 "origin으로 가므로 맞다"고 단정했다(R4-5, 재현 확인). 생성기는 3라운드 수정을 못 받아 다음 프로젝트가 같은 결함을 물려받을 상태였다(R4-3, 2라운드 D1 재발) |
| 2026-07-26 | 반입 경계 5라운드 검증 BLOCK 수정 — 반입 4단계 편집은 **변경 이력 행 대신 대기 큐 항목**으로 기록(이력 표는 개인 사본의 것), 5단계에서 미러 대상 파일의 이력 행 제외, 완료 체크리스트 사내 분기를 3조건 동시 충족으로 강화, 2단계의 "자기 설치처 이송 표에 기록" 지시 철회(그 절은 수출 측 질문으로만 생성돼 반입 측엔 없음 — id는 받은 파일 안에 있으므로 최초 1회 문제), scaffold 미러 문단에 이력 표 규칙 동반 반영, ADR 006 완화책 ①에 방향 명시 | .agents/skills/metaskill/SKILL.md, .agents/skills/metaskill/references/scaffold.md, docs/adr/006-subproject-harness-central-management.md | 4라운드 수정이 만든 새 충돌 — 변경 이력 표가 신선도 신호이자 사내가 쓰는 대상이라 두 의무가 서로를 무효화했고(R5-2), 체크리스트는 큐 경유를 조건이 아닌 부수 설명으로 적어 큐 없이도 완료 처리됐다(R5-3). 반입 측 REGISTRY에 없는 표를 지목했고(R5-1), ADR 006은 R4-1이 유출로 지목한 방향 없는 문구를 그대로 두고 있었다(R5-4) |
| 2026-07-26 | **secret gist 미러 철회** — 반입 시나리오 2단계를 손 이송으로 환원(자동 채널을 두지 않는 것이 결정임을 명시), 5단계·완료 체크리스트에서 gist 조건 제거하고 "반대쪽 사본이 뒤처졌음을 보고에 명시"로 대체, scaffold 생성기를 손 이송 + 미러 재도입 금지로 개정, ADR 006 gist 예외 행을 철회 표기(근거 보존), harness-install 반출 발동 지점 생성기에 대기 큐 쓰기 포함 | .agents/skills/metaskill/SKILL.md, .agents/skills/metaskill/references/scaffold.md, .agents/skills/harness-install/SKILL.md, docs/adr/006-subproject-harness-central-management.md | 사용자 결정(기각). gist는 개인 계정에 있어 사내 push가 곧 반출인데 git 경로의 `set-url --push … DISABLED` 같은 기계적 차단이 없었다. 규칙만으로 단방향을 세우려는 시도가 연쇄 충돌을 냈고(방향 규칙 ↔ 신선도 신호인 이력 행, 그 면제 ↔ "모든 변경 기록" 의무), 4·5·6라운드 독립 검증이 매번 직전 라운드 수정의 후속 결함을 잡아냈다. 손 이송은 사람이 방향을 판단하므로 이 부류의 충돌이 발생하지 않는다 |
| 2026-07-28 | 라우팅 앵커에 `vault-write` 1행 추가 (vault 기록 작성 → `vault-write`, 설계 입력으로 읽기는 §7 8항 유지) | CLAUDE.md | vault 쓰기 경로 신설(ADR 034) — description 자동 매칭만으로는 앵커 목록이 불완전해져 다른 스킬과 열거 기준이 어긋난다 |
